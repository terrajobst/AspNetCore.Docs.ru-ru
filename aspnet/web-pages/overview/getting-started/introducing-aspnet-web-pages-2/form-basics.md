---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: "Введение в ASP.NET Web Pages — основы HTML-формы | Документы Microsoft"
author: tfitzmac
description: "Этот учебник показывает основные сведения о том, как создать форму ввода и как обрабатывать введенные пользователем данные, при использовании веб-страниц ASP.NET (Razor). И теперь, которые..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: 97e4a2a1794dbdccf80f0b44c1246c743fa23019
ms.sourcegitcommit: 9a9483aceb34591c97451997036a9120c3fe2baf
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 11/10/2017
---
<a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="7e7e8-104">Общие сведения о веб-страницах ASP.NET — основы HTML-формы</span><span class="sxs-lookup"><span data-stu-id="7e7e8-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>
====================
<span data-ttu-id="7e7e8-105">по [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="7e7e8-106">Этот учебник показывает основные сведения о том, как создать форму ввода и как обрабатывать введенные пользователем данные, при использовании веб-страниц ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="7e7e8-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="7e7e8-107">И теперь, когда у вас есть базы данных, чтобы позволить пользователям находить определенные фильмы в базе данных используйте свои навыки формы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="7e7e8-108">Предполагается, что завершена ряда через [Общие сведения для отображения данных с помощью веб-страниц ASP.NET](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="7e7e8-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="7e7e8-109">Что вы узнаете следующее.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="7e7e8-110">Как создать форму с помощью стандартных элементов HTML.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="7e7e8-111">Как выполнить чтение пользователь ввода данных в форме.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="7e7e8-112">Предоставляет способ создания SQL-запроса, выборочно получает данные с помощью поиска терминов, пользователь.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="7e7e8-113">Как с полями, на странице «запомнить» ввод пользователя.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="7e7e8-114">Обсуждаются функции и технологии:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="7e7e8-115">Объект `Request`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-115">The `Request` object.</span></span>
> - <span data-ttu-id="7e7e8-116">SQL `Where` предложения.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="7e7e8-117">Что мы создадим</span><span class="sxs-lookup"><span data-stu-id="7e7e8-117">What You'll Build</span></span>

<span data-ttu-id="7e7e8-118">В предыдущем учебнике создана база данных, добавить данные к нему, а затем используйте `WebGrid` вспомогательный метод для отображения данных.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="7e7e8-119">В этом учебнике вы добавите поле поиска позволяет находить фильмы из определенного жанра или которого заголовок содержит любые word ввода.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="7e7e8-120">(Например, вы сможете найти все фильмы которого жанре — «Действие» или которого заголовок содержит «Харри» или «Adventure.»)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="7e7e8-121">После завершения работы с учебником, вы получите страницы, похожее на следующее.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Страница фильмов с поиском жанр и Title](form-basics/_static/image1.png)

<span data-ttu-id="7e7e8-123">Листинг часть страницы является такой же, как последний Учебник &mdash; сетки.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="7e7e8-124">Отличие может быть, что в сетке отображаются только фильмы производился поиск.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="7e7e8-125">О HTML-формы</span><span class="sxs-lookup"><span data-stu-id="7e7e8-125">About HTML Forms</span></span>

<span data-ttu-id="7e7e8-126">(Если у вас есть опыт создания HTML-форм и разницу между `GET` и `POST`, этот раздел можно пропустить.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="7e7e8-127">Содержит элементы пользовательского ввода форма &mdash; текстовые поля, кнопки, переключатели, флажки, раскрывающиеся списки и т. д.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="7e7e8-128">Пользователи заполнить эти элементы управления или сделайте выбор и отправить форму, путем нажатия кнопки.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="7e7e8-129">В этом примере показана базовый синтаксис формы HTML.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="7e7e8-130">При запуске этой разметки на странице создает простой формы, который выглядит как на этом рисунке:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Базовая форма HTML как готовый для просмотра в браузере](form-basics/_static/image2.png)

<span data-ttu-id="7e7e8-132">`<form>` Элемент заключает HTML-элементов, которые требуется отправить.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="7e7e8-133">(Является простой ошибки, чтобы добавить элементы на страницу, но затем забывают поместить их в `<form>` элемент.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="7e7e8-134">В этом случае ничего не отправляется.) `method` Атрибут предписывает браузеру том, как отправить данные, введенные пользователем.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="7e7e8-135">Это значение `post` при выполнении обновления на сервере или к `get` если только что выборка данных с сервера.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="7e7e8-136">**GET, POST и HTTP-команду, безопасность**</span><span class="sxs-lookup"><span data-stu-id="7e7e8-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="7e7e8-137">HTTP-протокола, браузеры и серверы используют для обмена данными, его основные операции чрезвычайно проста.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="7e7e8-138">Браузеры используют только несколько команд для выполнения запросов к серверам.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="7e7e8-139">При написании кода для веб-сайта, необходимо понимать эти команды и браузером и сервером их использовании.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="7e7e8-140">Использующимся наиболее часто используемых команд указаны следующие:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="7e7e8-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-141">`GET`.</span></span> <span data-ttu-id="7e7e8-142">Эта команда обозревателем для выборки нечто с сервера.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="7e7e8-143">Например, при вводе URL-адрес в адресную строку браузера браузер выполняет `GET` операцию запроса страницы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="7e7e8-144">Если страница содержит графики, браузер выполняет дополнительные `GET` операции для получения изображений.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="7e7e8-145">Если `GET` операция имеет для передачи данных на сервер, эти сведения передаются как часть URL-адреса в строке запроса.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="7e7e8-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-146">`POST`.</span></span> <span data-ttu-id="7e7e8-147">Браузер отправляет `POST` запроса для отправки данных будут добавлены или изменены на сервере.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="7e7e8-148">Например `POST` команда используется для создания записей в базе данных или изменение существующих.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="7e7e8-149">В большинстве случаев, заполните форму при нажатии кнопки "Отправить", обозреватель выполняет `POST` операцию.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="7e7e8-150">В `POST` операции, данные, передаваемые на сервер находится в основной области страницы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="7e7e8-151">Это важное различие между эти команды `GET` операции не должен изменять что-либо на сервере — или поместить его в виде немного более абстрактное `GET` операция не приведет к смене состояния на сервере.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="7e7e8-152">Можно выполнить `GET` операцию на те же ресурсы столько раз, сколько угодно, а не изменять эти ресурсы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="7e7e8-153">(A `GET` операция часто называется «безопасность», или использовать технический термин, является *идемпотентными*.) В отличие от этого, конечно `POST` запросить изменения, что-нибудь на сервере каждый раз при выполнении операции.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="7e7e8-154">Два примера будет проиллюстрировать это различие.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="7e7e8-155">При выполнении поиска с использованием механизма, например Bing или Google, заполните форму, которая состоит из одного текстового поля и затем нажмите кнопку поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="7e7e8-156">Браузер выполняет `GET` операции со значением, введенным в поле, переданная как часть URL-адрес.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="7e7e8-157">С помощью `GET` операцию для этого типа формы подходит, так как операции поиска не изменяется все ресурсы на сервере, он просто извлекает сведения.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="7e7e8-158">Теперь рассмотрим процесс упорядочения, что-либо сети.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="7e7e8-159">Введите сведения о заказе и нажмите кнопку "Отправить".</span><span class="sxs-lookup"><span data-stu-id="7e7e8-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="7e7e8-160">Эта операция будет `POST` запрос, поскольку операция приведет к изменению на сервере, например записи нового заказа, изменения в данные учетной записи и может быть других изменений.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="7e7e8-161">В отличие от `GET` операции, не может повторяться в `POST` запроса — Если вы выполнили, каждый раз повторно запрос, создается новый заказ на сервере.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="7e7e8-162">(В таких случаях веб-сайтов часто предупредит не нажимать кнопку отправки более одного раза или отключит кнопки "Отправить", чтобы не отправку формы случайно.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="7e7e8-163">В этом учебнике будет использоваться `GET` операции и `POST` операции для работы с HTML-формы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="7e7e8-164">Мы объясним, в том, почему каждого варианта команды, используемой содержится правильный.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="7e7e8-165">(Дополнительные сведения о HTTP-команд, см. в разделе [определений методов](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) статье на сайте консорциума W3C.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="7e7e8-166">Большинство элементов ввода пользователя находятся HTML `<input>` элементов.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="7e7e8-167">Они выглядят `<input type="type" name="name">,` где *тип* указывает тип элемента управления вводом пользователя, необходимо.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="7e7e8-168">Эти элементы являются наиболее частых из них:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-168">These elements are the common ones:</span></span>

- <span data-ttu-id="7e7e8-169">Текстовое поле:`<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="7e7e8-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="7e7e8-170">Флажок:`<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="7e7e8-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="7e7e8-171">Переключатель:`<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="7e7e8-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="7e7e8-172">Кнопки:`<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="7e7e8-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="7e7e8-173">Кнопка отправки:`<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="7e7e8-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="7e7e8-174">Можно также использовать `<textarea>` элемент для создания многострочного текстового поля и `<select>` элемент для создания раскрывающегося списка или прокручиваемого списка.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="7e7e8-175">(Дополнительные сведения о HTML элементах form, см. в разделе [HTML-формы и ввода](http://www.w3schools.com/html/html_forms.asp) на сайте W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="7e7e8-176">`name` Атрибут очень важен, так как оно получение значение элемента в более поздней версии, как вы увидите в ближайшее время.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="7e7e8-177">Особый интерес представляет, разработчик, выполняемых с входные данные пользователя.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="7e7e8-178">Отсутствует встроенный поведение связанную с этими элементами.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="7e7e8-179">Вместо этого необходимо получить значения, которые пользователь ввести или выбрать и с ними.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="7e7e8-180">Это будет рассмотрен в этом учебнике.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="7e7e8-181">**HTML5 и формами ввода**</span><span class="sxs-lookup"><span data-stu-id="7e7e8-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="7e7e8-182">Как правило, HTML находится в состоянии перехода и последнюю версию (HTML5) включает поддержку интуитивно способы, которыми пользователи будут вводить данные.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="7e7e8-183">Например в HTML5, (разработчик страницы) можно настроить страницы, необходимо, чтобы пользователь мог ввести дату.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="7e7e8-184">Затем, браузер может автоматически отображать календаре, а не требуют от пользователя ввести дату вручную.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="7e7e8-185">Тем не менее новые HTML5 и еще не поддерживается во всех браузерах.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="7e7e8-186">Веб-страницы ASP.NET поддерживает входных данных при условии, что обозреватель пользователя делает HTML5.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="7e7e8-187">Для получения представления о новых атрибутов для `<input>` в HTML5, см. в разделе [HTML &lt;ввода&gt; атрибут типа](http://www.w3schools.com/html/html_form_input_types.asp) на сайте W3Schools.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="7e7e8-188">Создание формы</span><span class="sxs-lookup"><span data-stu-id="7e7e8-188">Creating the Form</span></span>

<span data-ttu-id="7e7e8-189">В WebMatrix в **файлы** рабочей области, откройте *Movies.cshtml* страницы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="7e7e8-190">После закрывающей `</h1>` тега и перед открытием `<div>` тег `grid.GetHtml` call, добавьте следующую разметку:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="7e7e8-191">Эта разметка создает форму, которая содержит текстовое поле с именем `searchGenre` и кнопку submit.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="7e7e8-192">Текст поле и отправьте кнопка заключаются в `<form>` элемент которого `method` атрибута задано значение `get`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="7e7e8-193">(Следует помнить, что если не включить в текстовое поле, а кнопка внутри отправки `<form>` элемент, при нажатии кнопки ничего не будет отправлено.) Вы используете `GET` команда здесь так, как вы создаете формы, не вносит изменения на сервере — только в результате поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="7e7e8-194">(В предыдущем учебнике используется `post` метод, который является, как отправить изменения на сервер.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="7e7e8-195">Вы увидите, в следующем уроке еще раз.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="7e7e8-196">Запустите страницу.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-196">Run the page.</span></span> <span data-ttu-id="7e7e8-197">Несмотря на то, что любое поведение формы еще не определен, вы увидите, как оно выглядит:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Страница фильмов с поле поиска для жанр](form-basics/_static/image3.png)

<span data-ttu-id="7e7e8-199">Введите значение в текстовое поле, например «Комедия».</span><span class="sxs-lookup"><span data-stu-id="7e7e8-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="7e7e8-200">Нажмите кнопку **жанр поиска**.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="7e7e8-201">Запишите URL-адрес страницы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-201">Take note of the URL of the page.</span></span> <span data-ttu-id="7e7e8-202">Так как задать `<form>` элемента `method` атрибут `get`, введенное значение теперь является частью строки запроса в URL-адрес, следующим образом:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="7e7e8-203">Считывание значений формы</span><span class="sxs-lookup"><span data-stu-id="7e7e8-203">Reading Form Values</span></span>

<span data-ttu-id="7e7e8-204">Страница уже содержит код, который получает данные базы данных и отображение результатов в виде сетки.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="7e7e8-205">Теперь необходимо добавить некоторый код, который считывает значение текстового поля, чтобы можно было запустить SQL-запрос, включающий условие поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="7e7e8-206">Поскольку значение формы метод `get`, можно считывать значение, введенное в текстовое поле, с помощью кода следующим образом:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="7e7e8-207">`Request.QueryString` Объекта ( `QueryString` свойство `Request` объекта) включает в себя значения элементов, которые были отправлены в рамках `GET` операции.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="7e7e8-208">`Request.QueryString` Свойство содержит *коллекции* (список) значений, переданных в форме.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="7e7e8-209">Для получения всех индивидуальных значений, укажите имя нужного элемента.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="7e7e8-210">Вот почему необходимо иметь `name` атрибут `<input>` элемент (`searchTerm`), создающего текстового поля.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="7e7e8-211">(Дополнительные сведения о `Request` см. в разделе [боковой панели](#BKMK_TheRequestObject) более поздней версии.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="7e7e8-212">Это довольно простой, считывается значение текстового поля.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="7e7e8-213">Однако если пользователь ничего не введено вообще в текстовом поле, но щелчке **поиска** в любом случае, можно пропустить, щелкните, так как нет элементов для поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="7e7e8-214">Ниже приведен пример, в котором показано, как реализовать эти условия.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="7e7e8-215">(Не нужно добавить этот код еще предстоит выполнить, чуть позже.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="7e7e8-216">Разбивает теста таким образом:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="7e7e8-217">Получить значение `Request.QueryString["searchGenre"]`, а именно, введенный в значение `<input>` элемента с именем `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="7e7e8-218">Узнайте, пуст ли с помощью `IsEmpty` метод.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="7e7e8-219">Этот метод является стандартным способом, чтобы определить, содержит ли что-то (например, элемент form) значение.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="7e7e8-220">Но, необходима только в том случае, если он имеет *не* пуст, поэтому...</span><span class="sxs-lookup"><span data-stu-id="7e7e8-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="7e7e8-221">Добавить `!` оператор перед `IsEmpty` тестирования.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="7e7e8-222">( `!` Оператор означает логическое не).</span><span class="sxs-lookup"><span data-stu-id="7e7e8-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="7e7e8-223">На английском, вся `if` условие преобразуется в следующее: *Если формы searchGenre элемент не является пустым, затем...*</span><span class="sxs-lookup"><span data-stu-id="7e7e8-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="7e7e8-224">Этот блок задает рабочей области для создания запроса, использующего поисковому запросу.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="7e7e8-225">Вы сможете сделать, в следующем разделе.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="7e7e8-226">**Объект запроса**</span><span class="sxs-lookup"><span data-stu-id="7e7e8-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="7e7e8-227">`Request` Объект содержит всю информацию, браузер отправляет в приложение при страницы запрошенный или отправки.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="7e7e8-228">Этот объект включает все сведения, предоставленные пользователем, как значения текстового поля или файл для передачи.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="7e7e8-229">Она также включает все виды Дополнительные сведения, например файлы cookie, значения в строке запроса URL-адрес (если таковые имеются), путь к файлу страницы, на котором выполняется тип браузера, которое пользователь использует список языков, заданных в браузере и многое другое.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="7e7e8-230">`Request` Объект *коллекции* (список) значений.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="7e7e8-231">Получить отдельные значения из коллекции, указав его имя:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="7e7e8-232">`Request` Объект фактически предоставляет несколько подмножеств.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="7e7e8-233">Пример:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-233">For example:</span></span>
> 
> - <span data-ttu-id="7e7e8-234">`Request.Form`Предоставляет значения из элементов в указанной `<form>` элемент, если запрос является `POST` запроса.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="7e7e8-235">`Request.QueryString`предоставляет только значения в строке запроса URL-адрес.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="7e7e8-236">(В URL-адрес: `http://mysite/myapp/page?searchGenre=action&page=2`, `?searchGenre=action&page=2` раздел URL-адрес является строка запроса.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="7e7e8-237">`Request.Cookies`Коллекция предоставляет доступ к файлам cookie, отправленных браузер.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="7e7e8-238">Чтобы получить значение, которое известно в отправленной формы, можно использовать `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="7e7e8-239">Кроме того, можно использовать более конкретные версии `Request.Form["name"]` (для `POST` запросов) или `Request.QueryString["name"]` (для `GET` запросов).</span><span class="sxs-lookup"><span data-stu-id="7e7e8-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="7e7e8-240">Конечно *имя* имя элемента.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="7e7e8-241">Имя элемента, который вы хотите получить должно быть уникальным в пределах коллекции, которую вы используете.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="7e7e8-242">Вот почему `Request` объект предоставляет подмножества, например `Request.Form` и `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="7e7e8-243">Предположим, что страница содержит элемент формы с именем `userName` и *также* содержащее куки-файл с именем `userName`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="7e7e8-244">Если вы получаете `Request["userName"]`, он является неоднозначным, нужно ли значение формы или куки-файл.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="7e7e8-245">Тем не менее если вы получаете `Request.Form["userName"]` или `Request.Cookie["userName"]`, вы поступили явно значение, которое нужно получить.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="7e7e8-246">Рекомендуется указать точно и использовать подмножество `Request` , вас интересует, как `Request.Form` или `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="7e7e8-247">Для простых страниц, которые вы создаете в этом учебнике он, скорее всего, не менять действительно.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="7e7e8-248">Тем не менее, при создании более сложных страниц, с помощью явного версии `Request.Form` или `Request.QueryString` поможет вам избежать проблем, которые могут возникнуть, когда страница содержит формы (или несколько форм), файлы cookie, значения строки запроса и т. д.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="7e7e8-249">Создание запроса с помощью условия поиска</span><span class="sxs-lookup"><span data-stu-id="7e7e8-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="7e7e8-250">Теперь, когда вы знаете, как получить условия поиска, введенного пользователем, можно создать запрос, который его использует.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="7e7e8-251">Помните, что для получения всех элементов фильма из базы данных, вы используете запрос SQL, который выглядит как эта инструкция:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="7e7e8-252">Чтобы получить только определенные фильмы, необходимо использовать запрос, включающий `Where` предложения.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="7e7e8-253">Это предложение позволяет задавать условия, на котором строки возвращаются запросом.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="7e7e8-254">Ниже приведен пример:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="7e7e8-255">Стандартный формат имеет `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="7e7e8-256">Можно использовать различные операторы, кроме только что `=`, таких как `>` (больше), `<` (меньше), `<>` (не равно), `<=` (меньше или равно), т. д., в зависимости от того, что вы ищете.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="7e7e8-257">В случае, если вам интересно, инструкции SQL не учитывается регистр &mdash; `SELECT` совпадает со значением `Select` (или даже `select`).</span><span class="sxs-lookup"><span data-stu-id="7e7e8-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="7e7e8-258">Тем не менее, люди часто выгоду ключевые слова в инструкции SQL, таких как `SELECT` и `WHERE`, чтобы сделать его более удобным для чтения.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="7e7e8-259">Передача в качестве параметра условия поиска</span><span class="sxs-lookup"><span data-stu-id="7e7e8-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="7e7e8-260">Поиск конкретного жанр достаточно просто (`WHERE Genre = 'Action'`), но вы хотите иметь возможность искать любые жанра, вводимых пользователем.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="7e7e8-261">Чтобы сделать это, создаваемых в SQL-запрос, который включает заполнитель для значения для поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="7e7e8-262">Будет выглядеть, как эта команда:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="7e7e8-263">Является прототипом `@` символ за которыми следует ноль.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="7e7e8-264">Как можно догадаться, запрос может содержать несколько заполнители и будет называться `@0`, `@1`, `@2`и т. д.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="7e7e8-265">Настройка запроса и фактически передает значение, используйте код, аналогичный следующему:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="7e7e8-266">Это тот же том, что вы уже сделали для отображения данных в сетке.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="7e7e8-267">Единственными отличиями являются:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-267">The only differences are:</span></span>

- <span data-ttu-id="7e7e8-268">Запрос содержит заполнитель (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="7e7e8-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="7e7e8-269">Запрос помещается в переменную (`selectCommand`), прежде чем вы передали запрос непосредственно в `db.Query` метод.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="7e7e8-270">При вызове `db.Query` метода, передаче запроса и значение, используемое для заполнителя.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="7e7e8-271">(Если запрос содержит несколько заполнителей, следует передавать их все как отдельные значения для метода.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="7e7e8-272">Если поместить эти элементы друг с другом, отобразится следующий код:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="7e7e8-273">**Важно!**</span><span class="sxs-lookup"><span data-stu-id="7e7e8-273">**Important!**</span></span> <span data-ttu-id="7e7e8-274">С помощью заполнителей (таких как `@0`) для передачи значений в команды SQL является *крайне важно* для обеспечения безопасности.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="7e7e8-275">Он отображается, с заполнителями для данных переменной, способ является единственным способом, следует создавать команды SQL.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="7e7e8-276">Никогда не создания инструкции SQL путем объединения (объединения) литеральный текст и значения, которые можно получить от пользователя.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="7e7e8-277">Объединения вводимых пользователем данных в одну инструкцию SQL открывает веб-сайт в *атак путем внедрения кода SQL* когда злоумышленник отправляет значения на страницу, взломать базы данных.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="7e7e8-278">(Дополнительные статьи о [атаки SQL Injection](https://msdn.microsoft.com/en-us/library/ms161953.aspx) веб-сайте MSDN.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/en-us/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="7e7e8-279">Обновление страницы фильмов с кодом поиска</span><span class="sxs-lookup"><span data-stu-id="7e7e8-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="7e7e8-280">Теперь можно обновить код в *Movies.cshtml* файла.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="7e7e8-281">Чтобы начать, замените код в блоке кода в верхней части страницы следующим кодом:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="7e7e8-282">Различие состоит в том, что вы помещаете запроса в `selectCommand` переменной, передаваемой в `db.Query` позже.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="7e7e8-283">Помещение инструкции SQL в переменную можно изменить оператор, который является то, что вы выполните для выполнения поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="7e7e8-284">Кроме того, вы удалили эти две строки, в которых вы сможете вернуться к более поздней версии:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="7e7e8-285">Выполните запрос еще не требуется (то есть вызывать `db.Query`) и не нужно инициализировать `WebGrid` вспомогательный еще один.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="7e7e8-286">Вам предстоит выполнить эти действия, после вы разобрались, как инструкцию SQL должен выполняться.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="7e7e8-287">После этого перезаписанного блока можно добавить новый логику для обработки поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="7e7e8-288">Полный код будет выглядеть следующим образом.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-288">The completed code will look like the following.</span></span> <span data-ttu-id="7e7e8-289">Обновите код на странице, чтобы он соответствовал в этом примере:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="7e7e8-290">Страницы теперь выглядит следующим образом.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-290">The page now works like this.</span></span> <span data-ttu-id="7e7e8-291">Каждый раз при запуске страницы код открывает базу данных и `selectCommand` переменной присваивается инструкции SQL, которая возвращает все записи из `Movies` таблицы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="7e7e8-292">Этот код инициализирует также `searchTerm` переменной.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="7e7e8-293">Тем не менее если текущий запрос включает значение для `searchGenre` элемент, в коде устанавливается `selectCommand` к другому запросу, а именно на ту, которая включает в себя `Where` предложение для поиска жанра.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="7e7e8-294">Кроме того, задается `searchTerm` независимо от был передан для поля поиска (это может быть nothing).</span><span class="sxs-lookup"><span data-stu-id="7e7e8-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="7e7e8-295">Независимо от того, какие SQL инструкция находится в `selectCommand`, затем код вызывает `db.Query` для выполнения запроса, передавая ему независимо от возможности `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="7e7e8-296">Если никакая `searchTerm`, не имеет значения, так как в этом случае нет параметра для передачи значения для `selectCommand` несмотря на это.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="7e7e8-297">Наконец, этот код инициализирует `WebGrid` вспомогательный с помощью результатов запроса, так же, как раньше.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="7e7e8-298">Можно видеть, что путем помещения инструкции SQL и условие поиска в переменные, добавленные гибкость кода.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="7e7e8-299">Как вы увидите далее в этом учебнике, можно использовать этот базовой платформой и добавлять логику для различных типов поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="7e7e8-300">Тестирование возможность поиска по жанру</span><span class="sxs-lookup"><span data-stu-id="7e7e8-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="7e7e8-301">В WebMatrix, выполните *Movies.cshtml* страницы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="7e7e8-302">Вы увидите страницу с текстовым полем для жанра.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="7e7e8-303">Введите жанра, вы ввели по одной записи теста, а затем нажмите кнопку **поиска**.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="7e7e8-304">На этом этапе отобразится список фильмы, которые соответствуют этого жанра:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-304">This time you see a listing of just the movies that match that genre:</span></span>

![Страница фильмы, со списком после поиска жанр «Comedies»](form-basics/_static/image4.png)

<span data-ttu-id="7e7e8-306">Введите другой жанр и повторить поиск.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-306">Enter a different genre and search again.</span></span> <span data-ttu-id="7e7e8-307">Попробуйте ввести его, используя все и строчные буквы, чтобы вы увидите, что поиск выполняется без учета регистра.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="7e7e8-308">«Запоминание» пользователь ввел</span><span class="sxs-lookup"><span data-stu-id="7e7e8-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="7e7e8-309">Вы могли заметить, после ввода жанра и щелчке **поиска жанр**, можно было увидеть список для этого жанра.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="7e7e8-310">Тем не менее, текстового поля поиска был пустым &mdash; иными словами, он не запоминать был введен.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="7e7e8-311">Важно понять, почему это происходит.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="7e7e8-312">При отправке страницы браузер отправляет запрос на веб-сервер.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="7e7e8-313">Когда ASP.NET получает запрос, создает совершенно новый экземпляр страницы, выполняет код в нем и выводит страницу в браузере.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="7e7e8-314">Фактически, страницы не знает, что только вы работали с предыдущей версии самого себя.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="7e7e8-315">Все знает, что он получил запрос существовавшей некоторые данные формы в нем.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="7e7e8-316">При каждом запросе страницы &mdash; в первый раз или отправив его &mdash; для получения новой страницы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="7e7e8-317">Веб-сервер имеет недостаточно памяти последнего запроса.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="7e7e8-318">Ни один не ASP.NET, и ни один браузер.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="7e7e8-319">Только подключения между этими экземплярами отдельные страницы имеет все данные, передаваемые между ними.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="7e7e8-320">Например, при передаче страницы к новому экземпляру страницы можно получить данные формы, которое было отправлено с ранней версии экземпляра.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="7e7e8-321">(Другой способ передачи данных между страницами — использовать файлы cookie).</span><span class="sxs-lookup"><span data-stu-id="7e7e8-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="7e7e8-322">Формальные способ описания этой ситуации — сказать, что веб-страницы находятся *без сохранения состояния*.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="7e7e8-323">Веб-серверов и сами страницы и элементы на странице не поддерживать все сведения о состоянии предыдущей страницы.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="7e7e8-324">Веб-сайте был разработан таким образом, так как поддержание состояния для отдельных запросов может быстро исчерпать ресурсы веб-серверов, которые часто обработки тысяч, может быть даже сотни тысяч запросов в секунду.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="7e7e8-325">Поэтому поэтому текстовое поле было пустым.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-325">So that's why the text box was empty.</span></span> <span data-ttu-id="7e7e8-326">После отправки страницы ASP.NET создается новый экземпляр страницы и запустил кода и разметки.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="7e7e8-327">Произошла ничего не в этом коде, которые сообщили ASP.NET, чтобы поместить значение в текстовое поле.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="7e7e8-328">Поэтому ASP.NET не выполнять никаких действий и текстовое поле прорисован без значения в нем.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="7e7e8-329">Нет фактически простой способ обойти эту проблему.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="7e7e8-330">Жанра, введенный в текстовое поле *—* доступна в коде &mdash; в `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="7e7e8-331">Обновить разметки для текстового поля, чтобы `value` атрибут получает свое значение из `searchTerm`, как в этом примере:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="7e7e8-332">На этой странице вы могли также задать `value` атрибут `searchTerm` переменной, так как эта переменная содержит также жанр введено.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="7e7e8-333">Однако с помощью `Request` объекта, чтобы зафиксировать `value` атрибута, как показано ниже приведен стандартный способ выполнения этой задачи.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="7e7e8-334">(При условии, что требуется даже этого &mdash; в некоторых ситуациях может потребоваться отображать страницу *без* значения в полях.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="7e7e8-335">Все зависит происходящем вместе с приложением.)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="7e7e8-336">«Неизвестен» значение текстового поля, используемый для паролей.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="7e7e8-337">Было бы брешь в системе безопасности для заполнения поля пароля с помощью кода.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="7e7e8-338">Снова запустить страницу введите жанра, а щелкните **жанр поиска**.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="7e7e8-339">Это время не только вы увидите результаты поиска, но текстовое поле запоминает введенное время последнего:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Страница, показывающая, что текстовое поле «запомнить» предыдущей записи](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="7e7e8-341">Поиск любых слов в заголовке</span><span class="sxs-lookup"><span data-stu-id="7e7e8-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="7e7e8-342">Теперь можно выполнять поиск любого жанра, но можно также выполнить поиск по названию.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="7e7e8-343">Трудно получить заголовок совершенно верно при поиске, поэтому вместо этого можно искать слово, которое появляется в любом месте внутри заголовка.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="7e7e8-344">Чтобы сделать это в SQL, используйте `LIKE` операторы и синтаксис, следующим образом:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="7e7e8-345">Эта команда возвращает все фильмы, названия которых содержат «adventure».</span><span class="sxs-lookup"><span data-stu-id="7e7e8-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="7e7e8-346">При использовании `LIKE` оператор, включать символ-шаблон `%` как часть условия поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="7e7e8-347">Поиск `LIKE 'adventure%'` означает «'adventure'».</span><span class="sxs-lookup"><span data-stu-id="7e7e8-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="7e7e8-348">(Технически означает «Строка adventure следует ничего.») Аналогичным образом, условие поиска `LIKE '%adventure'` означает «все, за которым следует строка 'adventure'», являющееся другим способом, чтобы сказать «заканчивая 'adventure'».</span><span class="sxs-lookup"><span data-stu-id="7e7e8-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="7e7e8-349">Условие поиска `LIKE '%adventure%'` таким образом означает «с «adventure' в любом месте названия.»</span><span class="sxs-lookup"><span data-stu-id="7e7e8-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="7e7e8-350">(Технически «все в заголовке, следуют 'adventure', следует ничего.»)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="7e7e8-351">Внутри `<form>` элемента, добавьте следующую разметку прямо под закрывающей `</div>` тег для поиска жанр (непосредственно перед закрывающим тегом `</form>` элемент):</span><span class="sxs-lookup"><span data-stu-id="7e7e8-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="7e7e8-352">Код для обработки данного поиска аналогичен кода для поиска жанра, за исключением того, что необходимо собрать `LIKE` поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="7e7e8-353">Добавьте в блок кода в верхней части страницы, это `if` сразу после блока `if` блок для поиска жанра:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="7e7e8-354">Этот код использует ту же логику, вы видели ранее, за исключением того, что при поиске используются `LIKE` оператор и помещает кода «`%`» до и после поисковому запросу.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="7e7e8-355">Обратите внимание на то, каким образом было легко добавить другой поиск на страницу.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="7e7e8-356">Все, что было нужно было:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-356">All you had to do was:</span></span>

- <span data-ttu-id="7e7e8-357">Создание `if` блок, проверяется ли поле поиска соответствующего имел значение.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="7e7e8-358">Задать `selectCommand` переменной новой инструкции SQL.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="7e7e8-359">Задать `searchTerm` переменной значение для передачи в запрос.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="7e7e8-360">Вот блок полный код, который содержит новый логику для поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="7e7e8-361">Ниже приведена сводка делает этот код:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="7e7e8-362">Переменные `searchTerm` и `selectCommand` инициализируются вверху.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="7e7e8-363">Вы собираетесь установить эти параметры для соответствующих поисковому запросу (если таковые имеются) и соответствующей командой SQL, основанных на действия пользователя на странице.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="7e7e8-364">Поиск по умолчанию является простой регистр получения всех фильмов из базы данных.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="7e7e8-365">В тестах для `searchGenre` и `searchTitle`, в коде устанавливается `searchTerm` значение будет выполнять поиск.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="7e7e8-366">Эти блоки кода также задать `selectCommand` на соответствующую команду SQL для поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="7e7e8-367">`db.Query` Метод вызывается только один раз, используя любой команды SQL в `selectedCommand` и значение `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="7e7e8-368">Если условие поиска не задано (не жанр и нет заголовка word), значение `searchTerm` является пустой строкой.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="7e7e8-369">Однако это не имеет значения, так как в этом случае запрос не требует параметра.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="7e7e8-370">Тестирование функцию поиска заголовка</span><span class="sxs-lookup"><span data-stu-id="7e7e8-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="7e7e8-371">Теперь можно проверить страницы завершения поиска.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-371">Now you can test your completed search page.</span></span> <span data-ttu-id="7e7e8-372">Запустите *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="7e7e8-373">Введите жанра и нажмите кнопку **жанр поиска**.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="7e7e8-374">В сетке отображаются фильмы из этого жанра, как и перед.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="7e7e8-375">Введите слово заголовок и нажмите кнопку **заголовок поиска**.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="7e7e8-376">В сетке отображаются фильмы, к которым это слово в заголовке.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-376">The grid displays movies that have that word in the title.</span></span>

![После поиска «в заголовке страницы фильмов](form-basics/_static/image6.png)

<span data-ttu-id="7e7e8-378">Не указывайте оба поля и нажмите любую кнопку.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="7e7e8-379">В сетке отображаются все фильмов.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="7e7e8-380">Объединение запросов</span><span class="sxs-lookup"><span data-stu-id="7e7e8-380">Combining the Queries</span></span>

<span data-ttu-id="7e7e8-381">Можно заметить, что критерии поиска, который можно выполнить являются взаимоисключающими.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="7e7e8-382">Не удается найти заголовок и жанр одновременно, даже если оба поля поиска значений в них.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="7e7e8-383">Например нельзя выполнять поиск для всех действий фильмов, заголовок которого содержит «Adventure».</span><span class="sxs-lookup"><span data-stu-id="7e7e8-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="7e7e8-384">(Как страницы кодируется теперь при вводе значения для жанр и title, поиск заголовка получает приоритет). Чтобы создать поиск, который объединяет условия, пришлось бы создать SQL-запроса, который имеет следующий синтаксис:</span><span class="sxs-lookup"><span data-stu-id="7e7e8-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="7e7e8-385">И необходимо выполнить запрос с помощью инструкции следующим образом (Грубо говоря):</span><span class="sxs-lookup"><span data-stu-id="7e7e8-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="7e7e8-386">Создание логики для многих вариантов условия поиска можно получить несколько участвующих как можно видеть.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="7e7e8-387">Таким образом мы будем остановиться на этом.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="7e7e8-388">В ближайшее время</span><span class="sxs-lookup"><span data-stu-id="7e7e8-388">Coming Up Next</span></span>

<span data-ttu-id="7e7e8-389">В следующем уроке вы создадите страницы, использующей формы, чтобы пользователи могли добавлять фильмы в базу данных.</span><span class="sxs-lookup"><span data-stu-id="7e7e8-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="7e7e8-390">Полный пример для страницы фильма (обновляются с использованием поиска)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="7e7e8-391">Дополнительные ресурсы</span><span class="sxs-lookup"><span data-stu-id="7e7e8-391">Additional Resources</span></span>

- [<span data-ttu-id="7e7e8-392">Введение в программирование веб-ASP.NET с использованием синтаксиса Razor</span><span class="sxs-lookup"><span data-stu-id="7e7e8-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="7e7e8-393">[Предложение WHERE SQL](http://www.w3schools.com/sql/sql_where.asp) на сайте W3Schools</span><span class="sxs-lookup"><span data-stu-id="7e7e8-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="7e7e8-394">[Метод определения](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) статье на сайте W3C</span><span class="sxs-lookup"><span data-stu-id="7e7e8-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

>[!div class="step-by-step"]
<span data-ttu-id="7e7e8-395">[Назад](displaying-data.md)
[Вперед](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="7e7e8-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
