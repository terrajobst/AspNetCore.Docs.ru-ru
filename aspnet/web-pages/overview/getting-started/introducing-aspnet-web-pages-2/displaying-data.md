---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: "Введение в ASP.NET Web Pages — отображение данных | Документы Microsoft"
author: tfitzmac
description: "Этого учебника показано, как создать базу данных в WebMatrix и способ отображения данных базы данных на странице при использовании веб-страниц ASP.NET (Razor). Предполагается, что y..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: fdb9af0ba87c7802c63451ac7aa422e0020b5719
ms.sourcegitcommit: 9a9483aceb34591c97451997036a9120c3fe2baf
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 11/10/2017
---
<a name="introducing-aspnet-web-pages---displaying-data"></a>Общие сведения о веб-страницах ASP.NET - отображение данных
====================
по [Tom FitzMacken](https://github.com/tfitzmac)

> Этого учебника показано, как создать базу данных в WebMatrix и способ отображения данных базы данных на странице при использовании веб-страниц ASP.NET (Razor). Предполагается, что завершена ряда через [введение в программирование веб-страниц ASP.NET](../introducing-razor-syntax-c.md).
> 
> Что вы узнаете следующее.
> 
> - Инструкции по использованию средств WebMatrix для создания базы данных и таблицы базы данных.
> - Инструкции по использованию средств WebMatrix для добавления данных в базе данных.
> - Как отобразить данные из базы данных на странице.
> - Как запускать команды SQL в ASP.NET Web Pages.
> - Как настроить `WebGrid` вспомогательных для изменения порядка отображения данных и добавления разбиение по страницам и сортировка.
>   
> 
> Обсуждаются функции и технологии:
> 
> - WebMatrix инструменты для баз данных.
> - `WebGrid`Вспомогательный метод.


## <a name="what-youll-build"></a>Что мы создадим

В предыдущем учебнике были введены для веб-страниц ASP.NET (*.cshtml* файлы), с основами синтаксисом Razor и вспомогательных методов. В этом учебнике предстоит начать создание фактического веб-приложения, который будет использоваться для остальной части ряда. Это приложение фильма простое приложение, которое позволяет просматривать, добавлять, изменять и удалять сведения о фильмах.

После завершения работы с учебником, можно будет просматривать список фильмов, похожее на этой странице:

![WebGrid отображения, включает параметры, заданные для имен классов CSS](displaying-data/_static/image1.png)

Но чтобы начать, необходимо создать базу данных.

## <a name="a-very-brief-introduction-to-databases"></a>Очень краткое введение в базах данных

Этот учебник предоставит только briefest Знакомство с базами данных. Если у вас есть опыт базы данных, этот короткий раздел можно пропустить.

База данных содержит одну или несколько таблиц, содержащих сведения о &mdash; например, таблиц для клиентов, заказов и поставщиков или для учащихся, преподавателей, классы и оценок. Структурно таблицу базы данных является электронной таблицы. Представьте себе типичный адресной книги. Для каждой записи в адресной книге (то есть, для каждого пользователя), у вас есть несколько видов информации, такие как имя, Фамилия, адрес, адрес электронной почты и номер телефона.

![Образец таблицы базы данных как простую сетку](displaying-data/_static/image2.png)

(Строки, иногда называют *записей*, и столбцы, иногда называют *поля*.)

Для большинства таблиц базы данных таблица должна иметь столбец, содержащий уникальное значение, например номер заказчика, номер счета и т. д. Это значение называется таблицы *первичного ключа*, и использовать его для идентификации каждой строки в таблице. В примере идентификатор столбца является первичным ключом для адресной книги, показанный в предыдущем примере.

Большую часть работы, как в веб-приложения состоит из чтение данных из базы данных и его отображения на странице. Также часто будет собирать информацию от пользователей и добавить его в базу данных, или необходимо изменить существующие записи в базе данных. (Мы обсудим все эти операции во время этого учебника набора.)

Рабочие базы данных может быть огромным сложной и может потребовать специальных знаний. Для этого учебника набора, необходимо понимать только основные понятия, которые все объясняются при переходе.

## <a name="creating-a-database"></a>Создание базы данных

В WebMatrix имеются средства, которые позволяют легко создать базу данных и для создания таблиц в базе данных. (Структуры базы данных называется базы данных *схемы*.) Для этого учебника набора вы создадите базу данных, содержащую только одна таблица &mdash; фильмов.

Откройте WebMatrix, если это еще не сделано, а WebPagesMovies сайт, созданный в предыдущем учебнике.

В левой области щелкните **базы данных** рабочей области.

![Вкладка WebMatrix базы данных рабочей области](displaying-data/_static/image3.png)

Изменяется ленты для отображения задач, относящихся к базе данных. На ленте щелкните **новой базы данных**.

![«Новая база данных» кнопку на ленте WebMatrix](displaying-data/_static/image4.png)

WebMatrix создает базу данных SQL Server CE ( *.sdf* файл), имеет то же имя, что и сайт &mdash; *WebPagesMovies.sdf*. (Это не здесь, но можно переименовать файл к чему вам нравится, при условии, что он имеет *.sdf* расширение.)

## <a name="creating-a-table"></a>Создание таблицы

На ленте щелкните **новую таблицу**. WebMatrix конструкторе таблиц откроется на новой вкладке. (Новая таблица недоступен, если убедитесь, что новая база данных выбран в древовидном представлении слева.)

![«Новая таблица» кнопку на ленте WebMatrix](displaying-data/_static/image5.png)

В текстовом поле в верхней (где водяной знак говорит «Введите имя таблицы») введите «Фильмы».

![Ввод имени таблицы в конструкторе базы данных WebMatrix](displaying-data/_static/image6.png)

Области под именем таблицы является определение отдельных столбцов. Для *фильмов* таблицы в этом учебнике вы создадите только несколько столбцов: *идентификатор*, *заголовок*, *жанр*, и *год*.

В **имя** введите «ID». Введенное здесь активирует все элементы управления для нового столбца.

Чтобы **тип данных** и выберите **int**. Это значение указывает, что столбец ID будет содержать целочисленные данные (числа).

> [!NOTE]
> Мы не будет вызывать его всех здесь (гораздо), но вы можно использовать стандартные Windows клавиатуры для перехода в этой сетке. Например переместиться между полями, просто можно начните ввод, чтобы выбрать элемент в списке и т. д.


Клавиши табуляции **значение по умолчанию** поле (то есть, оставьте его пустым). Чтобы **— первичный ключ** флажок и выберите его. Этот параметр указывает базу данных *идентификатор* столбец будет содержать данные, которые определяют отдельных строк. (То есть каждая строка будет иметь уникальное значение в столбец идентификатора, которое можно использовать для поиска этой строки.)

Выберите **Is Identity** параметр. Этот параметр указывает базу данных, что он автоматически создает следующий последовательный номер для каждой новой строки. ( **Is Identity** работает параметр только в том случае, если вы выбрали также **— первичный ключ** вариант.)

Щелкните следующую строку сетки, или нажмите клавишу Tab дважды, чтобы завершить текущую строку. Либо жестов сохраняет текущую строку и начинается другая. Обратите внимание, что **значение по умолчанию** столбец теперь говорит **Null**. (Значение null значение по умолчанию для значения по умолчанию, так сказать.)

Завершив определение нового **идентификатор** столбца, конструктор будет выглядеть, как на этом рисунке:

![Конструктор баз данных WebMatrix, определив идентификатор столбца для таблицы фильмов](displaying-data/_static/image7.png)

Чтобы создать следующий столбец, щелкните поле в **имя** столбца. Введите «Title» для столбца, а затем выберите **nvarchar** для **тип данных** значение. «Var» часть **nvarchar** указывает базу данных, представляет собой строку, размер которого может отличаться от записи к записи данных для этого столбца. (Префикс «n» представляет «national», которое указывает, что поле может содержать символьные данные для любой алфавит или системы письма, то есть, это поле содержит данные в кодировке Юникод.)

При выборе **nvarchar**, появится другое поле для ввода максимальную длину для поля. Введите 50 на предположении, что без названия фильма, с которой будет работать в этом учебнике будет длиной более 50 символов.

Пропустить **значение по умолчанию** и снимите **Allow Nulls** параметр. Вы не хотите разрешить фильмы вводятся в базу данных, у которых нет заголовка базы данных.

После завершения работы и перейти к следующей строке, конструктор выглядит следующим образом на этом рисунке.

![Конструктор баз данных WebMatrix после определения столбец заголовков для таблицы фильмов](displaying-data/_static/image8.png)

Повторите эти шаги для создания столбца с именем «Жанр», за исключением длины, задайте для него значение только 30.

Создавать дополнительный столбец с именем «Год». Тип данных, выберите **nchar** (не **nvarchar**) и длину 4. За год Вы собираетесь использовать 4-значное число, например «1995» или «2010», поэтому не требуется столбец переменного размера.

Вот, как выглядит готовой разработки:

![Конструктор баз данных WebMatrix, определив все поля для таблицы фильмов](displaying-data/_static/image9.png)

Нажмите клавиши Ctrl + S или щелкните **Сохранить** на панели инструментов быстрого доступа. Закройте конструктор баз данных, закрыв вкладку.

## <a name="adding-some-example-data"></a>Добавление некоторых пример данных

Далее в этой серии учебника вы создадите страницы, в которой вводятся новые фильмы в форме. Теперь можно добавить некоторые данные примера, который затем можно отобразить на странице.

В **базы данных** рабочей области в WebMatrix, обратите внимание, что дерево, которое показывает, *.sdf* файла, созданного ранее. Откройте узел для нового *.sdf* файла, а затем откройте **таблиц** узла.

![WebMatrix базы данных рабочей области с деревом откройте таблицу фильмов](displaying-data/_static/image10.png)

Щелкните правой кнопкой мыши **фильмов** узел и выберите **данные**. WebMatrix открывает сетку, где можно ввести данные для *фильмов* таблицы:

![Сетка запись базы данных в WebMatrix (пусто)](displaying-data/_static/image11.png)

Нажмите кнопку **заголовок** столбца и введите «При Харри выполнены Sally». Переместите **жанр** столбца (можно использовать клавишу Tab) и введите «Romantic комедия». Переместите **год** столбца и введите «1989»:

![Сетка запись базы данных в WebMatrix с одной записью](displaying-data/_static/image12.png)

Нажмите клавишу ВВОД и WebMatrix сохраняет новый фильма. Обратите внимание, что **идентификатор** столбца было заполнено.

![Сетка запись базы данных в WebMatrix с одной записью и автоматически созданный идентификатор](displaying-data/_static/image13.png)

Введите другой фильма (например, «больше не с воздушного», «Драма», «1939»). Столбец идентификатора заполняется снова:

![Сетка запись базы данных в WebMatrix с две записи с автоматически генерируемым идентификаторы](displaying-data/_static/image14.png)

Введите третий фильма (например, «Ghostbusters», «Комедия»). В качестве эксперимента можно оставить **год** столбец остается пустым, и нажмите клавишу ВВОД. Поскольку вы не выбрано **Allow Nulls** параметра отображает ошибку в базе данных:

![Если не указан обязательный столбец значение к возникновению ошибки «Недопустимые данные»](displaying-data/_static/image15.png)

Нажмите кнопку **ОК** чтобы вернуться назад и исправить записи (года для «Ghostbusters» — 1984) и нажмите клавишу ВВОД.

Заполните несколько фильмов, пока имеется 8 или так. (Введите 8 упрощает для работы с разбиение по страницам в более поздней версии. Но если это слишком много, введите несколько сейчас). Фактические данные не имеет значения.

![Сетка запись базы данных в WebMatrix с либо записей](displaying-data/_static/image16.png)

При вводе всех фильмов без ошибок, последовательные значения Идентификаторов. При попытке сохранить запись фильма неполные номера Идентификаторов может оказаться последовательных. Если Да, это нормально. Номера не какого-либо специфические значения, а единственное, что важно, если они уникальным в *фильмов* таблицы.

Закройте вкладку, содержащую конструктор баз данных.

Теперь можно включить отображение этих данных на веб-странице.

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a>Отображение данных на странице с помощью вспомогательного элемента WebGrid

Для отображения данных на странице, которые планируется использовать `WebGrid` вспомогательный. Этот вспомогательный позволяет вывести в сетке или таблице (строки и столбцы). Как вы увидите, вы будете может обновить сетку с форматирование и другие функции.

Для запуска в сетке, вам придется написать несколько строк кода. Эти несколько строк будет использоваться как тип шаблона для почти всех доступа к данным, что делать в этом учебнике.

> [!NOTE]
> Есть множество параметров для отображения данных на странице; `WebGrid` вспомогательный — лишь одна. Мы выбрали его в этом учебнике, поскольку это самый простой способ отображения данных и является достаточно гибким. В следующей учебника вы увидите, как использовать дополнительные «manual» способ работы с данными в страницу, которая позволяет непосредственно контролировать способ отображения данных.


В левой области в WebMatrix, нажмите кнопку **файлы** рабочей области.

Вы создали новую базу данных находится в *приложения\_данные* папки. Папка не существует, WebMatrix создания новой базы данных. (Папка может существовать Если вспомогательные методы были установлены ранее.)

В древовидном представлении выберите корневой веб-сайта. Необходимо выбрать корневой веб-сайта; в противном случае новый файл могут быть добавлены в приложение\_папки данных.

На ленте щелкните **New**. В **выберите тип файла** выберите **CSHTML**.

В **имя** поле, введите имя новой страницы «Movies.cshtml»:

![«Выберите тип файла» диалоговое окно отображается на странице «Видео»](displaying-data/_static/image17.png)

Нажмите кнопку **ОК** кнопки. WebMatrix открывается новый файл с некоторыми элементами схемы, в нем. Сначала вы напишете код для перехода получения данных из базы данных. Затем вы добавите разметки страницы, чтобы фактически отображения данных.

### <a name="writing-the-data-query-code"></a>Написание кода запроса данных

В верхней части страницы между `@{` и `}` символов, введите следующий код. (Не забудьте ввести этот код между открывающей и закрывающей фигурными скобками.)

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

Первая строка открывает базу данных, созданную ранее, это всегда первый шаг перед действия с базой данных. Вы указываете `Database.Open` имя метода для открытия базы данных. Обратите внимание, что его не включить *.sdf* в имени. `Open` Метод предполагает, что он ищет *.sdf* файла (то есть *WebPagesMovies.sdf*) и что *.sdf* файл находится в *приложения\_ Данные* папки. (Ранее отмечалось, *приложения\_данные* папка является зарезервированным; этот сценарий является одно из мест, где ASP.NET делает предположения о существовании.)

При открытии базы данных, ссылка на него помещается в переменную с именем `db`. (Который может быть любое имя.) `db` Переменная доступна как в конечном итоге взаимодействия с базой данных.

Вторая строка фактически выполняет выборку данных базы данных с помощью `Query` метод. Обратите внимание на то, как работает этот код: `db` переменной содержит ссылку на открытую базу данных, и вызвать `Query` с помощью метода `db` переменной (`db.Query`).

Сам запрос является SQL `Select` инструкции. (Небольшой сведения, относящиеся к SQL, см. объяснение в более поздней версии.) В операторе `Movies` определяет таблицу для запроса. `*` Знак указывает, что запрос должен возвращать все столбцы из таблицы. (Можно также указать столбцы по отдельности, разделенные запятыми.)

Результаты запроса, если таковые имеются, возвращаются и становятся доступными в `selectedData` переменной. Опять же переменная может быть любое имя.

Наконец, третья строка сообщает ASP.NET, что вы хотите использовать экземпляр `WebGrid` вспомогательный. Вы создаете (*создать экземпляр*) вспомогательный объект с помощью `new` ключевое слово и передать его в результатах запроса через `selectedData` переменной. Новый `WebGrid` объекта вместе с результатами запроса к базе данных, становятся доступными в `grid` переменной. Вам потребуется полученный результат в момент, когда фактически отображения данных на странице.

На этом этапе открыть базу данных, вы получили данные, а также подготовленного `WebGrid` вспомогательное приложение с помощью этих данных. Следующий шаг — создание разметки на странице.

> [!TIP] 
> 
> **Язык SQL (SQL)**
> 
> SQL — это язык, который используется в большинстве реляционных баз данных для управления данными в базе данных. Он включает в себя команды, которые позволяют получать данные и обновить его, а, позволяют создавать, изменять и управлять данными в таблицах базы данных. SQL отличается от языка программирования (таких как C#). Определить базу данных, выберите SQL, и это задание базы данных, чтобы понять, как получить данные или выполнять задачи. Ниже приведены примеры некоторых команд SQL и их функции.
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> Первый `Select` инструкция возвращает все столбцы (заданные `*`) из *фильмов* таблицы.
> 
> Второй `Select` инструкции извлечет идентификатор, имя и цена столбцов из записей в *продукта* таблицы, значение столбцов которых цена превышает 10. Эта команда возвращает результаты в алфавитном порядке по значениям столбца Name. Если нет записей, соответствующих критериям цены, команда возвращает пустой набор.
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> Эта команда добавляет новую запись в *продукта* таблицы, задание столбца имени «Круассан», столбца описания «Подключением комфорт A» и цену для 1,99.
> 
> Обратите внимание, что при задании нечисловое значение, значение заключено в одинарные кавычки (не двойных кавычек, как в C#). Можно использовать эти кавычки вокруг значений текст или даты, но не вокруг номера.
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> Эта команда удаляет записи в *продукта* таблицы, которой столбец даты окончания срока действия предшествует 1 января 2008 года. (Предполагается, что *продукта* таблица имеет такой столбец, конечно.) Здесь вводится дата в формате мм/дд/гггг, но его следует вводить в формате, который используется для используемого языкового стандарта.
> 
> `Insert` И `Delete` команды не возвращают результирующие наборы. Вместо этого они возвращают число, которое показывает, сколько записей, затронутых командой.
> 
> Для некоторых из этих операций (например, вставка и удаление записей) процесс, который запрашивает операцию должна иметь соответствующие разрешения в базе данных. Вот почему для рабочих баз данных, обычно требуется ввести имя пользователя и пароль при подключении к базе данных.
> 
> Существует множество команд SQL, но все они соответствуют шаблону, как и команды, которые вы видите здесь. Можно использовать команды SQL для создания таблиц базы данных, число записей в таблице, расчет цен и выполнять многие другие операции.


### <a name="adding-markup-to-display-the-data"></a>Добавление разметки для отображения данных

Внутри `<head>` элемент, содержимое набора `<title>` элемент «Фильмы»:

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

Внутри `<body>` элемент страницы, добавьте следующий код:

[!code-html[Main](displaying-data/samples/sample3.html)]

Вот и все. `grid` Переменной является значение, созданное при создании `WebGrid` объекта в коде выше.

В древовидном представлении WebMatrix, щелкните правой кнопкой мыши страницу и выберите **запуск в браузере**. Вы увидите нечто похожее на этой странице:

![По умолчанию WebGrid вспомогательный выходные данные в таблице фильмов](displaying-data/_static/image18.png)

Щелкните ссылку заголовок столбца для сортировки по этому столбцу. Возможность отсортировать, щелкнув заголовок — это компонент, который встроен в **WebGrid** вспомогательный.

`GetHtml` Метод, как и предполагает его имя, создает разметку, которая отображает данные. По умолчанию `GetHtml` метод создает HTML `<table>` элемента. (Если требуется, можно проверить отрисовку, просмотрев источник страницы в браузере.)

## <a name="modifying-the-look-of-the-grid"></a>Изменение внешнего вида сетки

С помощью `WebGrid` поддержки, как только что было сделано легко, но дисплее plain. `WebGrid` Вспомогательные доступно множество параметров, позволяющих управлять отображением данных. Слишком много изучить в этом учебнике, но в данном разделе содержатся о том, некоторые из этих параметров. На следующих занятиях рассматривается в этой серии будет рассмотрена несколько дополнительных параметров.

### <a name="specifying-individual-columns-to-display"></a>Указав отдельные столбцы для отображения

Чтобы начать, можно указать, что нужно отобразить только определенные столбцы. По умолчанию, как показано выше, в сетке показаны все четыре столбцы из *фильмов* таблицы.

В *Movies.cshtml* файла, замените `@grid.GetHtml()` разметка, которую вы только что добавили со следующими:

[!code-css[Main](displaying-data/samples/sample4.css)]

Определить столбцы для отображения вспомогательным методом, включают `columns` параметр `GetHtml` метод и передайте его в коллекцию столбцов. В коллекции укажите каждый столбец для включения. Указать отдельный столбец для отображения, включая `grid.Column` и имя столбца данных, необходимо передать. (Эти столбцы должны быть включены в результаты запроса SQL — `WebGrid` вспомогательный не удается отобразить столбцы, которые не были возвращены запросом.)

Запустите *Movies.cshtml* страницу в браузере, снова и это время, появится экран, подобный приведенному ниже (Обратите внимание, что столбец не идентификатор):

![WebGrid окна, содержащего только выбранные столбцы](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a>Изменение внешнего вида сетки

Существует довольно много Дополнительные параметры для отображения столбцов, некоторые из которых будет изучена на следующих занятиях рассматривается в этом наборе. Теперь в этом разделе приведены способы, в котором можно стиль сетки в целом.

Внутри `<head>` раздел страницы, непосредственно перед закрывающим тегом `</head>` , добавьте следующие `<style>` элемента:

[!code-css[Main](displaying-data/samples/sample5.css)]

Эта разметка CSS определяет классов с именем `grid`, `head`, и т. д. Можно также поместить эти определения стиля в отдельном *.css* файла и связать его с страницы. (На самом деле вы выполните, далее в этом наборе данных учебника.) Но чтобы упростить действия для этого учебника, они внутри той же странице, на которой отображаются данные.

Теперь вы можете получить `WebGrid` вспомогательный метод для использования этих классов стиля. Вспомогательный объект имеет несколько свойств (например, `tableStyle`) для этого — Назначьте имя класса стиля CSS к ним, а имя этого класса подготавливается к просмотру как часть разметки, который подготавливается к просмотру вспомогательный метод.

Изменение `grid.GetHtml` разметки, так что он теперь выглядит как следующий код:

[!code-css[Main](displaying-data/samples/sample6.css)]

Новые возможности здесь — вы добавили `tableStyle`, `headerStyle`, и `alternatingRowStyle` параметры `GetHtml` метод. Эти параметры заданы имена стилей CSS, которые вы добавили некоторое время назад.

Откройте страницу, и на этом этапе отобразится сетка, выглядит гораздо меньше обычный, чем до:

![WebGrid отображения, включает параметры, заданные для имен классов CSS](displaying-data/_static/image20.png)

Чтобы увидеть, что `GetHtml` метод создан, можно просмотреть источник страницы в браузере. Не будем сейчас углубляться в подробности здесь, но важно то, указав параметры, как `tableStyle`, вызвавшего сетки для создания HTML-теги следующим образом:

`<table class="grid">`

`<table>` Тег был `class` добавить к нему атрибут, который ссылается на один правила CSS, добавленный ранее. Этот код показан базовый шаблон &mdash; различные параметры для `GetHtml` классы позволяют вам Справочник по методам CSS затем метод создает вместе с разметку. Что делать с помощью классов CSS принимает разработчик.

## <a name="adding-paging"></a>Добавление разбиения на страницы

В качестве последней задачи в этом учебнике разбиение на страницы добавляется в сетку. В данный момент это не проблема для отображения всех фильмов за один раз. Но при добавлении сотни фильмы отображения страницы будет получена long.

В коде страницы, измените строку, которая создает `WebGrid` объекта со следующим кодом:

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

Из перед отличается только что добавили `rowsPerPage` параметр, который имеет значение 3.

Запустите страницу. Сетка содержит 3 строки во время плюс ссылки навигации, которые позволяют пролистывать фильмов в базе данных.

![Отображение WebGrid с разбиением на страницы](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a>В ближайшее время

В следующем уроке будет рассмотрено использование кода Razor и C# для получения ввода пользователя в форме. Вы добавите поле поиска на странице фильмы, чтобы вы могли найти фильмов по заголовку или жанр.

## <a name="complete-listing-for-movies-page"></a>Полный список фильмов страницы

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a>Дополнительные ресурсы

- [Введение в программирование веб-ASP.NET с использованием синтаксиса Razor](https://go.microsoft.com/fwlink/?LinkID=202890)

>[!div class="step-by-step"]
[Назад](intro-to-web-pages-programming.md)
[Вперед](form-basics.md)
