---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: "Введение в ASP.NET Web Pages — обновление баз данных | Документы Microsoft"
author: tfitzmac
description: "Этого учебника показано, как обновить запись (изменение) существующей базы данных при использовании веб-страниц ASP.NET (Razor). Предполагается, что выполнены ряда th..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 01/02/2018
ms.topic: article
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: aabf572e254de9861719fdc502340353482919b4
ms.sourcegitcommit: 281f0c614543a6c3db565ea4655b70fe49b61d84
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 01/03/2018
---
<a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="6d1cf-104">Введение в ASP.NET Web Pages — обновление баз данных</span><span class="sxs-lookup"><span data-stu-id="6d1cf-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>
====================
<span data-ttu-id="6d1cf-105">по [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="6d1cf-106">Этого учебника показано, как обновить запись (изменение) существующей базы данных при использовании веб-страниц ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="6d1cf-107">Предполагается, что завершена ряда через [ввод данных с помощью форм с помощью веб-страниц ASP.NET](entering-data.md).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="6d1cf-108">Что вы узнаете следующее.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="6d1cf-109">Как выбрать отдельную запись в `WebGrid` вспомогательный.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="6d1cf-110">Как выполнить чтение одной записи из базы данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="6d1cf-111">Инструкции по предварительной загрузке формы со значениями из записи базы данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="6d1cf-112">Как обновить существующую запись в базе данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="6d1cf-113">Как сохранить сведения на странице без его отображения.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="6d1cf-114">Инструкции по использованию скрытое поле для хранения данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="6d1cf-115">Обсуждаются функции и технологии:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="6d1cf-116">`WebGrid` Вспомогательные.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="6d1cf-117">SQL `Update` команды.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="6d1cf-118">метод `Database.Execute`;</span><span class="sxs-lookup"><span data-stu-id="6d1cf-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="6d1cf-119">Скрытые поля (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-119">Hidden fields (`<input type="hidden">`).</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="6d1cf-120">Что мы создадим</span><span class="sxs-lookup"><span data-stu-id="6d1cf-120">What You'll Build</span></span>

<span data-ttu-id="6d1cf-121">В предыдущем учебнике вы узнали, как добавить запись в базе данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="6d1cf-122">Здесь вы узнаете, как отобразить запись для редактирования.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="6d1cf-123">В *фильмов* страницы, мы обновим `WebGrid` вспомогательный, потому что он отображается как **изменить** ссылку рядом с каждой фильма:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![WebGrid отображения, включая ссылку «Изменить» для каждого фильма](updating-data/_static/image1.png)

<span data-ttu-id="6d1cf-125">При нажатии кнопки **изменить** , его ссылке на другую страницу, где сведения о фильмах уже находится в форме:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Изменить отображение фильмов, который нужно изменить страницу фильма](updating-data/_static/image2.png)

<span data-ttu-id="6d1cf-127">Можно изменить какие-либо значения.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-127">You can change any of the values.</span></span> <span data-ttu-id="6d1cf-128">При отправке изменений кода в странице обновляет базу данных и вернуться в список фильмов.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="6d1cf-129">Эта часть процесса работает почти полностью идентично *AddMovie.cshtml* страницы, созданной в предыдущем учебнике, большая часть этого учебника будут знакомы.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="6d1cf-130">Существует несколько способов, которые можно реализовать способ редактирования отдельных фильма.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="6d1cf-131">Показанный подход был выбран, так как это легко реализовать и легкими для понимания.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="6d1cf-132">Добавление ссылки редактирования в описание фильма</span><span class="sxs-lookup"><span data-stu-id="6d1cf-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="6d1cf-133">Чтобы начать, мы обновим *фильмов* страницы, чтобы каждого фильма список также содержит **изменить** ссылку.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="6d1cf-134">Откройте *Movies.cshtml* файла.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="6d1cf-135">В основной области страницы, измените `WebGrid` разметки, добавив столбец.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="6d1cf-136">Ниже приведен измененный разметки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="6d1cf-137">Новый столбец является следующим:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="6d1cf-138">Цель этого столбца — отображать ссылку (`<a>` элемент), текст которых говорится «Изменить».</span><span class="sxs-lookup"><span data-stu-id="6d1cf-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="6d1cf-139">Идет речь требуется создать ссылку, которая выглядит следующим образом, при выполнении страницы с `id` значение отличается для каждого фильма:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="6d1cf-140">Эта ссылка будет вызывать страницу с именем *EditMovie*, и он будет передавать строки запроса `?id=7` к этой странице.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="6d1cf-141">Синтаксис для нового столбца может выглядеть немного сложным, но только потому, что он объединяет несколько элементов.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="6d1cf-142">Каждый отдельный элемент нетрудно.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-142">Each individual element is straightforward.</span></span> <span data-ttu-id="6d1cf-143">Если сосредоточиться на только что `<a>` элемент, появится эта разметка:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="6d1cf-144">Общие сведения о работе сетки: Сетка отображает строки, по одной для каждой записи базы данных и отображает столбцы для каждого поля в записи базы данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="6d1cf-145">Хотя создается каждой строки сетки `item` объект содержит запись базы данных (элемент) для этой строки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="6d1cf-146">Такой подход позволяет в коде для получения данных для этой строки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="6d1cf-147">Это, как показано ниже: выражение `item.ID` получает значение идентификатора текущего элемента базы данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="6d1cf-148">Может возникнуть какие-либо значения базы данных (название, Жанр или год) так же, как с помощью `item.Title`, `item.Genre`, или `item.Year`.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="6d1cf-149">Выражение `"~/EditMovie?id=@item.ID` объединяет жестко частью целевой URL-адрес (`~/EditMovie?id=`) с этим идентификатором динамически производных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="6d1cf-150">(Вы видели `~` оператор в предыдущем учебнике; это оператор ASP.NET, который представляет текущий корневой каталог веб-сайта.)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="6d1cf-151">Результатом является то, что эта часть разметки в столбце просто создает примерно следующую разметку во время выполнения:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="6d1cf-152">Естественно, фактическое значение `id` будет отличаться для каждой строки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="6d1cf-153">Создание пользовательского представления для столбца сетки</span><span class="sxs-lookup"><span data-stu-id="6d1cf-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="6d1cf-154">Вернемся к столбце сетки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-154">Now back to the grid column.</span></span> <span data-ttu-id="6d1cf-155">Три столбца изначально были в сетке отображаются только значения данных (название, Жанр и год).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="6d1cf-156">Указанный экран, передав имя столбца базы данных &mdash; , `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="6d1cf-157">Этот новый **изменить** столбца ссылки на другой.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="6d1cf-158">Вместо указания имени столбца, передается `format` параметра.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="6d1cf-159">Этот параметр позволяет определять разметки, `WebGrid` вспомогательный объект будет отображать вместе с `item` отображения столбцов данных выделены полужирным шрифтом или зеленый или в любом формате, который требуется значение.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="6d1cf-160">Например если требуется заголовок отображаться полужирным шрифтом, можно создать столбец, как в следующем примере:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="6d1cf-161">(Различные `@` символы, отображаемые в `format` свойство пометить переход между разметки и значение кода.)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="6d1cf-162">Если известно о `format` свойств, проще понять, как новый **изменить** скомпоновано столбца ссылки:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="6d1cf-163">Столбец содержит только *только* из разметку, представляющую связь, а также некоторые сведения (идентификатор), извлекается из записи базы данных для строки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="6d1cf-164">**Именованные параметры и позиционных параметров для метода**</span><span class="sxs-lookup"><span data-stu-id="6d1cf-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="6d1cf-165">Сколько раз когда вы вызвал метод и передается ему параметры, просто списка значения параметров, разделенных запятыми.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="6d1cf-166">Вот несколько примеров:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="6d1cf-167">Мы не упомянул проблему, когда видно этот код, но в каждом случае передача параметров в методы в определенном порядке &mdash; а именно, порядок определенные параметры в этом методе.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="6d1cf-168">Для `db.Execute` и `Validation.RequireFields`, получаемому при совместном порядок переданные значения, сообщение об ошибке при выполнении страницы или по крайней мере непредвиденных результатов.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="6d1cf-169">Очевидно необходимо знать для передачи параметров в порядке.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="6d1cf-170">(В WebMatrix, IntelliSense может помочь вам узнать выясните имя, тип и порядок параметров.)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="6d1cf-171">В качестве альтернативы для передачи значений в порядке, можно использовать *именованных параметров*.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="6d1cf-172">(Передача параметров в порядке называется с помощью *позиционные параметры*.) Для именованных параметров явно укажите имя параметра при передаче его значение.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="6d1cf-173">Вы уже использовали именованные параметры уже несколько раз в этих учебниках.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="6d1cf-174">Пример:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="6d1cf-175">и</span><span class="sxs-lookup"><span data-stu-id="6d1cf-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="6d1cf-176">Именованные параметры удобны для двух ситуациях, особенно в том случае, если метод принимает много параметров.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="6d1cf-177">Одна — Если требуется передать только один или два параметра, но не являются значения, которые необходимо передать из первой позиции в списке параметров.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="6d1cf-178">Другая ситуация — Если вы хотите сделать код более удобочитаемым путем передачи параметров в порядке, в котором вам больше смысла.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="6d1cf-179">Очевидно, что для использования именованных параметров, необходимо знать имена параметров.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="6d1cf-180">WebMatrix IntelliSense можно *Показать* имена, но его нельзя заполнить в настоящее время их автоматически.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>


## <a name="creating-the-edit-page"></a><span data-ttu-id="6d1cf-181">Создание страницы правки</span><span class="sxs-lookup"><span data-stu-id="6d1cf-181">Creating the Edit Page</span></span>

<span data-ttu-id="6d1cf-182">Теперь можно создавать *EditMovie* страницы.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="6d1cf-183">Когда пользователь нажимает кнопку **изменить** ссылку, они получим на этой странице.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="6d1cf-184">Создайте страницу с именем *EditMovie.cshtml* и замените в файле следующей разметкой:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="6d1cf-185">Это разметку и код аналогичен имеется *AddMovie* страницы.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="6d1cf-186">Нет небольшими различиями в текст для кнопки "Отправить".</span><span class="sxs-lookup"><span data-stu-id="6d1cf-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="6d1cf-187">Как и в *AddMovie* страницы, `Html.ValidationSummary` вызов, который будет отображать ошибки проверки, если таковые имеются.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="6d1cf-188">Это время пропускают вызовы `Validation.Message`, так как ошибки отображаются в сводку проверки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="6d1cf-189">Как отмечалось в предыдущем учебнике сводки проверки и сообщения об отдельных ошибках можно использовать в различных сочетаниях.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="6d1cf-190">Снова Обратите внимание, `method` атрибут `<form>` задан равным `post`.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="6d1cf-191">Как и в *AddMovie.cshtml* страницы, эта страница вносит изменения в базу данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="6d1cf-192">Таким образом, следует выполнить эту форму `POST` операции.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="6d1cf-193">(Дополнительные сведения о различиях между `GET` и `POST` операций, в разделе [GET, POST и HTTP глагол безопасности](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) боковой панели в учебнике HTML-форм.)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="6d1cf-194">Как было показано ранее учебника `value` атрибуты текстовых полей устанавливаются с помощью кода Razor для предварительной загрузки их.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="6d1cf-195">Это время, вы используете переменных как `title` и `genre` для этой задачи, а не `Request.Form["title"]`:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="6d1cf-196">Как и прежде, эта разметка будет предварительную загрузку значения текстового поля со значениями фильма.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="6d1cf-197">Вы увидите через несколько секунд, поэтому очень удобно использовать переменные в данный момент вместо использования `Request` объекта.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="6d1cf-198">Имеется также `<input type="hidden">` элемент на этой странице.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="6d1cf-199">Этот элемент сохраняет идентификатор фильма не станет видимым на странице.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="6d1cf-200">Идентификатор изначально передается на страницу с помощью значение строки запроса (`?id=7` или аналогичные в URL-адрес).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="6d1cf-201">С помещается значение идентификатора в скрытое поле, можно гарантировать, что он доступен при отправке формы, даже если отсутствует доступ к исходный URL-адрес страницы был вызван с.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="6d1cf-202">В отличие от *AddMovie* страницы кода для *EditMovie* страница имеет два различных функций.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="6d1cf-203">Первая функция, что при отображении страницы в первый раз (и *только* затем), код получает идентификатор фильма из строки запроса.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="6d1cf-204">Код, а затем по идентификатору чтения соответствующего фильма из базы данных и отображения (предварительно) его в текстовых полях.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="6d1cf-205">Вторая функция, что при нажатии **отправить изменения** кнопки, код должен считывать значения текстовых полей и их проверки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="6d1cf-206">Код также должен обновить элемент базы данных с новыми значениями.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="6d1cf-207">Этот метод аналогичен добавлению записи, как видно из *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="6d1cf-208">Добавление кода для чтения одной фильма</span><span class="sxs-lookup"><span data-stu-id="6d1cf-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="6d1cf-209">Для выполнения первой функции, добавьте следующий код в верхней части страницы:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="6d1cf-210">Большая часть кода находится внутри блока, который начинается `if(!IsPost)`.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="6d1cf-211">`!` Оператор означает «нет», поэтому выражение означает *Если этот запрос не является отправка post*, который является косвенным способом о том *Если этот запрос создается впервые, эта страница была выполнена*.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="6d1cf-212">Как отмечалось ранее, этот код следует запускать *только* при первом запуске страницы.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="6d1cf-213">Если не заключить код в `if(!IsPost)`, он будет выполнять каждый раз при страницы вызывается, является ли первый раз или в ответ на нажатие кнопки щелкните.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="6d1cf-214">Обратите внимание, что код включает `else` блок в данный момент.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="6d1cf-215">Как уже было сказано, когда мы представили `if` блоки, иногда требуется выполнять альтернативный код, если условие выполняется тестирование не так.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="6d1cf-216">Что происходит в случае здесь.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-216">That's the case here.</span></span> <span data-ttu-id="6d1cf-217">Если условие выполняется (то есть, если идентификатор, переданный на страницу — ОК), прочтением строки из базы данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="6d1cf-218">Тем не менее, если условие не проходит, `else` блок запускается и устанавливает код сообщение об ошибке.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="6d1cf-219">Проверка значения, переданного на страницу</span><span class="sxs-lookup"><span data-stu-id="6d1cf-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="6d1cf-220">Код использует `Request.QueryString["id"]` для получения идентификатора, который передается на страницу.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="6d1cf-221">Код гарантирует, что значение действительно было передано для идентификатора.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="6d1cf-222">Если значение не было передано, код задает ошибку проверки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="6d1cf-223">Этот код показывает другой способ проверить данные.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="6d1cf-224">В предыдущем учебнике вы работали с `Validation` поддержки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="6d1cf-225">Вы зарегистрировали полей для проверки и ASP.NET, автоматически не проверки и отображения ошибок с помощью `Html.ValidationMessage` и `Html.ValidationSummary`.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="6d1cf-226">Таким образом тем не менее, вы будете проверка не совсем пользовательского ввода.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="6d1cf-227">Вместо этого проверка значение, которое было передано на страницу из другого источника.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="6d1cf-228">`Validation` Поддержки, не делает.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="6d1cf-229">Таким образом, можно проверить значения самостоятельно, тестируя его с `if(!Request.QueryString["ID"].IsEmpty()`).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="6d1cf-230">Если проблема, ошибки можно отобразить с помощью `Html.ValidationSummary`, как это было сделано с `Validation` поддержки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="6d1cf-231">Чтобы сделать это, следует вызвать `Validation.AddFormError` и передать ему сообщения для отображения.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="6d1cf-232">`Validation.AddFormError`— это встроенный метод, который позволяет определять пользовательские сообщения, связывающих вход с помощью системы проверки, которые вы уже знакомы с.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="6d1cf-233">(Далее в этом руководстве будет рассказано о том, как сделать этот процесс проверки немного более надежным.)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="6d1cf-234">Убедившись в том, что идентификатор фильм, код считывает базы данных, выполняя только одной базы данных элемента.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="6d1cf-235">(Вы, вероятно, заметили общий шаблон для операций базы данных: откройте базу данных, определите инструкцию SQL и выполнить инструкцию.) На этот раз SQL `Select` инструкция включает в себя `WHERE ID = @0`.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="6d1cf-236">Поскольку этот идентификатор является уникальным, могут быть возвращены только одна запись.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="6d1cf-237">Запрос выполняется с помощью `db.QuerySingle` (не `db.Query`, как использовать список фильмов), и код помещает результат в `row` переменной.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="6d1cf-238">Имя `row` произвольное; можно назвать переменные любое название.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="6d1cf-239">Переменных, инициализируемых в верхней затем заполняются сведения о фильме для отображения этих значений в текстовых полях.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="6d1cf-240">Тестирование страницы правки (пока)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="6d1cf-241">Если вы хотите протестировать страницу, запустите *фильмов* теперь и нажмите кнопку **изменить** ссылку рядом с любой фильма.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="6d1cf-242">Вы увидите *EditMovie* указан страницу с подробными сведениями для фильма, выбранных:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Изменить отображение фильмов, который нужно изменить страницу фильма](updating-data/_static/image3.png)

<span data-ttu-id="6d1cf-244">Обратите внимание, что URL-адрес страницы содержится примерно `?id=10` (или некоторое другое число).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="6d1cf-245">Данный момент, когда работа **изменить** связывает *фильма* страницы рабочие страницы считывает идентификатор из строки запроса, которое работает, базы данных запроса для получения записи одного фильма.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="6d1cf-246">Можно изменить сведения о фильмах, но ничего не происходит при нажатии кнопки **отправить изменения**.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="6d1cf-247">Добавление кода для обновления фильма с изменения пользователя</span><span class="sxs-lookup"><span data-stu-id="6d1cf-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="6d1cf-248">В *EditMovie.cshtml* файл, чтобы реализовать функцию второго (при сохранении изменений), добавьте следующий код внутри закрывающую скобку `@` блока.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="6d1cf-249">(Если вы не знаете, куда поместить код, можно взглянуть на [полный листинг кода для страницы изменить фильма](#Complete_Page_Listing_for_EditMovie) , которая отображается в конце данного руководства.)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="6d1cf-250">Этот разметку и код подобен код в *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="6d1cf-251">Код находится в `if(IsPost)` заблокировать, так как этот код выполняется только в том случае, когда пользователь щелкает **отправить изменения** кнопку &mdash; то есть если (и только если) после отправки формы.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="6d1cf-252">В этом случае вы не используете такой тест `if(IsPost && Validation.IsValid())`— то есть, не объединенных оба теста с помощью и.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="6d1cf-253">На этой странице вы сначала определить, является отправки формы (`if(IsPost)`) и только затем зарегистрируйте полей для проверки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="6d1cf-254">Затем можно проверить результаты проверки (`if(Validation.IsValid()`).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="6d1cf-255">Поток немного отличается от *AddMovie.cshtml* страницы, но эффект одинаково.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="6d1cf-256">Получите значения свойств текстовых полей с помощью `Request.Form["title"]` и аналогичный код для других `<input>` элементов.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="6d1cf-257">Обратите внимание, что на этот раз код получает идентификатор фильма из скрытого поля (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="6d1cf-258">При запуске страницы в первый раз код получен код из строки запроса.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="6d1cf-259">Получить значение из скрытого поля, чтобы убедиться в том, что вы видите идентификатор фильм, который первоначально был отображен, в случае, если строка запроса каким-либо образом был изменен с тех пор.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="6d1cf-260">На самом деле важное отличие между *AddMovie* код и этот код находится в этом коде используется SQL `Update` инструкции, а не `Insert Into` инструкции.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="6d1cf-261">В следующем примере показано синтаксис SQL `Update` инструкции:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="6d1cf-262">Можно указать какие-либо столбцы в любом порядке и не обязательно для каждого столбца во время обновления `Update` операции.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="6d1cf-263">(Невозможно обновить идентификатор, как новую запись фактически это сэкономит запись и не разрешена для `Update` операцию.)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="6d1cf-264">**Важные** `Where` предложения с Идентификатором очень важен, поскольку это как базы данных знает, какая база данных записи, которую необходимо обновить.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="6d1cf-265">Если остановились `Where` предложение, обновит базу данных *каждого* записей в базе данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="6d1cf-266">В большинстве случаев, которые были бы аварии.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-266">In most cases, that would be a disaster.</span></span>


<span data-ttu-id="6d1cf-267">В коде значения для обновления передаются в инструкцию SQL с помощью заполнителей.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="6d1cf-268">Повторять, что мы указали перед: по соображениям безопасности *только* используйте заполнители для передачи значений в инструкции SQL.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="6d1cf-269">После код использует `db.Execute` для запуска `Update` инструкции, он перенаправляет на страницу списка, где можно просмотреть изменения.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="6d1cf-270">**Операторы разных SQL, различные методы**</span><span class="sxs-lookup"><span data-stu-id="6d1cf-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="6d1cf-271">Вы могли заметить, использовать немного разные методы для выполнения другой инструкции SQL.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="6d1cf-272">Для запуска `Select` запрос, потенциально возвращает несколько записей, использовать `Query` метода.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="6d1cf-273">Для запуска `Select` запрос, вы знаете, будет возвращать только один элемент базы данных, использовать `QuerySingle` метода.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="6d1cf-274">Чтобы выполнять команды, внести изменения, но которая не возвращает элементы базы данных, используйте `Execute` метод.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="6d1cf-275">Необходимо иметь различные методы, так как каждый из них возвращает различные результаты, как вы уже видели разницы между `Query` и `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="6d1cf-276">( `Execute` Метод фактически возвращает значение также &mdash; , количество строк базы данных, затронутых командой &mdash; , но вы игнорируется, пока.)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="6d1cf-277">Конечно `Query` метод может вернуть только одну строку базы данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="6d1cf-278">Однако ASP.NET всегда обрабатывает результаты `Query` метод как коллекция.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="6d1cf-279">Даже если метод возвращает только одну строку, необходимо извлечь, одну строку из коллекции.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="6d1cf-280">Таким образом, в ситуациях, где вы *знать* вы получите только одну строку, это немного удобнее всего использовать `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="6d1cf-281">Существует несколько методов, которые выполняют определенные типы операций базы данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="6d1cf-282">Можно найти список методов базы данных в [краткий справочник по API веб-страниц ASP.NET](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>


## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="6d1cf-283">Делая проверки для идентификатора более надежный</span><span class="sxs-lookup"><span data-stu-id="6d1cf-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="6d1cf-284">При первом запуске страницы, можно получить идентификатор фильма из строки запроса, чтобы получить из базы данных, фильма можно перейти.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="6d1cf-285">Внесенные в том, что фактически был переход искать, значение которого это было сделано с помощью следующего кода:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="6d1cf-286">Этот код позволяет убедитесь, что если пользователь получает *EditMovies* страницы, не выделяя фильма в *фильмов* страницы, страница может отображаться сообщение об ошибке удобной для пользователей.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="6d1cf-287">(В противном случае пользователи будут видеть ошибки, которая будет скорее всего лишь путают их).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="6d1cf-288">Однако такая проверка не самый надежный.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="6d1cf-289">Страница также может вызываться с этими ошибками:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="6d1cf-290">Идентификатор не является числом.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-290">The ID isn't a number.</span></span> <span data-ttu-id="6d1cf-291">Например, страницы может быть вызван с URL-адрес: `http://localhost:nnnnn/EditMovie?id=abc`.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="6d1cf-292">Идентификатор представляет собой число, но он ссылается на фильм, который не существует (например, `http://localhost:nnnnn/EditMovie?id=100934`).</span><span class="sxs-lookup"><span data-stu-id="6d1cf-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="6d1cf-293">Если вы хотите увидеть ошибки, возникающие в результате эти URL-адреса, запустите *фильмов* страницы.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="6d1cf-294">Выберите фильма для редактирования и измените URL-адрес *EditMovie* страницы на URL-адрес, содержащий буквенным Идентификатором или несуществующий фильма.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="6d1cf-295">Что следует сделать?</span><span class="sxs-lookup"><span data-stu-id="6d1cf-295">So what should you do?</span></span> <span data-ttu-id="6d1cf-296">Первый исправление заключается в том, чтобы убедиться в том, что не только код передается на страницу, но что идентификатор является целым числом.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="6d1cf-297">Изменить код для `!IsPost` тест должен выглядеть как в этом примере:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="6d1cf-298">Вы добавили второе условие, которое `IsEmpty` теста, связанные с `&&` (логическое и):</span><span class="sxs-lookup"><span data-stu-id="6d1cf-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="6d1cf-299">Может запомнить из [введение в программирование веб-страниц ASP.NET](../introducing-razor-syntax-c.md) учебник, в котором методы, например `AsBool` `AsInt` преобразование строки символов в другой тип данных.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="6d1cf-300">`IsInt` Метод (и другие, например `IsBool` и `IsDateTime`) похожи.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="6d1cf-301">Тем не менее, они проверяют только ли вы *можно* преобразовать строку, без фактического выполнения преобразования.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="6d1cf-302">Здесь можно по сути, говорит *Если можно преобразовать в целое значение строки запроса...* .</span><span class="sxs-lookup"><span data-stu-id="6d1cf-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="6d1cf-303">Другие возможные проблемы ищет фильм, который не существует.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="6d1cf-304">Код для получения фильма выглядит как следующий код:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="6d1cf-305">Если передать `movieId` значение `QuerySingle` метода, не соответствует фактическое фильм, ничего не возвращается, и следуйте инструкциям, (например, `title=row.Title`) приводят к ошибкам.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="6d1cf-306">Опять же имеет простого решения.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-306">Again there's an easy fix.</span></span> <span data-ttu-id="6d1cf-307">Если `db.QuerySingle` метод не возвращает результатов, `row` переменной будет иметь значение null.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="6d1cf-308">Чтобы можно было вернуть ли `row` переменная имеет значение null, прежде чем пытаться получить значения из него.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="6d1cf-309">Следующий код добавляет `if` в блок инструкций, которые получают значения из `row` объекта:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="6d1cf-310">С помощью этих двух дополнительные проверочные тесты страница становится более маркер проверки.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="6d1cf-311">Полный код для `!IsPost` ветви теперь выглядит как в следующем примере:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="6d1cf-312">Мы будем Обратите внимание еще раз, эта задача является рекомендуется использовать для `else` блока.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="6d1cf-313">Если не проходит тесты, `else` установки блоки сообщений об ошибках.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="6d1cf-314">Добавление ссылки, чтобы вернуться на страницу фильмов</span><span class="sxs-lookup"><span data-stu-id="6d1cf-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="6d1cf-315">Является конечным и полезные сведения для добавления ссылки к *фильмов* страницы.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="6d1cf-316">В потоке событий обычных пользователей начнется с *фильмов* и нажмите кнопку **изменить** ссылку.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="6d1cf-317">Это приводит к *EditMovie* страницы, где можно изменить фильма и нажмите кнопку.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="6d1cf-318">После кода обрабатывает изменения, он перенаправляет *фильмов* страницы.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="6d1cf-319">Тем не менее:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-319">However:</span></span>

- <span data-ttu-id="6d1cf-320">Пользователь может принять решение не вносите никаких изменений.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="6d1cf-321">Получения пользователя на эту страницу без предварительного нажатия кнопки **изменить** ссылку в *фильмов* страницы.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="6d1cf-322">В любом случае требуется позволяют легко вернуться к основной список.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="6d1cf-323">Это легко исправить &mdash; добавьте следующую разметку сразу после закрывающей `</form>` тег в разметке:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="6d1cf-324">Эта разметка использует тот же синтаксис для `<a>` элемент, который вы видели в другом месте.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="6d1cf-325">URL-адрес включает `~` означает «root веб-сайта».</span><span class="sxs-lookup"><span data-stu-id="6d1cf-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="6d1cf-326">Тестирование процесса обновления фильма</span><span class="sxs-lookup"><span data-stu-id="6d1cf-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="6d1cf-327">Теперь можно протестировать.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-327">Now you can test.</span></span> <span data-ttu-id="6d1cf-328">Запустите *фильмов* и нажмите кнопку **изменить** рядом с фильма.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="6d1cf-329">Когда *EditMovie* появится страница, внести изменения в файл фильма и щелкните **отправить изменения**.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="6d1cf-330">Когда появится список фильмов, убедитесь, что изменения отображаются.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="6d1cf-331">Чтобы убедиться в том, что проверка работает, нажмите кнопку **изменить** для другой фильма.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="6d1cf-332">При получении для *EditMovie* снимите **жанр** поле (или **год** поля или оба) и пытается отправить изменения.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="6d1cf-333">Вы увидите ошибку, как и следовало ожидать:</span><span class="sxs-lookup"><span data-stu-id="6d1cf-333">You'll see an error, as you'd expect:</span></span>

![Изменение отображения ошибок проверки страницы фильма](updating-data/_static/image4.png)

<span data-ttu-id="6d1cf-335">Нажмите кнопку **вернуться в список фильмов** ссылку, чтобы отменить внесенные изменения и вернуться к *фильмов* страницы.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="6d1cf-336">В ближайшее время</span><span class="sxs-lookup"><span data-stu-id="6d1cf-336">Coming Up Next</span></span>

<span data-ttu-id="6d1cf-337">В следующем уроке вы увидите, как удалить запись фильма.</span><span class="sxs-lookup"><span data-stu-id="6d1cf-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="6d1cf-338">Полный пример для страницы фильма (последнее обновление ссылок правки)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="6d1cf-339">Полный пример страницы для страницы правки фильма</span><span class="sxs-lookup"><span data-stu-id="6d1cf-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="6d1cf-340">Дополнительные ресурсы</span><span class="sxs-lookup"><span data-stu-id="6d1cf-340">Additional Resources</span></span>

- [<span data-ttu-id="6d1cf-341">Введение в программирование веб-ASP.NET с синтаксисом Razor</span><span class="sxs-lookup"><span data-stu-id="6d1cf-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](introducing-razor-syntax-c.md)
- <span data-ttu-id="6d1cf-342">[Инструкции SQL UPDATE](http://www.w3schools.com/sql/sql_update.asp) на сайте W3Schools</span><span class="sxs-lookup"><span data-stu-id="6d1cf-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

>[!div class="step-by-step"]
<span data-ttu-id="6d1cf-343">[Назад](entering-data.md)
[Вперед](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="6d1cf-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
