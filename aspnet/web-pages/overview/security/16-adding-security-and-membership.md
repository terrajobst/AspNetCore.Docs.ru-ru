---
uid: web-pages/overview/security/16-adding-security-and-membership
title: Добавление безопасности и членство в ASP.NET Web Pages (Razor) сайта | Документы Microsoft
author: tfitzmac
description: В этой главе показано, как защитить веб-сайта таким образом, чтобы некоторые страницы доступны только для тех, кто вход. (Вы также увидите Создание tha страницы...
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/24/2014
ms.topic: article
ms.assetid: 7a77c2c0-deea-4290-a9c3-97958891758e
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/security/16-adding-security-and-membership
msc.type: authoredcontent
ms.openlocfilehash: 351368a356a71e85d4abfdceac8d4f84e0b217f4
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/06/2018
---
<a name="adding-security-and-membership-to-an-aspnet-web-pages-razor-site"></a>Добавление безопасности и членство в ASP.NET веб-сайт страниц (Razor)
====================
по [Tom FitzMacken](https://github.com/tfitzmac)

> В этой статье объясняется, как защитить несколько страниц, доступные только для пользователей, войдите на веб-сайт ASP.NET Web Pages (Razor). (Вы также увидите Создание страниц, которые становятся доступным всем.)
> 
> **Что вы узнаете следующее.** 
> 
> - Инструкции по созданию веб-сайт, который имеет страницу регистрации и входа в систему, поэтому для некоторых страниц можно ограничить доступ только элементам.
> - Инструкции по созданию общих и доступны только для элемента страницы.
> - Определение роли, которые являются группами, которые имеют различные права на веб-узле, и как назначать роли пользователям.
> - Инструкции по использованию CAPTCHA для предотвращения создания элемента учетных записей автоматического программами (программы-роботы).
>   
> 
> Существуют следующие функции ASP.NET, представленные в статье:
> 
> - WebMatrix **начального сайта** шаблона.
> - `WebSecurity` Вспомогательный и `Roles` класса.
> - `ReCaptcha` Вспомогательные.
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a>Версии программного обеспечения, используемая в этом учебнике
> 
> 
> - Веб-страниц ASP.NET (Razor) 2
> - WebMatrix 3
> - Библиотека вспомогательных методов ASP.NET Web


Можно настроить веб-сайта, чтобы пользователи могут войти в него &#8212; то есть, чтобы сайт поддерживает *членства*. Это может быть удобно по многим причинам. Например у узла может быть страниц, которые должны быть доступны только для членов. В некоторых случаях может потребоваться пользователям входить в систему, чтобы отправить отзыв или оставьте комментарий.

Даже если веб-сайт поддерживает членства, пользователей и не обязательно требуется войти, прежде чем использовать некоторые страницы на сайте. Пользователей, которые не вошли в называются *анонимным пользователям*.

Пользователь может зарегистрировать на веб-сайт и может затем войдите на сайт. Веб-сайт требует имя пользователя (адрес электронной почты) и пароль для подтверждения того, пользователи, себя выдает. Вход и подтверждение идентификатора пользователя этот процесс известен как *проверки подлинности*.

Можно выполнить настройку безопасности и членство разными способами:

- Если вы используете WebMatrix, легким способом является создание как новый сайт на основе **начального сайта** шаблона. Этот шаблон уже настроена для обеспечения безопасности и членство и уже страницу регистрации, страницы входа и т. д.

    Узел, созданный с помощью шаблона также имеет параметр, чтобы дать пользователям возможность выполнить вход с помощью внешнего узла как Facebook, Google, и Twitter.
- Если вы хотите добавить к существующему сайту безопасности или если вы не хотите использовать **начального сайта** шаблона, можно создать собственную страницу регистрации, страницы входа и т. д.

Эта статья посвящена первый вариант &mdash; Добавление безопасности с помощью **начального сайта** шаблона. Он также предоставляет некоторые основные сведения о реализации собственного безопасности и затем предоставляет ссылки на дополнительные сведения о том, как сделать это. Также есть сведения о способах включения внешних имен входа, который описан более подробно в отдельной статье.

## <a name="creating-website-security-using-the-starter-site-template"></a>Создание с помощью шаблона начальный узел безопасности веб-сайта

В WebMatrix, можно использовать **начального сайта** шаблона для создания веб-сайта, который содержит следующее:

- База данных, используемая для хранения имен пользователей и паролей для членов.
- Страница регистрации, где можно зарегистрировать анонимных пользователей (новый).
- Страница входа и выхода из системы.
- Страница восстановления и сброса пароля.

Следующая процедура описывает, как создать сайт и настроить его.

1. Запуск WebMatrix и в **быстрый запуск** выберите **сайта из шаблона**.
2. Выберите **начального сайта** шаблона и нажмите кнопку **ОК**. WebMatrix создает новый сайт.
3. В левой области щелкните **файлы** селектор рабочей области.
4. В корневой папке веб-сайта, откройте  *\_AppStart.cshtml* файл, который является особым файлом, который содержит глобальные параметры. Она содержит некоторые инструкции, закомментированы с помощью `//` символов:

    [!code-csharp[Main](16-adding-security-and-membership/samples/sample1.cs)]

    Настройка этих инструкций `WebMail` вспомогательного приложения, который может использоваться для отправки электронной почты. Система членства может использовать электронной почты для отправки подтверждающих сообщений при регистрации пользователей или если нужно изменить свой пароль. (Например, после регистрации пользователей, получают сообщение электронной почты, содержащее ссылку, их можно щелкнуть, чтобы завершить процесс регистрации.)

    Отправка электронной почты требуется доступ к SMTP-сервера, как описано в [Добавление электронной почты для узла веб-страниц ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202899). Параметры электронной почты будут храниться в этот центральный  *\_AppStart.cshtml* так, чтобы не нужно их код несколько раз на каждой странице, можно отправить по электронной почте. (Не нужно настроить параметры протокола SMTP для настройки регистрации базы данных требуется только параметры SMTP, если требуется проверять подлинность пользователей из их псевдоним электронной почты, пользователи могут сбросить забытый пароль.)
5. Раскомментируйте инструкций, удалив `//` перед каждого из них.

    Если вы не хотите настроить подтверждение по электронной почте, можно пропустить этот шаг и следующий шаг. Если не заданы параметры SMTP, новая учетная запись немедленно доступно без подтверждения адреса электронной почты.
6. Измените следующие параметры с электронной почтой в коде:

   - Задать `WebMail.SmtpServer` имя SMTP-сервера, у вас есть право.
   - Оставить `WebMail.EnableSsl` значение `true`. Этот параметр обеспечивает защиту учетных данных, отправляемых на сервер SMTP, путем их шифрования.
   - Задать `WebMail.UserName` к имени пользователя для учетной записи сервера SMTP.
   - Задать `WebMail.Password` пароль пользователя для учетной записи сервера SMTP.
   - Задать `WebMail.From` на собственный адрес электронной почты. Это адрес электронной почты отправителя сообщения.

     > [!NOTE] 
     > 
     > **Совет** Дополнительные сведения о значениях для этих свойств см. в разделе [Настройка параметров электронной почты](https://go.microsoft.com/fwlink/?LinkID=202906#configuring_email_settings) в [поведение Настройка сайта для веб-страниц ASP.NET](https://go.microsoft.com/fwlink/?LinkID=202906).
7. Сохраните и закройте  *\_AppStart.cshtml*.
8. Запустите *Default.cshtml* страницы в браузере.

    ![безопасность членства-2](16-adding-security-and-membership/_static/image1.png)

   > [!NOTE]
   > Если появится сообщение об ошибке, информирующее о том, что свойство должно быть экземпляром `ExtendedMembershipProvider`, сайт не может быть настроен для использования системы членства веб-страниц ASP.NET (SimpleMembership). Это может случиться, если по-другому, чем на локальном сервере настроен сервер поставщика услуг размещения. Чтобы устранить эту проблему, добавьте следующий элемент на сайт *Web.config* файла:
   > 
   > [!code-xml[Main](16-adding-security-and-membership/samples/sample2.xml)]
   > 
   > Добавьте этот элемент в качестве дочернего элемента `<configuration>` элемент и как одноранговый по `<system.web>` элемента.
9. В правом верхнем углу страницы нажмите кнопку **зарегистрировать** ссылку. *Register.cshtml* -страница.
10. Введите имя пользователя и пароль и нажмите кнопку **зарегистрировать**.

    ![безопасность членства-3](16-adding-security-and-membership/_static/image2.png)

    При создании веб-сайта из **начального сайта** шаблона, базу данных с именем *StarterSite.sdf* был создан на сайте *приложения\_данные* папки. Во время регистрации сведения о пользователе добавляется в базу данных. Если задать значения SMTP используется, чтобы можно было завершить регистрации сообщение отправляется на адрес электронной почты.

    ![безопасность членства-4](16-adding-security-and-membership/_static/image3.png)
11. Перейдите в программу электронной почты и найти сообщение, который будет содержать код подтверждения и гиперссылку на сайт.
12. Щелкните гиперссылку, чтобы активировать свою учетную запись. Подтверждение гиперссылка открывает страницу подтверждения регистрации.

    ![безопасность членства-5](16-adding-security-and-membership/_static/image4.png)
13. Нажмите кнопку **входа** ссылку и снова войдите с помощью учетной записи, которое вы зарегистрировали.

      После входа в **входа** и **зарегистрировать** заменяются ссылками **выхода** ссылку. Имя для входа, отображаются в виде ссылки. (Эта ссылка позволяет перейти на страницу, где можно изменить пароль.)

      ![безопасность членства-6](16-adding-security-and-membership/_static/image5.png)

      > [!NOTE]
      > По умолчанию веб-страниц ASP.NET отправляет учетные данные на сервер в виде открытого текста (в виде удобочитаемого текста). Рабочего веб-сайта следует использовать защищенный HTTP (https://, также известные как *протокол SSL* или SSL) для шифрования секретной информации, которыми обмениваются с сервером. Вы можете электронной почты требуется отправлять сообщения с использованием SSL, установив `WebMail.EnableSsl=true` как в предыдущем примере. Дополнительные сведения о протоколе SSL см. в разделе [обеспечение безопасности веб-соединений: сертификаты SSL и https://](https://go.microsoft.com/fwlink/?LinkId=208660).

## <a name="additional-membership-functionality-in-the-site"></a>Дополнительные членством на сайте

Данный узел содержит другие функции, позволяющие управлять их учетными записями пользователей. Пользователи могут сделать следующее.

- Измените пароли. После входа в систему, он сможет щелкнуть имя пользователя (которое представляет собой ссылку). Это ведет к странице которой можно создать новый пароль (*Account/ChangePassword.cshtml*).
- Восстановите забытый пароль. На странице входа является ссылкой (**забыли пароль?**), принимающий пользователей на страницу (*Account/ForgotPassword.cshtml*) где они могут ввести адрес электронной почты. Сайт отправляет их сообщение электронной почты, содержащее ссылку, их можно щелкнуть, чтобы задать новый пароль (*Account/PasswordReset.cshtml*).

Также можно предоставить пользователям можно также в систему с помощью внешнего узла, как описано ниже.

<a id="Creating_a_Members-Only_Page"></a>
## <a name="creating-a-members-only-page"></a>Создание страницы только для членов

На данный момент, любой пользователь, перейдите на любую страницу веб-сайта. Однако может потребоваться страниц, которые доступны только пользователям, вошедшим в (то есть членам). ASP.NET позволяет создавать страницы, которые можно осуществлять только члены вошедшего в систему. Как правило если доступ к странице только для членов анонимных пользователей, вы перенаправит их на страницу входа.

В этой процедуре вы создадите папки, которая будет содержать страниц, которые доступны только для зарегистрированных пользователей.

1. В корневом узле создайте новую папку. (На ленте щелкните стрелку, расположенную под **New** и выберите **новая папка**.)
2. Имя новой папки *элементы*.
3. Внутри *элементы* папки, создайте новую страницу с именем *MembersInformation.cshtml*.
4. Замените существующее содержимое следующий код и разметку:

    [!code-cshtml[Main](16-adding-security-and-membership/samples/sample3.cshtml)]

    Этот код проверяет `IsAuthenticated` свойство `WebSecurity` объекта, который возвращает `true` , если пользователь выполнил вход. Если пользователь не выполнил вход, этот код вызывает `Response.Redirect` для отправки пользователю *Login.cshtml* страницы в *учетной записи* папки.

    URL-адрес перенаправления содержит `returnUrl` запрос строковое значение, которое использует `Request.Url.LocalPath` для присвоения пути к текущей страницы. Если задать `returnUrl` значение в строке запроса следующим образом (и если возвращаемый URL-адрес является локальным путем), страница входа вернет пользователей на эту страницу после входа.

    В этом коде также задаются  *\_SiteLayout.cshtml* страницу как его макет. (Дополнительные сведения о макете страницы см. в разделе [Создание согласованного макета в веб-узлы веб-страниц ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202891).)
5. Запустите узел. Если вы по-прежнему входили, щелкните **выхода** кнопку в верхней части страницы.
6. В браузере запросите страницу */члены/MembersInformation*. Например URL-адрес может выглядеть следующим образом:

    `http://localhost:38366/Members/MembersInformation`

    (Номер порта (38366), скорее всего, будет отличаются в URL-адрес.)

    Вы будете перенаправлены на *Login.cshtml* страницы, так как вы не вошли в.
7. Войдите, используя учетную запись, созданную ранее. Будет перенаправлен к *MembersInformation* страницы. Так как вход настоящее время вы видите содержимое страницы.

Чтобы обеспечить безопасный доступ к нескольким страницам, это можно сделать:

- Добавьте проверку безопасности на каждой странице.
- Создание  *\_PageStart.cshtml* страницы в папке, где сохранить защищенные страницы и добавить существует проверки безопасности.  *\_PageStart.cshtml* страницы действует в качестве глобального страницы для всех страниц в папке. Этот способ является более подробно в [поведение Настройка сайта для веб-страниц ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202906#Using__PageStart.cshtml_to_Restrict_Folder_Access).

## <a name="creating-security-for-groups-of-users-roles"></a>Создание системы безопасности для групп пользователей (роли)

Если узел содержит много элементов, не эффективным для проверки разрешений для каждого пользователя по отдельности, перед ее см. на странице. Вместо этого можно сделать — создавать группы, или *ролей*, что отдельные члены принадлежат. Затем можно проверить разрешения на основе роли. В этом разделе вы создадите &quot;администратора&quot; роли, а затем создать страницы, доступной для пользователей, работающих в (которые входят в различные) этой роли.

Система членства ASP.NET настроена для поддержки ролей. Однако в отличие от регистрации членства и входа в систему **начального сайта** шаблон не содержит страницы, которые помогают управлять ролями. (Управление ролями является задачи администрирования, а не задачу пользователя.) Тем не менее можно добавлять группы непосредственно в базе данных членства в WebMatrix.

1. В WebMatrix, щелкните **баз данных** селектор рабочей области.
2. В левой области откройте *StarterSite.sdf* открытым узлом **таблиц** узел, а затем дважды щелкните *веб-страниц\_ролей* таблицы.

    ![безопасности, членства, 7](16-adding-security-and-membership/_static/image6.png)
3. Добавьте роль с именем &quot;администратора&quot;. *RoleId* поле будет заполнено автоматически. (Он является первичным ключом и ему было присвоено быть Идентификация поля, как описано в статье [введение в работу с базой данных в веб-узлы веб-страниц ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202893).)
4. Обратите внимание что такое значение *RoleId* поля. (Если это первая роль, которые вы определяете, она будет 1.)

    ![безопасность членства-8](16-adding-security-and-membership/_static/image7.png)
5. Закрыть *веб-страниц\_ролей* таблицы.
6. Откройте *UserProfile* таблицы.
7. Запомните или запишите *UserId* значение одного или нескольких пользователей в таблице и затем закройте таблицу.
8. Откройте *веб-страниц\_UserInRoles* таблицы и введите *UserID* и *RoleID* значение в таблицу. Например, чтобы поместить пользователя 2 в &quot;администратора&quot; роли, можно ввести эти значения:

    ![безопасность членство — 9](16-adding-security-and-membership/_static/image8.png)
9. Закрыть *веб-страниц\_UsersInRoles* таблицы.

    Теперь, когда у вас есть определенные роли, можно настроить страницы, который доступен пользователям в этой роли.
10. В корневой папке веб-сайта, создайте новую страницу с именем *AdminError.cshtml* и заменить существующее содержимое следующим кодом. Это будет, если они не разрешен доступ к странице пользователи будут перенаправлены на страницу.

    [!code-cshtml[Main](16-adding-security-and-membership/samples/sample4.cshtml)]
11. В корневой папке веб-сайта, создайте новую страницу с именем *AdminOnly.cshtml* и замените существующий код следующим кодом:

    [!code-cshtml[Main](16-adding-security-and-membership/samples/sample5.cshtml)]

    `Roles.IsUserInRole` Возвращает `true` Если текущий пользователь является членом указанной роли (в данном случае роль «Администратор»).
12. Запустите *Default.cshtml* в браузере, но не заносить в журнал. (Если вы уже вошли в, выйдите из системы.)
13. В адресной строке браузера добавьте *AdminOnly* в URL-АДРЕСЕ. (Другими словами, запрос *AdminOnly.cshtml* файл.) Вы будете перенаправлены на *AdminError.cshtml* страницы, так как вы не вошли в настоящее время в качестве пользователя в &quot;администратора&quot; роли.
14. Вернитесь к *Default.cshtml* и войдите в систему как пользователь, добавляемый &quot;администратора&quot; роли.
15. Перейдите к *AdminOnly.cshtml* страницы. Это время, вы увидите страницу.

## <a name="preventing-automated-programs-from-joining-your-website"></a>Запрет присоединения веб-сайте автоматические программы

Страницы входа не будет останавливать автоматические программы (иногда называют *веб-роботов* или *программы-роботы*) регистрироваться в веб-сайта. Эта процедура описывает Включение ReCaptcha тест для страницы регистрации.

![/media/38777/ch16securitymembership-18.jpg](16-adding-security-and-membership/_static/image1.jpg)

1. Регистрация веб-сайта в ReCaptcha.Net ([http://recaptcha.net](http://recaptcha.net)). После завершения регистрации, вы сможете получить открытый ключ и закрытый ключ.
2. Добавить библиотека вспомогательных методов ASP.NET Web веб-сайта, как описано в [Установка помощников в узел веб-страниц ASP.NET](https://go.microsoft.com/fwlink/?LinkId=252372), если это еще не сделано.
3. В *учетной записи* папку, откройте файл с именем *Register.cshtml*.
4. В коде в верхней части страницы найдите следующие строки и раскомментируйте их, удаляя `//` символов комментария:

    [!code-csharp[Main](16-adding-security-and-membership/samples/sample6.cs)]
5. Замените `PRIVATE_KEY` с помощью собственного закрытого ключа ReCaptcha.
6. В разметке страницы, удалите `@*` и `*@` символы комментария из примерно следующие строки в разметке страницы:

    [!code-cshtml[Main](16-adding-security-and-membership/samples/sample7.cshtml)]
7. Замените `PUBLIC_KEY` с помощью ключа.
8. Если он еще не удалены уже, удалить `<div>` элемент, который содержит текст, который начинается с «Включить проверку CAPTCHA...». (Полностью удалите `<div>` элемент и его содержимое.)

9. Запустите *Default.cshtml* в браузере. Если вы выполнили вход в веб-узел, щелкните **выхода** ссылку.
10. Нажмите кнопку **зарегистрировать** связывание и тестовых регистрацию, с помощью тест CAPTCHA.

     ![безопасность членства-10](16-adding-security-and-membership/_static/image9.png)

Дополнительные сведения о `ReCaptcha` вспомогательного приложения, в разделе [с помощью ваш веб-сайт ASP.NET с помощью CATPCHA для предотвращения автоматической программ (программы-роботы)](https://go.microsoft.com/fwlink/?LinkId=251967).

<a id="Additional_Resources"></a>
## <a name="letting-users-log-in-using-an-external-site"></a>Предоставление пользователям возможности выполнить вход с помощью внешнего узла

**Начального сайта** шаблон включает в себя код и разметку, которая позволяет пользователям вход с использованием Facebook, Windows Live, Twitter, Google или Yahoo. По умолчанию эта функция не включена. Общая процедура использования информирующее пользователей в систему с помощью этих внешних поставщиков это:

- Решите, какой из внешних сайтов требуется поддерживать.
- При необходимости перехода на узел и настроить приложение имени входа. (Например, необходимо сделать это, чтобы разрешить имена входа Facebook.)
- На сайте настройте поставщика. В большинстве случаев достаточно раскомментируйте некоторый код в  *\_AppStart.cshtml* файла.
- Добавьте разметку страницы регистрации, который позволит пользователям ссылку на внешний сайт для протоколирования. Обычно можно скопировать разметки необходима и немного изменить текст.

Пошаговые инструкции можно найти в разделе [Включение имени входа из внешних узлов в сайт веб-страниц ASP.NET](https://go.microsoft.com/fwlink/?LinkId=251969).

После входа пользователя с другого сайта, пользователь возвращается к веб-узла и *связывает* этого имени входа с веб-узла. По сути это создает запись членства на узле для внешней учетной записи пользователя. Это позволяет использовать обычные средства членства (такие как роли) с внешней учетной записи.

## <a name="adding-security-to-an-existing-website"></a>Добавление защиты в существующем веб-сайте

Процедуре выше в этой статье основывается на использование **начального сайта** шаблон в качестве основы для обеспечения безопасности веб-сайта. Если непрактично запуск из **начального сайта** шаблона или чтобы скопировать соответствующие страницы с узла, на основе этого шаблона, можно применить один и тот же тип безопасности в вашем собственном узле с помощью кодирования самостоятельно. Создать одинаковые типы страниц — регистрации, имя входа и так далее — и затем использовать классы и вспомогательные методы для настройки членства.

Это основная процедура описана в записи блога [самый простой способ реализации безопасности ASP.NET Razor](http://mikepope.com/blog/DisplayBlog.aspx?permalink=2240). Большая часть работы выполняется с помощью следующих методов и свойств `WebSecurity` поддержки:

- [WebSecurty.UserExists](https://msdn.microsoft.com/library/webmatrix.webdata.websecurity.userexists(v=vs.99).aspx), [WebSecurity.CreateUserAndAccount](https://msdn.microsoft.com/library/webmatrix.webdata.websecurity.createuserandaccount(v=vs.99).aspx). Эти методы позволяют определить, является ли кто-то уже зарегистрирован и зарегистрировать их.
- [WebSecurty.IsAuthenticated](https://msdn.microsoft.com/library/webmatrix.webdata.websecurity.isauthenticated(v=vs.99).aspx). Это свойство позволяет определить, вошел ли текущий пользователь. Это полезно для перенаправления пользователей на страницу входа, если они еще не зарегистрировались.
- [WebSecurity.Login](https://msdn.microsoft.com/library/webmatrix.webdata.websecurity.login(v=vs.99).aspx), [WebSecurity.Logout](https://msdn.microsoft.com/library/webmatrix.webdata.websecurity.logout(v=vs.99).aspx). Эти методы входа пользователя in или out.
- [WebSecurity.CurrentUserName](https://msdn.microsoft.com/library/webmatrix.webdata.websecurity.currentusername(v=vs.99).aspx). Это свойство можно использовать для отображения имени текущего пользователя вошедшего в систему (если пользователь вошел в).
- [WebSecurity.ConfirmAccount](https://msdn.microsoft.com/library/gg569286(v=vs.99).aspx). Этот метод полезен, если вы настроили подтверждение по электронной почте для регистрации. (Сведения приводятся в записи блога [с помощью подтверждения для безопасности веб-страниц ASP.NET](http://mikepope.com/blog/DisplayBlog.aspx?permalink=2267).)

Для управления ролями, можно использовать [ролей](https://msdn.microsoft.com/library/gg538398(v=vs.99).aspx) и [членства](https://msdn.microsoft.com/library/gg569035(v=vs.99).aspx) классов, как описано в записи блога.

## <a name="additional-resources"></a>Дополнительные ресурсы

- [Настройка поведения в масштабах сайта](https://go.microsoft.com/fwlink/?LinkId=202906)
- [Обеспечение безопасности веб-соединений: Сертификаты SSL и https://](https://go.microsoft.com/fwlink/?LinkId=208660)
- [Самый простой способ реализации безопасности ASP.NET Razor](http://mikepope.com/blog/DisplayBlog.aspx?permalink=2240) и [с помощью подтверждения для безопасности веб-страниц ASP.NET](http://mikepope.com/blog/DisplayBlog.aspx?permalink=2267). Это записи в блогах, описывающие способы реализации возможностей членства ASP.NET без использования **начального сайта** шаблона.
- [Включение входа с внешних сайтов на сайте веб-страниц ASP.NET](https://go.microsoft.com/fwlink/?LinkId=251969)
- [Справочник по API класс WebSecurity](https://msdn.microsoft.com/library/webmatrix.webdata.websecurity(v=vs.99)) (MSDN)
- [Справочник по API класс SimpleRoleProvider](https://msdn.microsoft.com/library/webmatrix.webdata.simpleroleprovider(v=vs.99)) (MSDN)
- [Справочник по API класс SimpleMembershipProvider](https://msdn.microsoft.com/library/webmatrix.webdata.simplemembershipprovider(v=vs.99)) (MSDN)
