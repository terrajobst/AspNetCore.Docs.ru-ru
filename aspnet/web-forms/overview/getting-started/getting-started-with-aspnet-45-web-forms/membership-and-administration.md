---
uid: web-forms/overview/getting-started/getting-started-with-aspnet-45-web-forms/membership-and-administration
title: Членство и Администрирование | Документы Microsoft
author: Erikre
description: Этот учебник ряд описываются основы построения приложения веб-форм ASP.NET с помощью ASP.NET 4.5 и Microsoft Visual Studio Express 2013 для мы...
ms.author: aspnetcontent
manager: wpickett
ms.date: 09/08/2014
ms.topic: article
ms.assetid: 732a2316-e49f-4f72-becd-0cd72f14457e
ms.technology: dotnet-webforms
ms.prod: .net-framework
msc.legacyurl: /web-forms/overview/getting-started/getting-started-with-aspnet-45-web-forms/membership-and-administration
msc.type: authoredcontent
ms.openlocfilehash: 166bc642ea2083f455be0648e424f0b0ae3b082c
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/10/2018
ms.locfileid: "30889635"
---
<a name="membership-and-administration"></a>Членство и Администрирование
====================
по [Эрик Reitan](https://github.com/Erikre)

[Загрузите образец проекта Wingtip Toys (C#)](http://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) или [загрузить электронную (PDF)](http://download.microsoft.com/download/0/F/B/0FBFAA46-2BFD-478F-8E56-7BF3C672DF9D/Getting%20Started%20with%20ASP.NET%204.5%20Web%20Forms%20and%20Visual%20Studio%202013.pdf)

> Этот учебник ряд описываются основы построения с помощью ASP.NET 4.5 и Microsoft Visual Studio Express 2013 для веб-приложения веб-форм ASP.NET. Visual Studio 2013 [проект с исходным кодом C#](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) доступен по следующему этого учебника ряда.


Этого учебника показано, как обновить Wingtip Toys образец приложения для добавления пользовательской роли и использовать ASP.NET Identity. Он также показано, как реализовать страницу администрирования, из которого пользователь с пользовательской роли можно добавлять и удалять продукты с веб-сайта.

[ASP.NET Identity](../../../../identity/overview/getting-started/introduction-to-aspnet-identity.md) — система членства, используемые для построения веб-приложения ASP.NET, доступный в ASP.NET 4.5. ASP.NET Identity используется в шаблоне проекта Visual Studio 2013 веб-форм, а также шаблоны для [ASP.NET MVC](../../../../mvc/index.md), [веб-API ASP.NET](../../../../web-api/index.md), и [ASP.NET одностраничного приложения](../../../../single-page-application/index.md). Кроме того можно установить системы ASP.NET Identity, если начать с пустой веб-приложение, с помощью NuGet. Однако в этой серии учебника использовать **Web Forms**projecttemplate, включая системы ASP.NET Identity. ASP.NET Identity позволяет легко интегрировать данные профиля конкретного пользователя с данными приложения. Кроме того ASP.NET Identity позволяет выбирать модель сохраняемости для профилей пользователей в приложении. Данные можно сохранить в базе данных SQL Server или другом хранилище данных, включая *NoSQL* хранилища данных, таких как таблицы хранилища Windows Azure.

Этот учебник построен на работу с предыдущим учебником под названием «Оформления заказа и оплаты с PayPal» Wingtip Toys учебника ряда.

## <a name="what-youll-learn"></a>Что вы узнаете следующее.

- Как добавить пользовательскую роль и пользователя для приложения с помощью кода.
- Как ограничить доступ к папке администрирования и страницы.
- Как предоставить средства навигации для пользователя, которому принадлежит пользовательской роли.
- Использование привязки модели для заполнения [DropDownList](https://msdn.microsoft.com/library/system.web.ui.webcontrols.dropdownlist(v=vs.110).aspx) элемента управления с помощью категорий продуктов.
- Отправка файла для веб-приложения с помощью [FileUpload](https://msdn.microsoft.com/library/system.web.ui.webcontrols.fileupload(v=vs.110).aspx) элемента управления.
- Инструкции по использованию проверяющих элементов управления для реализации проверки входных данных.
- Добавление и удаление продуктов из приложения.

## <a name="these-features-are-included-in-the-tutorial"></a>Эти компоненты включены в учебнике:

- ASP.NET Identity
- Конфигурация и авторизации
- Привязка модели
- Ненавязчивой проверки

Веб-форм ASP.NET предоставляет возможности членства. С помощью шаблона по умолчанию, у вас есть встроенные членством, можно сразу же использовать при запуске приложения. Этого учебника показано, как использовать ASP.NET Identity для добавления пользовательской роли и назначить этой роли пользователя. Вы узнаете, как ограничить доступ к папке администрирования. Вы добавите страницы в папку администрирования, который позволяет пользователям с пользовательской роли для добавления и удаления продуктов и просмотреть продукта после его добавления.

## <a name="adding-a-custom-role"></a>Добавление пользовательской роли

ASP.NET Identity можно добавить пользовательскую роль и назначение для этой роли, с помощью кода.

1. В **обозревателе решений**, щелкните правой кнопкой мыши *логику* папки и создайте новый класс.
2. Имя для нового класса *RoleActions.cs*.
3. Измените код так, чтобы он выглядит следующим образом:  

    [!code-csharp[Main](membership-and-administration/samples/sample1.cs?highlight=8)]
4. В **обозревателе решений**откройте *Global.asax.cs* файла.
5. Изменить *Global.asax.cs* файла путем добавления кода, выделены желтым цветом, чтобы он выглядит следующим образом:  

    [!code-csharp[Main](membership-and-administration/samples/sample2.cs?highlight=11,26-28)]
6. Обратите внимание, что `AddUserAndRole` подчеркнуто красной чертой. Дважды щелкните AddUserAndRole кода.  
   Будет подчеркнуто буква «A» в начале выделенного метода.
7. Наведите указатель мыши на буква «A» и нажмите кнопку пользовательский Интерфейс, позволяющий создать заглушку метода `AddUserAndRole` метод. 

    ![Членство и Advministration - создать заглушку метода](membership-and-administration/_static/image1.png)
8. Выберите параметр под названием:  
    `Generate method stub for "AddUserAndRole" in "WingtipToys.Logic.RoleActions"`
9. Откройте *RoleActions.cs* файл из *логику* папки.  
   `AddUserAndRole` Метод был добавлен в файл класса.
10. Изменить *RoleActions.cs* файла путем удаления `NotImplementedeException` и добавления кода, выделяются желтым цветом, выглядит следующим образом:  

    [!code-csharp[Main](membership-and-administration/samples/sample3.cs?highlight=5-7,15-51)]

Приведенный выше код сначала задает контекст базы данных для базы данных членства. База данных членства также хранятся как *.mdf* файла в *приложения\_данные* папки. Можно просмотреть этой базы данных, когда первый пользователь вошел в это веб-приложение. 

> [!NOTE] 
> 
> Если вы хотите хранить данные членства вместе с данными продукта, можно с помощью одного **DbContext** , используемый для хранения данных продукта в приведенном выше коде.


 *Внутренней* ключевое слово — это модификатор доступа для типов (классы) и члены типов (например, методы или свойства). Внутренние типы или члены доступны только внутри файлов, содержащихся в той же сборке *(.dll* файла). При построении приложения, файл сборки *(.dll*) создается, содержит код, выполняемый при запуске приложения. 

Объект `RoleStore` object, который предоставляет управление ролями, создан на основе контекста базы данных.

> [!NOTE] 
> 
> Обратите внимание, что при `RoleStore` создания объекта он использует универсальный `IdentityRole` типа. Это означает, что `RoleStore` только могут содержать `IdentityRole` объектов. Также с помощью универсальных шаблонов, ресурсы памяти обрабатываются лучше.


Далее, `RoleManager` объекта, основанного на `RoleStore` объект, который вы только что создали. `RoleManager` объект предоставляет связанные с ролями API, который может использоваться для автоматического сохранения изменений `RoleStore`. `RoleManager` Только могут содержать `IdentityRole` объекты, так как код использует `<IdentityRole>` универсального типа.

Вызывается `RoleExists` метод, чтобы определить, присутствует ли роль «canEdit» в базе данных членства. Если это не так, можно создать роль.

Создание `UserManager` кажется сложнее, чем объект `RoleManager` управления, однако он является практически идентичны. Она используется только в коде на одну строку, а не несколько. Здесь параметр, который вы передаете создание экземпляров как новый объект, содержащийся в скобки.

Далее создайте пользователя «canEditUser» путем создания нового `ApplicationUser` объекта. Затем при успешном создании пользователя, добавьте пользователя в новую роль.

> [!NOTE] 
> 
> Обработка ошибок будет обновлен при учебнике «Обработка ошибок ASP.NET» этой серии учебника.


При следующем запуске приложения пользователя с именем «canEditUser» будут добавлены как роли с именем «canEdit» приложения. Далее в этом учебнике будет войти как пользователь «canEditUser» для отображения дополнительных возможностей, которые будут добавлены с помощью этого учебника. Дополнительные сведения об удостоверении ASP.NET API см. [имен Microsoft.AspNet.Identity](https://msdn.microsoft.com/library/microsoft.aspnet.identity(v=vs.111).aspx). Дополнительные сведения об инициализации системы ASP.NET Identity см. в разделе [AspnetIdentitySample](https://github.com/rustd/AspnetIdentitySample/blob/master/AspnetIdentitySample/App_Start/IdentityConfig.cs).

### <a name="restricting-access-to-the-administration-page"></a>Ограничение доступа к странице администрирования

Пример приложения Wingtip Toys позволяет анонимным пользователям и вошедшие в систему пользователи просматривать и покупать продукты. Тем не менее вошедшего в систему пользователя с ролью пользовательские «canEdit» можно получить доступ к страницу с ограниченным доступом для добавления и удаления продуктов.

#### <a name="add-an-administration-folder-and-page"></a>Добавить папку администрирования и страницы

Далее будет создана папка с именем *администратора* для пользователя «canEditUser», принадлежащие к пользовательской роли Wingtip Toys образец приложения.

1. Щелкните правой кнопкой мыши имя проекта (**Wingtip Toys**) в **обозревателе решений** и выберите **добавить**  - &gt; **новую папку**.
2. Имя новой папки *администратора*.
3. Щелкните правой кнопкой мыши *администратора* папки, а затем выберите **добавить**  - &gt; **новый элемент**.   
   Откроется диалоговое окно **Добавление нового элемента**.
4. Выберите <strong>Visual C#</strong> - &gt; <strong>Web</strong> группа шаблонов в левой части экрана. В среднем списке выберите <strong>веб-форма с главной страницы</strong>, назовите его <em>AdminPage.aspx</em><strong>,</strong> , а затем выберите <strong>добавить</strong>.
5. Выберите *Site.Master* файл с именем главной страницы, а затем выберите **ОК**.

#### <a name="add-a-webconfig-file"></a>Добавьте файл Web.config

Добавив *Web.config* файл *администратора* папки, можно ограничить доступ к странице, содержащиеся в папке.

1. Щелкните правой кнопкой мыши *администратора* папку и выберите **добавить**  - &gt; **новый элемент**.  
   Откроется диалоговое окно **Добавление нового элемента**.
2. В списке веб-шаблонов Visual C# выберите <strong>файл веб-конфигурации</strong>в списке средней примите имя по умолчанию <em>Web.config</em><strong>,</strong> , а затем выберите <strong>Добавить</strong>.
3. Замена существующего содержимого в XML *Web.config* файл со следующим:  

    [!code-xml[Main](membership-and-administration/samples/sample4.xml)]

Сохранить *Web.config* файла. *Web.config* указывает файл, только пользователь, принадлежащих роли «canEdit» приложения можно получить доступ к странице, содержащихся в *администратора* папки.

### <a name="including-custom-role-navigation"></a>Включая навигацию пользовательской роли

Чтобы пользователь мог перейти к разделу администрирования приложения роли пользовательские «canEdit», необходимо добавить ссылку на *Site.Master* страницы. Пользователи, которые входят в роль «canEdit» будут отображаться только **администратора** ссылку и доступа в разделе "Администрирование".

1. В обозревателе решений, найдите и откройте *Site.Master* страницы.
2. Для создания связи для пользователя в роль «canEdit» добавьте разметку, выделены желтым цветом, позволяя следующие неупорядоченный список `<ul>` элемент для отображения в списке ниже:  

    [!code-html[Main](membership-and-administration/samples/sample5.html?highlight=2-3)]
3. Откройте *Site.Master.cs* файла. Сделать **администратора** связи видимы только пользователю «canEditUser» путем добавления кода, выделяются желтым цветом, позволяя `Page_Load` обработчика. `Page_Load` Обработчика будет выглядеть следующим образом:   

    [!code-csharp[Main](membership-and-administration/samples/sample6.cs?highlight=3-6)]

При загрузке страницы, код проверяет, имеет ли вошедшего в систему пользователя роль «canEdit». Если пользователь принадлежит к роли «canEdit», содержащий ссылку на элемент span *AdminPage.aspx* страницы (и по ссылке в диапазоне) станет видимым.

### <a name="enabling-product-administration"></a>Включение администрирования продукта

На данный момент создания роли «canEdit» и добавить пользователя с «canEditUser», страницу администрирования и администрирования папку. Задать права доступа для администрирования папки и страницы и добавили ссылку навигации для пользователя в роль «canEdit» в приложение. Далее можно добавить разметку для *AdminPage.aspx* страницы и в код *AdminPage.aspx.cs* файл кода, включение этого пользователя с ролью «canEdit» для добавления и удаления продуктов.

1. В **обозревателе решений**откройте *AdminPage.aspx* файл из *администратора* папки.
2. Замените существующую разметку следующей:  

    [!code-aspx[Main](membership-and-administration/samples/sample7.aspx)]
3. Затем откройте *AdminPage.aspx.cs* файл кода, щелкнув правой кнопкой мыши *AdminPage.aspx* и щелкнув **Просмотр кода**.
4. Замените существующий код в *AdminPage.aspx.cs* файл кода с помощью следующего кода:  

    [!code-csharp[Main](membership-and-administration/samples/sample8.cs)]

В коде, введенным для *AdminPage.aspx.cs* файл кода программной части класс с именем `AddProducts` осуществляет фактическую деятельность добавления продуктов в базе данных. Этот класс еще не существует, поэтому следует создать ее сейчас.

1. В **обозревателе решений**, щелкните правой кнопкой мыши *логику* папки, а затем выберите **добавить**  - &gt; **новый элемент**.   
   Откроется диалоговое окно **Добавление нового элемента**.
2. Выберите **Visual C#**  - &gt; **кода** группа шаблонов в левой части экрана. Выберите **класса**из середины списка и назовите его *AddProducts.cs*.   
   Отображается новый файл класса.
3. Замените существующий код следующим кодом:  

    [!code-csharp[Main](membership-and-administration/samples/sample9.cs)]

*AdminPage.aspx* страница позволяет пользователь, принадлежащий роли «canEdit» для добавления и удаления продуктов. При добавлении нового продукта, сведения о продукте, проверяются и затем вводить в базе данных. Новый продукт сразу становятся доступными для всех пользователей веб-приложения.

#### <a name="unobtrusive-validation"></a>Ненавязчивой проверки

Сведения о продукте, предоставленные пользователем на *AdminPage.aspx* страницы проверяются с помощью элементов управления проверки (`RequiredFieldValidator` и `RegularExpressionValidator`). Эти элементы управления автоматически использовать ненавязчивой проверки. Ненавязчивой проверки позволяет проверяющие элементы управления для использования JavaScript для логики проверки на стороне клиента, это означает, что страницы не требует обращения к серверу для проверки. По умолчанию проверки ненавязчивого включается в *Web.config* файла на основании следующий параметр конфигурации:

[!code-xml[Main](membership-and-administration/samples/sample10.xml)]

#### <a name="regular-expressions"></a>Регулярные выражения

Цена продукта на *AdminPage.aspx* страницы проверяется с использованием **RegularExpressionValidator** элемента управления. Этот элемент управления проверяет, является ли значение связанного элемента управления ввода (текстовое поле «AddProductPrice») соответствует шаблону, заданному регулярным выражением. Регулярное выражение — нотации поиска совпадения с шаблоном, позволяет быстро искать и сопоставлять определенных шаблонов символов. **RegularExpressionValidator** управления содержит свойство с именем `ValidationExpression` , содержащий регулярное выражение, используемое для проверки ввода цена, как показано ниже:

[!code-aspx[Main](membership-and-administration/samples/sample11.aspx)]

#### <a name="fileupload-control"></a>Элемент управления fileUpload

Помимо ввод и проверку элементов управления, вы добавили **FileUpload** управления *AdminPage.aspx* страницы. Этот элемент управления предоставляет возможность загрузки файлов. В этом случае файлы изображений, будут переданы только разрешена. В файле кода (*AdminPage.aspx.cs*), когда `AddProductButton` выбран, этот код проверяет `HasFile` свойство **FileUpload** элемента управления. Если элемент управления содержит файл, если разрешен тип файла (в зависимости от расширения файла), изображение сохраняется в *изображения* папки и *изображения или палец* папке приложения.

#### <a name="model-binding"></a>Привязка модели

Ранее в этой серии учебника привязки модели используется для заполнения **ListView** управления **FormsView** управления, **GridView** управления и  **DetailView** элемента управления. В этом учебнике используется привязка модели для заполнения **DropDownList** управления со списком категорий продуктов.

Разметка, добавленную в *AdminPage.aspx* файл содержит **DropDownList** управления с именем `DropDownAddCategory`:

[!code-aspx[Main](membership-and-administration/samples/sample12.aspx)]

Использование привязки модели для заполнения это **DropDownList** , установив `ItemType` атрибута и `SelectMethod` атрибута. `ItemType` Атрибут указывает, что используется `WingtipToys.Models.Category` введите при заполнении элемента управления. Этот тип в начале этого учебника ряда определить путем создания `Category` класса (см. ниже). `Category` Класс находится в *моделей* папки *Category.cs* файла.

[!code-csharp[Main](membership-and-administration/samples/sample13.cs)]

`SelectMethod` Атрибут **DropDownList** управления указывает, что используется `GetCategories` метод (см. ниже), включены в файл кода (*AdminPage.aspx.cs*).

[!code-csharp[Main](membership-and-administration/samples/sample14.cs)]

Этот метод указывает, что `IQueryable` интерфейса используется для оценки запрос к `Category` типа. Возвращаемое значение используется для заполнения **DropDownList** в разметке страницы (*AdminPage.aspx*).

Текст, отображаемый для каждого элемента в списке задается параметром `DataTextField` атрибута. `DataTextField` Атрибут использует `CategoryName` из `Category` класс (как показано выше) для отображения каждой категории в **DropDownList** элемента управления. Действительное значение, которое передается при выборе элемента в **DropDownList** управления на основе `DataValueField` атрибута. `DataValueField` Атрибута задано значение `CategoryID` определять в `Category` класс (как показано выше).

### <a name="how-the-application-will-work"></a>Как приложение будет работать

Когда пользователь, принадлежащий к роли «canEdit» переход к странице в первый раз `DropDownAddCategory` **DropDownList** элемент заполняется, как описано выше. `DropDownRemoveProduct` **DropDownList** с продуктами, используя тот же подход также заполнения элемента управления. Пользователь, принадлежащий к роли «canEdit» выбирает тип категории и добавляет сведения о продукте (**имя**, **описание**, **цены**, и **файла изображения**). При щелчке пользователем, принадлежащих роли «canEdit» **добавить продукт** кнопки `AddProductButton_Click` вызывается обработчик события. `AddProductButton_Click` Обработчик событий находится в файле кода (*AdminPage.aspx.cs*) проверяет файл образа, убедитесь, что он соответствует допустимых типов файлов *(.gif*, *.png*, *.jpeg*, или *.jpg*). Затем файл изображение сохраняется в папку образца приложения Wingtip Toys. Далее нового продукта добавляется в базу данных. Для выполнения при добавлении нового продукта, новый экземпляр `AddProducts` класса с именами продуктов. `AddProducts` Класс содержит метод с именем `AddProduct`, и объект products вызывает этот метод для добавления продуктов в базу данных.

[!code-csharp[Main](membership-and-administration/samples/sample15.cs)]

Если код успешно добавляет новый продукт в базу данных, страницы будут обновляться значение строки запроса `ProductAction=add`.

[!code-csharp[Main](membership-and-administration/samples/sample16.cs)]

При перезагрузке страницы строка запроса включается в URL-адрес. Путем перезагрузки страницы, пользователь, принадлежащий к роли «canEdit» можно сразу же видеть обновления в **DropDownList** элементов управления в *AdminPage.aspx* страницы. Кроме того, включая строку запроса URL-адресом, странице можно отобразить сообщение об успешном выполнении пользователю, принадлежащему к роли «canEdit».

Когда *AdminPage.aspx* страницы перезагрузок, `Page_Load` вызывается событие.

[!code-csharp[Main](membership-and-administration/samples/sample17.cs)]

`Page_Load` Обработчик событий проверяет значение строки запроса и определяет, следует ли отображать сообщение об успешном выполнении.

## <a name="running-the-application"></a>Запуск приложения

Можно запустить приложение теперь, чтобы увидеть, как можно добавить, удаления и обновления элементов в список покупок. Покупательской корзины всего будет отражать стоимость всех элементов в список покупок.

1. В обозревателе решений, нажмите клавишу **F5** для запуска образца приложения Wingtip Toys.  
   Браузер откроется со *Default.aspx* страницы.
2. Нажмите кнопку **входа** ссылку в верхней части страницы. 

    ![Членство и администрирование - войдите в ссылке](membership-and-administration/_static/image2.png)

   *Login.aspx* -страница.
3. Используйте следующие имя пользователя и пароль:  
   Имя пользователя: canEditUser@wingtiptoys.com  
   Пароль: Pa$ $слово1 

    ![Членство и администрирование - страница входа в](membership-and-administration/_static/image3.png)
4. Нажмите кнопку **входа** кнопки в нижней части страницы.
5. В верхней части следующей страницы, выберите **администратора** ссылку для перехода к *AdminPage.aspx* страницы. 

    ![Членство и администрирование - Admin ссылку](membership-and-administration/_static/image4.png)
6. Чтобы проверить проверку входных данных, щелкните **добавить продукт** кнопки без добавления каких-либо деталей продукта. 

    ![Членство и администрирование - странице администрирования](membership-and-administration/_static/image5.png)

   Обратите внимание, что отображаются сообщения обязательное поле.
7. Добавьте сведения для нового продукта и нажмите кнопку **добавить продукт** кнопки. 

    ![Членство и администрирование - добавить продукт](membership-and-administration/_static/image6.png)
8. Выберите **продуктов** меню верхней панели навигации, чтобы просмотреть новый продукт был добавлен. 

    ![Членство и администрирование - Показать новый продукт](membership-and-administration/_static/image7.png)
9. Нажмите кнопку **администратора** ссылку, чтобы вернуться на страницу администрирования.
10. В **удалить продукт** раздел страницы, выберите новый продукт, вы добавили в **DropDownListBox**.
11. Нажмите кнопку **удалить продукт** кнопку, чтобы удалить новый продукт из приложения. 

    ![Членство и администрирование - удаление продукта](membership-and-administration/_static/image8.png)
12. Выберите **продуктов** меню верхней панели навигации, чтобы убедиться, что продукт был удален.
13. Нажмите кнопку **выйти** должно храниться в режиме администрирования.   
    Обратите внимание, что больше не отображается на верхней панели навигации панели **администратора** элемента меню.

## <a name="summary"></a>Сводка

В этом учебнике добавить пользовательскую роль и пользователь, принадлежащий к пользовательской роли, ограниченный доступ к папке администрирования и страницы и предоставленных навигации для принадлежащих пользовательской роли пользователя. Привязка модели используются для заполнения **DropDownList** элемента управления данными. При реализации **FileUpload** управления и элементов управления проверки. Кроме того вы узнали, как для добавления и удаления продуктов из базы данных. В следующем уроке вы узнаете, как реализовать маршрутизации ASP.NET.

## <a name="additional-resources"></a>Дополнительные ресурсы

[Web.config - авторизации элемент](https://msdn.microsoft.com/library/8d82143t(v=vs.100).aspx)  
[ASP.NET Identity](../../../../identity/overview/getting-started/introduction-to-aspnet-identity.md)  
[Развертывание приложения безопасного ASP.NET Web Forms членства, OAuth и базы данных SQL Azure веб-сайт](https://azure.microsoft.com/documentation/articles/web-sites-dotnet-deploy-aspnet-webforms-app-membership-oauth-sql-database/)  
[Microsoft Azure — бесплатная пробная версия](https://azure.microsoft.com/pricing/free-trial/)

> [!div class="step-by-step"]
> [Назад](checkout-and-payment-with-paypal.md)
> [Вперед](url-routing.md)
