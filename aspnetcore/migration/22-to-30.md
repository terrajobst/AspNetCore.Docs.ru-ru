---
title: Миграция из ASP.NET Ядра 2.2 до 3.0
author: rick-anderson
description: Узнайте, как перенести проект ASP.NET Core 2.2 в ASP.NET Core 3.0.
ms.author: riande
ms.custom: mvc
ms.date: 01/21/2020
no-loc:
- SignalR
uid: migration/22-to-30
ms.openlocfilehash: 46223011e82c7115147f59ee03e20d8033103b98
ms.sourcegitcommit: f0aeeab6ab6e09db713bb9b7862c45f4d447771b
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/08/2020
ms.locfileid: "80977240"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a>Миграция из ASP.NET Ядра 2.2 до 3.0

[Скотт Эдди](https://github.com/scottaddie) и [Рик Андерсон](https://twitter.com/RickAndMSFT)

В этой статье объясняется, как обновить существующий проект ASP.NET Core 2.2 до ASP.NET Core 3.0.

## <a name="prerequisites"></a>Предварительные требования

# <a name="visual-studio"></a>[Visual Studio](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-code"></a>[Visual Studio Code](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mac"></a>[Visual Studio для Mac](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-net-core-sdk-version-in-globaljson"></a>Обновление версии пакета SDK для .NET Core в файле global.json

Если ваше решение опирается на файл [global.json](/dotnet/core/tools/global-json) для таргетинга на `version` конкретную версию .NET Core SDK, обновите его свойство до версии 3.0, установленной на вашей машине:

```json
{
  "sdk": {
    "version": "3.0.100"
  }
}
```

## <a name="update-the-project-file"></a>Обновление файла проекта

### <a name="update-the-target-framework"></a>Обновление целевой рамки

ASP.NET Core 3.0, а затем работает только на .NET Core. Установите [целевой рамочный Moniker (TFM)](/dotnet/standard/frameworks) для: `netcoreapp3.0`

[!code-xml[](22-to-30/samples/Web1.csproj?highlight=4)]

### <a name="remove-obsolete-package-references"></a>Удаление устаревших ссылок пакетов

Большое количество пакетов NuGet не производятся для ASP.NET Core 3.0. Такие ссылки на пакет должны быть удалены из файла проекта. Рассмотрим следующий файл проекта для веб-приложения core 2.2 ASP.NET:

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>netcoreapp2.2</TargetFramework>
    <AspNetCoreHostingModel>InProcess</AspNetCoreHostingModel>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.App"/>
    <PackageReference Include="Microsoft.AspNetCore.Razor.Design" Version="2.2.0" PrivateAssets="All" />
  </ItemGroup>

</Project>
```

Обновленный файл проекта для ASP.NET Core 3.0:

[!code-xml[](22-to-30/samples/Web1.csproj?highlight=4)]

Обновленный ASP.NET файл проекта Core 3.0:

* В `<PropertyGroup>`:
  * Обновления TFM для`netcoreapp3.0`
  * Удаляет `<AspNetCoreHostingModel>` элемент. Для получения дополнительной информации [в](#in-process-hosting-model) этом документе см.

* В `<ItemGroup>`:
  * `Microsoft.AspNetCore.App`удаляется. Для получения дополнительной информации в этом документе можно [ознакомиться на справке Рамочной.](#framework-reference)
  * `Microsoft.AspNetCore.Razor.Design`удаляется и в следующем списке пакетов больше не производится.

Чтобы увидеть полный список пакетов, которые больше не производятся, выберите следующий список расширения:

<details>
    <summary>Нажмите, чтобы расширить список пакетов, которые больше не производятся</summary>
    <ul>
        <li>Microsoft.AspNetCore</li>
        <li>Microsoft.AspNetCore.All</li>
        <li>Microsoft.AspNetCore.App</li>
        <li>Microsoft.AspNetCore.Antiforgery</li>
        <li>Microsoft.AspNetCore.Authentication</li>
        <li>Microsoft.AspNetCore.Authentication.Abstractions</li>
        <li>Microsoft.AspNetCore.Authentication.Cookies</li>
        <li>Microsoft.AspNetCore.Authentication.Core</li>
        <li>Microsoft.AspNetCore.Authentication.OAuth</li>
        <li>Microsoft.AspNetCore.Authorization.Policy</li>
        <li>Microsoft.AspNetCore.CookiePolicy</li>
        <li>Microsoft.AspNetCore.Cors</li>
        <li>Microsoft.AspNetCore.Diagnostics</li>
        <li>Microsoft.AspNetCore.Diagnostics.HealthChecks</li>
        <li>Microsoft.AspNetCore.HostFiltering</li>
        <li>Microsoft.AspNetCore.Hosting</li>
        <li>Microsoft.AspNetCore.Hosting.Abstractions</li>
        <li>Microsoft.AspNetCore.Hosting.Server.Abstractions</li>
        <li>Microsoft.AspNetCore.Http</li>
        <li>Microsoft.AspNetCore.Http.Abstractions</li>
        <li>Microsoft.AspNetCore.Http.Connections</li>
        <li>Microsoft.AspNetCore.Http.Extensions</li>
        <li>Microsoft.AspNetCore.HttpOverrides</li>
        <li>Microsoft.AspNetCore.HttpsPolicy</li>
        <li>Microsoft.AspNetCore.Identity</li>
        <li>Microsoft.AspNetCore.Localization</li>
        <li>Microsoft.AspNetCore.Localization.Routing</li>
        <li>Microsoft.AspNetCore.Mvc</li>
        <li>Microsoft.AspNetCore.Mvc.Abstractions</li>
        <li>Microsoft.AspNetCore.Mvc.Analyzers</li>
        <li>Microsoft.AspNetCore.Mvc.ApiExplorer</li>
        <li>Microsoft.AspNetCore.Mvc.Api.Analyzers</li>
        <li>Microsoft.AspNetCore.Mvc.Core</li>
        <li>Microsoft.AspNetCore.Mvc.Cors</li>
        <li>Microsoft.AspNetCore.Mvc.DataAnnotations</li>
        <li>Microsoft.AspNetCore.Mvc.Formatters.Json</li>
        <li>Microsoft.AspNetCore.Mvc.Formatters.Xml</li>
        <li>Microsoft.AspNetCore.Mvc.Localization</li>
        <li>Microsoft.AspNetCore.Mvc.Razor</li>
        <li>Microsoft.AspNetCore.Mvc.Razor.ViewCompilation</li>
        <li>Microsoft.AspNetCore.Mvc.RazorPages</li>
        <li>Microsoft.AspNetCore.Mvc.TagHelpers</li>
        <li>Microsoft.AspNetCore.Mvc.ViewFeatures</li>
        <li>Microsoft.AspNetCore.Razor</li>
        <li>Microsoft.AspNetCore.Razor.Runtime</li>
        <li>Microsoft.AspNetCore.Razor.Design</li>
        <li>Microsoft.AspNetCore.ResponseCaching</li>
        <li>Microsoft.AspNetCore.ResponseCaching.Abstractions</li>
        <li>Microsoft.AspNetCore.ResponseCompression</li>
        <li>Microsoft.AspNetCore.Rewrite</li>
        <li>Microsoft.AspNetCore.Routing</li>
        <li>Microsoft.AspNetCore.Routing.Abstractions</li>
        <li>Microsoft.AspNetCore.Server.HttpSys</li>
        <li>Microsoft.AspNetCore.Server.IIS</li>
        <li>Microsoft.AspNetCore.Server.IISIntegration</li>
        <li>Microsoft.AspNetCore.Server.Kestrel</li>
        <li>Microsoft.AspNetCore.Server.Kestrel.Core</li>
        <li>Microsoft.AspNetCore.Server.Kestrel.Https</li>
        <li>Microsoft.AspNetCore.Server.Kestrel.Transport.Abstractions</li>
        <li>Microsoft.AspNetCore.Server.Kestrel.Transport.Sockets</li>
        <li>Microsoft.AspNetCore.Session</li>
        <li>Microsoft.AspNetCore.SignalR</li>
        <li>Microsoft.AspNetCore. SignalR. Основные</li>
        <li>Microsoft.AspNetCore.StaticFiles</li>
        <li>Microsoft.AspNetCore.WebSockets</li>
        <li>Microsoft.AspNetCore.WebUtilities</li>
        <li>Microsoft.Net.Http.Headers </li>
    </ul>
</details>

### <a name="review-breaking-changes"></a>Обзор изменения нарушения

[Обзор изменения нарушения](#break)

### <a name="framework-reference"></a>Рамочная ссылка

Функции ASP.NET Core, которые были доступны через один из пакетов, перечисленных выше, доступны как часть `Microsoft.AspNetCore.App` общей платформы. *Общая платформа* — это набор сборок (*DLL*-файлы), которые установлены на компьютере, содержащий компонент среды выполнения и целевой пакет. Дополнительную информацию см. в этой публикации об [общей платформе](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).

* Проекты, предназначенные для пакета SDK `Microsoft.NET.Sdk.Web`, неявно ссылаются на платформу `Microsoft.AspNetCore.App`.

  Для этих проектов не требуются дополнительные ссылки:

  ```xml
  <Project Sdk="Microsoft.NET.Sdk.Web">
    <PropertyGroup>
      <TargetFramework>netcoreapp3.0</TargetFramework>
    </PropertyGroup>
      ...
  </Project>
  ```

* Проекты, `Microsoft.NET.Sdk` `Microsoft.NET.Sdk.Razor` которые нацелены или SDK, должны добавить явный: `FrameworkReference` `Microsoft.AspNetCore.App`

  ```xml
  <Project Sdk="Microsoft.NET.Sdk.Razor">
    <PropertyGroup>
      <TargetFramework>netcoreapp3.0</TargetFramework>
    </PropertyGroup>

    <ItemGroup>
      <FrameworkReference Include="Microsoft.AspNetCore.App" />
    </ItemGroup>
      ...
  </Project>
  ```

#### <a name="framework-dependent-builds-using-docker"></a>Зависящие от рамок сборки с использованием Docker

Зависящие от рамок сборки консольных приложений, которые используют пакет, зависящий от [ASP.NET общей инфраструктуры](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/) Core может привести к следующей ошибке выполнения:

```console
It was not possible to find any compatible framework version
The specified framework 'Microsoft.AspNetCore.App', version '3.0.0' was not found.
  - No frameworks were found.
```

`Microsoft.AspNetCore.App`является общей структурой, содержащей ASP.NET основное время выполнения и присутствует только на [dotnet/core/aspnet](https://hub.docker.com/_/microsoft-dotnet-core-aspnet/) Docker изображение. 3.0 SDK уменьшает размер зависящих от фректов сборок, использующих ASP.NET Core, не включая дубликаты копий библиотек, доступных в общей структуре. Это потенциальная экономия до 18 МБ, но она требует, чтобы ASP.NET Core время выполнения присутствовать / установлен для запуска приложения.

Чтобы определить, есть ли у приложения зависимость (прямая или косвенная) от общей инфраструктуры ASP.NET Core, изучите файл *runtimeconfig.json,* созданный во время сборки/публикации вашего приложения. Следующий файл JSON показывает зависимость от общей инфраструктуры ASP.NET Core:

```json
{
  "runtimeOptions": {
    "tfm": "netcoreapp3.0",
    "framework": {
      "name": "Microsoft.AspNetCore.App",
      "version": "3.0.0"
    },
    "configProperties": {
      "System.GC.Server": true
    }
  }
}
```

Если приложение использует Docker, используйте базовое изображение, которое включает ASP.NET Core 3.0. Например, `docker pull mcr.microsoft.com/dotnet/core/aspnet:3.0`.

### <a name="add-package-references-for-removed-assemblies"></a>Добавление ссылок на пакеты для удаленных сборок

ASP.NET Core 3.0 удаляет некоторые сборки, которые `Microsoft.AspNetCore.App` ранее были частью ссылки пакета. Чтобы визуализировать, какие сборки были удалены, сравните две общие папки фреймворка. Например, сравнение версий 2.2.7 и 3.0.0:

![сравнение общих каркасных сборок](22-to-30/_static/assembly-diff.png)

Чтобы продолжить использование функций, предоставляемых удаленными сборками, ссылка на версии 3.0 соответствующих пакетов:

* Генерируемое шаблоном веб-приложение с **индивидуальными учетными записями пользователей** требует добавления следующих пакетов:

  [!code-xml[](22-to-30/samples/WebFull.csproj?highlight=9-13)]

* [Microsoft.EntityFrameworkCore](https://www.nuget.org/packages/Microsoft.EntityFrameworkCore)

  Для получения дополнительной информации о ссылках на [Database Providers](/ef/core/providers/index)пакет, в рамках которых имеется определенный пакет поставщиков баз данных, см.

* Удостоверение личности

  Поддержка [uI identity](xref:security/authentication/identity) можно добавить, ссылаясь на пакет [Microsoft.AspNetCore.Identity.UI.](https://www.nuget.org/packages/Microsoft.AspNetCore.Identity.UI)

* Услуги SPA

  * [Microsoft.AspNetCore.SpaServices](https://www.nuget.org/packages/Microsoft.AspNetCore.SpaServices);
  * [Microsoft.AspNetCore.SpaServices.Расширения](https://www.nuget.org/packages/Microsoft.AspNetCore.SpaServices.Extensions)

* Поддержка &ndash; аутентификации сторонних потоков аутентификации доступна в виде пакетов NuGet:

  * Facebook OAuth ([Microsoft.AspNetCore.Authentication.Facebook](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.Facebook))
  * Google OAuth ([Microsoft.AspNetCore.Authentication.Google](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.Google))
  * Проверка подлинности учетной записи Майкрософт ([Microsoft.AspNetCore.Authentication.MicrosoftAccount](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.MicrosoftAccount))
  * OpenID Подключение аутентификации ([Microsoft.AspNetCore.Authentication.OpenIdConnect](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.OpenIdConnect))
  * Токен openID Connect предъявителя ([Microsoft.AspNetCore.Authentication.JwtBearer](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.JwtBearer))
  * Twitter OAuth ([Microsoft.AspNetCore.Authentication.Twitter](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.Twitter))
  * WsFederation аутентификации ([Microsoft.AspNetCore.Authentication.WsFederation](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.WsFederation))

* Форматирование и поддержка `System.Net.HttpClient` &ndash; переговоров по контенту для пакета [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet обеспечивает полезную расширение `System.Net.HttpClient` с aPI, такими как `ReadAsAsync` и `PostJsonAsync`.

* Поддержка компиляции &ndash; часов выполнения для компиляции просмотров и страниц Бритвы теперь является частью [Microsoft.AspNetCore.Mvc.Razor.RuntimeCompilation](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.Razor.RuntimeCompilation).

* Поддержка `Newtonsoft.Json` поддержки MVC (Json.NET) &ndash; `Newtonsoft.Json` для использования MVC с теперь является частью [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).

## <a name="startup-changes"></a>Изменения в запуске

Следующее изображение показывает удаленные и измененные строки в веб-приложении ASP.NET Core 2.2 Razor Pages:

![удаленные и измененные строки в ASP.NET Core 2.2 Razor Web-приложении](22-to-30/_static/startup2.2.png)

На предыдущем изображении удаленный код отображается красным цветом. Удаленный код не показывает код вариантов файлов cookie, который был удален до сравнения файлов.

Следующее изображение показывает добавленные и измененные строки в веб-приложении ASP.NET Core 3.0 Razor Pages:

![добавлены и изменены линии в ASP.NET Core 3.0 Razor Web приложение](22-to-30/_static/startup3.0.png)

На предыдущем изображении добавленный код отображается зеленым цветом. Для получения информации о следующих изменениях:

* `services.AddMvc`, `services.AddRazorPages`см. [регистрацию службы MVC](#mvc-service-registration) в этом документе.
* `CompatibilityVersion`, <xref:mvc/compatibility-version>см.
* `IHostingEnvironment`чтобы, `IWebHostEnvironment` [см. это объявление GitHub](https://github.com/dotnet/AspNetCore/issues/7749).
* `app.UseAuthorization`был добавлен в шаблоны, чтобы показать, что промежуточное программное обеспечение авторизации заказа должно быть добавлено. Если приложение не использует авторизацию, вы можете `app.UseAuthorization`безопасно удалить вызов.
* `app.UseEndpoints`, [см. Страницы razor](#razor-pages) или [Мигрировать Startup.Configure](#migrate-startupconfigure) в этом документе.

### <a name="analyzer-support"></a>Поддержка анализатора

Проекты, `Microsoft.NET.Sdk.Web` ориентированные на неявные справочные анализаторы, ранее отправленные в рамках пакета [Microsoft.AspNetCore.Mvc.Analyzers.](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.Analyzers/) Для их включения не требуется никаких дополнительных ссылок.

Если ваше приложение использует [анализаторы API,](xref:web-api/advanced/analyzers) ранее отправленные с помощью пакета [Microsoft.AspNetCore.Mvc.Api.Analyzers,](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.Api.Analyzers/) отспособьте файл проекта, чтобы ссылаться на анализаторы, отправленные в рамках SDK .NET Core Core Web:

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">
    <PropertyGroup>
        <TargetFramework>netcoreapp3.0</TargetFramework>
        <IncludeOpenAPIAnalyzers>true</IncludeOpenAPIAnalyzers>
    </PropertyGroup>

    ...
</Project>
```

### <a name="razor-class-library"></a>Библиотека классов Razor

Проекты библиотеки класса Razor, предоставляющие `AddRazorSupportForMvc` компоненты uI для MVC, должны установить свойство в файле проекта:

```xml
<PropertyGroup>
  <AddRazorSupportForMvc>true</AddRazorSupportForMvc>
</PropertyGroup>
```

### <a name="in-process-hosting-model"></a>Модель внутрипроцессного размещения

Проекты по умолчанию в [процессе хостинга модели](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) в ASP.NET Core 3.0 или позже. Вы можете по `<AspNetCoreHostingModel>` желанию удалить свойство в `InProcess`файле проекта, если его значение.

## <a name="kestrel"></a>Kestrel

### <a name="configuration"></a>Конфигурация

Мигрировать Kestrel конфигурации веб-хостинга, предоставляемых `ConfigureWebHostDefaults` *(Program.cs):*

```csharp
public static IHostBuilder CreateHostBuilder(string[] args) =>
    Host.CreateDefaultBuilder(args)
        .ConfigureWebHostDefaults(webBuilder =>
        {
            webBuilder.ConfigureKestrel(serverOptions =>
            {
                // Set properties and call methods on options
            })
            .UseStartup<Startup>();
        });
```

Если приложение создает хост `HostBuilder`вручную с, `UseKestrel` вызов `ConfigureWebHostDefaults`на веб-хостинга строитель в:

```csharp
public static void Main(string[] args)
{
    var host = new HostBuilder()
        .UseContentRoot(Directory.GetCurrentDirectory())
        .ConfigureWebHostDefaults(webBuilder =>
        {
            webBuilder.UseKestrel(serverOptions =>
            {
                // Set properties and call methods on options
            })
            .UseIISIntegration()
            .UseStartup<Startup>();
        })
        .Build();

    host.Run();
}
```

### <a name="connection-middleware-replaces-connection-adapters"></a>Подключение Middleware заменяет адаптеры подключения

Адаптеры`Microsoft.AspNetCore.Server.Kestrel.Core.Adapter.Internal.IConnectionAdapter`подключения () были удалены из Kestrel. Замените адаптеры подключения с помощью connection Middleware. Подключение Middleware аналогично HTTP Middleware в ASP.NET core конвейера, но для соединений более низкого уровня. HTTPS и журнал соединения:

* Были перемещены из адаптеров подключения в Connection Middleware.
* Эти методы расширения работают, как и в предыдущих версиях ASP.NET Core. 

Для получения дополнительной информации смотрите [пример TlsFilterConnectionHandler в разделе ListenOptions.Protocols статьи Kestrel](/aspnet/core/fundamentals/servers/kestrel?view=aspnetcore-3.0#listenoptionsprotocols).

### <a name="transport-abstractions-moved-and-made-public"></a>Транспортные абстракции перемещались и обнародовались

Транспортный уровень Kestrel предоставляется как открытый интерфейс в `Connections.Abstractions`. В рамках этих обновлений:

* `Microsoft.AspNetCore.Server.Kestrel.Transport.Abstractions`и связанные типы были удалены.
* <xref:Microsoft.AspNetCore.Server.Kestrel.KestrelServerOptions.NoDelay>был перемещен <xref:Microsoft.AspNetCore.Server.Kestrel.Core.ListenOptions> из вариантов транспортировки.
* `Microsoft.AspNetCore.Server.Kestrel.Transport.Abstractions.Internal.SchedulingMode`был удален <xref:Microsoft.AspNetCore.Server.Kestrel.KestrelServerOptions>из .

Для получения дополнительной информации смотрите следующие ресурсы GitHub:

* [Клиент/серверсетевые абстракции (dotnet/AspNetCore #10308)](https://github.com/dotnet/AspNetCore/issues/10308)
* [Реализация новой абстракции слушателя породы и повторноплайте Kestrel сверху (dotnet/AspNetCore #10321)](https://github.com/dotnet/AspNetCore/pull/10321)

### <a name="kestrel-request-trailer-headers"></a>Kestrel Запрос прицепзаголовков

Для приложений, ориентированных на более ранние версии ASP.NET Core:

* Kestrel добавляет HTTP/1.1 chunked заголовки прицепа в коллекцию заголовков запросов.
* Трейлеры доступны после того, как тело запроса читается до конца.

Это вызывает некоторые опасения по поводу двусмысленности между заголовками и прицепами, поэтому`RequestTrailerExtensions`трейлеры были перемещены в новую коллекцию () в 3.0.

HTTP/2 трейлеры запроса являются:

* Недоступно в ASP.NET Core 2.2.
* Доступно в 3.0 как `RequestTrailerExtensions`.

Для доступа к этим прицепам присутствуют новые методы расширения запросов. Как и в отношении HTTP/1.1, прицепы доступны после того, как тело запроса будет прочитано до конца.

Для выпуска 3.0 доступны `RequestTrailerExtensions` следующие методы:

* `GetDeclaredTrailers`&ndash; Получает заголовок запроса, `Trailer` который перечисляет, какие прицепы ожидать после тела.
* `SupportsTrailers`&ndash; Указывает, поддерживает ли запрос получение заголовков прицепов.
* `CheckTrailersAvailable`&ndash; Проверяет, поддерживает ли запрос трейлеры и доступны ли они для чтения. Эта проверка не предполагает, что Есть прицепы для чтения. Там может быть не прицепов `true` для чтения, даже если возвращается с помощью этого метода.
* `GetTrailer`&ndash; Получает запрошенный заголовок от ответа. Проверьте `SupportsTrailers` `GetTrailer`перед вызовом, <xref:System.NotSupportedException> или может произойти, если запрос не поддерживает заголовки задних.

Для получения дополнительной информации смотрите [трейлеры запроса Put в отдельной коллекции (dotnet/AspNetCore #10410)](https://github.com/dotnet/AspNetCore/pull/10410).

### <a name="allowsynchronousio-disabled"></a>AllowSynchronousIO отключен

`AllowSynchronousIO`позволяет или отстраняет синхронные AIS `HttpRequest.Body.Read`I/O, такие как , `HttpResponse.Body.Write`и `Stream.Flush`. Эти AAP являются источником голодания потоков, ведущих к сбоям приложений. В 3.0 `AllowSynchronousIO` отключен по умолчанию. Для получения дополнительной информации смотрите [раздел Synchronous I/O в статье Kestrel](/aspnet/core/fundamentals/servers/kestrel?view=aspnetcore-3.0#synchronous-io).

Если необходим синхронный ввод/у, его можно `AllowSynchronousIO` настроить путем настройки используемой опции (при вызове, `ConfigureKestrel`например, при использовании Kestrel). Обратите внимание, что серверы (Kestrel, HttpSys, TestServer и т.д.) имеют свой собственный `AllowSynchronousIO` вариант, который не повлияет на другие серверы. Синхронный ввод/o может быть включен для всех `IHttpBodyControlFeature.AllowSynchronousIO` серверов на основе запроса, используя опцию:

```csharp
var syncIOFeature = HttpContext.Features.Get<IHttpBodyControlFeature>();

if (syncIOFeature != null)
{
    syncIOFeature.AllowSynchronousIO = true;
}
```

Если у вас <xref:System.IO.TextWriter> возникли проблемы с реализациями или другими потоками, которые называют сявронными API в [Dispose,](/dotnet/standard/garbage-collection/implementing-dispose)вместо этого позвоните в новый <xref:System.IO.Stream.DisposeAsync*> API.

Для получения дополнительной информации [#7644](https://github.com/dotnet/AspNetCore/issues/7644)см.

### <a name="microsoftaspnetcoreserverkestrelhttps-assembly-removed"></a>Сборка Microsoft.AspNetcore.Server.Kestrel.Https удалена

В ASP.NET Core 2.1 содержимое *Microsoft.AspNetCore.Server.Kestrel.Https.dll* было перенесено на *Microsoft.AspNetCore.Server.Kestrel.Core.dll*. Это было ненарушая `TypeForwardedTo` обновление с использованием атрибутов. Для 3.0, пустые *Microsoft.AspNetCore.Server.Kestrel.Https.dll* сборки и NuGet пакет были удалены.

Библиотеки, ссылающиеся на [Microsoft.AspNetCore.Server.Kestrel.Https,](https://www.nuget.org/packages/Microsoft.AspNetCore.Server.Kestrel.Https) должны обновлять ASP.NET основных зависимостей до 2.1 или позже.

Приложения и библиотеки, ориентированные на ASP.NET Core 2.1 или позже, должны удалять любые прямые ссылки на пакет [Microsoft.AspNetCore.Server.Kestrel.Https.](https://www.nuget.org/packages/Microsoft.AspNetCore.Server.Kestrel.Https)

<a id="jsonnet-support"></a>

## <a name="newtonsoftjson-jsonnet-support"></a>Поддержка Newtonsoft.Json (Json.NET)

В рамках работы по [совершенствованию общей структуры ASP.NET Core,](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/) [Newtonsoft.Json (Json.NET)](https://www.newtonsoft.com/json/help/html/Introduction.htm) был удален из общей структуры ASP.NET Core.

По умолчанию JSON serializer <xref:System.Text.Json>для ASP.NET Core в настоящее время , который является новым в .NET Core 3.0. Рассмотрите `System.Text.Json` возможность использования, когда это возможно. Это высокопроизводительный и не требует дополнительной библиотечной зависимости. Однако, `System.Text.Json` поскольку он является новым, в настоящее время могут отсутствовать функции, которые нужны вашему приложению. Для получения дополнительной информации [см. Как мигрировать из Newtonsoft.Json в System.Text.Json](/dotnet/standard/serialization/system-text-json-migrate-from-newtonsoft-how-to).

### <a name="use-newtonsoftjson-in-an-aspnet-core-30-opno-locsignalr-project"></a>Использование Newtonsoft.Json в проекте SignalR ASP.NET Core 3.0

* Установите [SignalRMicrosoft.AspNetCore. . Протоколы.НьютонсофтJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet пакет.

* На клиенте цепочка вызова `AddNewtonsoftJsonProtocol` `HubConnectionBuilder` метода к экземпляру:

  ```csharp
  new HubConnectionBuilder()
      .WithUrl("/chatHub")
      .AddNewtonsoftJsonProtocol(...)
      .Build();
  ```

* На сервере цепочка вызова `AddNewtonsoftJsonProtocol` `AddSignalR` метода `Startup.ConfigureServices`к вызову метода:

  ```csharp
  services.AddSignalR()
      .AddNewtonsoftJsonProtocol(...);
  ```

### <a name="use-newtonsoftjson-in-an-aspnet-core-30-mvc-project"></a>Используйте Newtonsoft.Json в проекте ASP.NET Core 3.0 MVC

* Установите пакет [Microsoft.AspNetCore.Mvc.NewtonsoftJson.](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson)

* Обновление `Startup.ConfigureServices` для `AddNewtonsoftJson`вызова .

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```

  `AddNewtonsoftJson`совместима с новыми методами регистрации услуг MVC:

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  `Newtonsoft.Json`настройки могут быть установлены `AddNewtonsoftJson`в вызове:

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

**Примечание:** Если `AddNewtonsoftJson` метод недоступен, убедитесь, что вы установили пакет [Microsoft.AspNetCore.Mvc.NewtonsoftJson.](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson) Распространенной ошибкой является установка пакета [Newtonsoft.Json](https://www.nuget.org/packages/Newtonsoft.Json/) вместо пакета [Microsoft.AspNetCore.Mvc.NewtonsoftJson.](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson)

## <a name="mvc-service-registration"></a>Регистрация сервиса MVC

ASP.NET Core 3.0 добавляет новые опции `Startup.ConfigureServices`для регистрации сценариев MVC внутри.

Доступны три новых метода расширения верхнего `IServiceCollection` уровня, связанные со сценариями MVC. Шаблоны используют эти новые `AddMvc`методы вместо . Тем `AddMvc` не менее, продолжает вести себя, как это было в предыдущих релизах.

Следующий пример добавляет поддержку для контроллеров и функций, связанных с API, но не просмотров или страниц. Шаблон API использует этот код:

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

Следующий пример добавляет поддержку контроллеров, связанных с API функций и представлений, но не страниц. Шаблон Web Application (MVC) использует этот код:

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

Следующий пример добавляет поддержку для страниц razor и минимальную поддержку контроллера. Шаблон Web Application использует этот код:

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

Новые методы также могут быть объединены. Следующий пример эквивалентен `AddMvc` вызову в ASP.NET Core 2.2:

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
    services.AddRazorPages();
}
```

## <a name="routing-startup-code"></a>Код запуска routing

Если приложение `UseMvc` вызывает `UseSignalR`или, мигрируйте приложение в [конечную точку routing,](xref:fundamentals/routing) если это возможно. Чтобы улучшить совместимость конечных точек с предыдущими версиями MVC, мы вернули некоторые изменения в генерации URL, внесенные в ASP.NET Core 2.2. Если у вас возникли проблемы с использованием конечных точек routing в 2.2, ожидайте улучшения в ASP.NET Core 3.0 с следующими исключениями:

* Если приложение реализует `IRouter` или наследует от, `Route`используйте [DynamicRouteValuesTransformer](https://github.com/dotnet/AspNetCore.Docs/issues/12997) в качестве замены.
* Если приложение напрямую получает доступ `RouteData.Routers` внутри MVC для разбора URL-адресов, вы можете заменить это использованием [LinkParser.ParsePathByEndpoint.](xref:Microsoft.AspNetCore.Routing.LinkParserEndpointNameAddressExtensions.ParsePathByEndpointName*) 
  * Определите маршрут с именем маршрута.
  * Используйте `LinkParser.ParsePathByEndpointName` и пройдите в нужном названии маршрута.

Конечная точка маршрутизации поддерживает тот же синтаксис шаблона маршрута и авторизущие функции шаблона маршрута, как. `IRouter` Конечные опоры. `IRouteConstraint` Конечная точка, `[Route]`опорная, `[HttpGet]`и другие атрибуты mVC-разгрома.

Для большинства приложений требуются только `Startup` изменения.

### <a name="migrate-startupconfigure"></a>Мигрировать Startup.Configure

Общий совет:

* Добавьте `UseRouting`.
* Если приложение `UseStaticFiles`вызывает, `UseStaticFiles` место **до** `UseRouting`.
* Если приложение использует функции аутентификации/авторизации, такие как `AuthorizePage` или, `[Authorize]` `UseAuthentication` разместите вызов и: `UseAuthorization` **после,** `UseRouting` и, `UseCors`но до `UseEndpoints`:

  ```csharp
  public void Configure(IApplicationBuilder app)
  {
    ...

    app.UseStaticFiles();

    app.UseRouting();
    app.UseCors();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints => {
       endpoints.MapControllers();
    });
  ```

* `UseMvc` Заменить `UseSignalR` `UseEndpoints`или с .
* Если приложение использует сценарии [CORS,](xref:security/cors) `[EnableCors]`такие `UseCors` как, поместите вызов перед любым другим `UseCors` посредниками, которые используют CORS (например, место перед `UseAuthentication`, `UseAuthorization`и `UseEndpoints`).
* `IHostingEnvironment` Замените `IWebHostEnvironment` и `using` добавьте <xref:Microsoft.Extensions.Hosting?displayProperty=fullName> заявление для пространства имен.
* `IApplicationLifetime` Заменить <xref:Microsoft.Extensions.Hosting.IHostApplicationLifetime> <xref:Microsoft.Extensions.Hosting?displayProperty=fullName> (пространство имен).
* `EnvironmentName` Заменить <xref:Microsoft.Extensions.Hosting.Environments> <xref:Microsoft.Extensions.Hosting?displayProperty=fullName> (пространство имен).

Следующий код является `Startup.Configure` примером в типичном ASP.NET приложении Core 2.2:

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();

    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

После обновления предыдущего `Startup.Configure` кода:

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();

    app.UseRouting();

    app.UseCors();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

> [!WARNING]
> Для большинства `UseAuthentication`приложений, `UseAuthorization`звонки, и `UseCors` должны `UseRouting` отображаться между вызовами и `UseEndpoints` быть эффективными.

### <a name="health-checks"></a>Проверки работоспособности

Для проверок работоспособности используется маршрутизация конечных точек с универсальным узлом. В `Startup.Configure` вызовите `MapHealthChecks` для построителя конечной точки с URL-адресом конечной точки или относительным путем:

```csharp
app.UseEndpoints(endpoints =>
{
    endpoints.MapHealthChecks("/health");
});
```

Конечные точки проверки работоспособности могут:

* указать один или несколько разрешенных узлов или портов;
* требовать авторизацию;
* требовать CORS.

Для получения дополнительной информации см. <xref:host-and-deploy/health-checks>.

### <a name="security-middleware-guidance"></a>Руководство по промежуточного обеспечения безопасности

Поддержка авторизации и CORS унифицирована вокруг [промежуточного](xref:fundamentals/middleware/index) подхода. Это позволяет использовать те же промежуточное программное обеспечение и функциональность в этих сценариях. Обновленное промежуточное программное обеспечение авторизации предоставляется в этом выпуске, и CORS Middleware усиливается, чтобы он мог понять атрибуты, используемые контроллерами MVC.

#### <a name="cors"></a>CORS

Ранее CORS может быть трудно настроить. Среднее посуды было предусмотрено для использования в некоторых случаях использования, но MVC фильтры были предназначены для использования **без** промежуточного программного обеспечения в других случаях использования. С ASP.NET Core 3.0, мы рекомендуем всем приложениям, которые требуют CORS использовать CORS Middleware в тандеме с конечным пунктом routing. `UseCors`может быть предоставлена политика `[EnableCors]` по `[DisableCors]` умолчанию, а также атрибуты могут быть использованы для переопределения политики по умолчанию, когда это необходимо.

Рассмотрим следующий пример:

* CORS включен для всех конечных точек с указанной `default` политикой.
* Класс `MyController` отражает CORS `[DisableCors]` с атрибутом.

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default");

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a>Авторизация

В более ранних версиях ASP.NET Core `[Authorize]` поддержка авторизации предоставлялась через атрибут. Разрешение промежуточного не было доступно. В ASP.NET Core 3.0 требуется промежуточное программное обеспечение для авторизации. Мы рекомендуем разместить ASP.NET Основные`UseAuthorization`Авторизации `UseAuthentication`Middleware ( ) сразу после . Программное обеспечение авторизации также может быть настроено с помощью политики по умолчанию, которая может быть отменена.

В ASP.NET Core 3.0 `UseAuthorization` или `Startup.Configure`позже, `HomeController` вызывается в , и следующее требует подписании в пользователе:

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : Controller
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

При использовании конечных точек, мы `<xref:Microsoft.AspNetCore.Mvc.Authorization.AuthorizeFilter>` рекомендуем не настраивать и вместо этого полагаться на промежуточное программное обеспечение авторизации.  Если приложение использует `AuthorizeFilter` в качестве глобального фильтра в MVC, мы рекомендуем рефакторинг кода, чтобы обеспечить политику в вызове. `AddAuthorization`

Изначально `DefaultPolicy` настроен аттенциру требуется проверка подлинности, поэтому дополнительная конфигурация не требуется. В следующем примере конечные точки `RequireAuthorization` MVC помечаются как так, что все запросы должны быть авторизованы на основе `DefaultPolicy`. Тем не `HomeController` менее, позволяет получить доступ без `[AllowAnonymous]`пользователя подписания в приложение из-за:

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute().RequireAuthorization();
    });
}

[AllowAnonymous]
public class HomeController : Controller
{
    ...
}
```

### <a name="authorization-for-specific-endpoints"></a>Авторизация для конкретных конечных точек

Авторизация также может быть настроена для определенных классов конечных точек. Следующий код является примером преобразования приложения MVC, `AuthorizeFilter` которое настроило глобальное приложение с определенной политикой, требующей авторизации:

[!code-csharp[](22-to-30/samples/Startup.cs?name=snippet&highlight=8-9,21-22,26-27,53-54)]

Политики также могут быть настроены. Настроен `DefaultPolicy` для проверки подлинности:

[!code-csharp[](22-to-30/samples/Startup2.cs?name=snippet&highlight=21-26,52)]

[!code-csharp[](22-to-30/samples/HomeController.cs?name=snippet)]

Кроме того, все конечные точки могут `[Authorize]` быть `RequireAuthorization` настроены таким `FallbackPolicy`образом, чтобы требовать авторизации без настройки . Отличается `FallbackPolicy` от `DefaultPolicy`. Срабатывает `DefaultPolicy` `[Authorize]` `RequireAuthorization`или, в `FallbackPolicy` то время как срабатывает, когда нет другой политики установлен. `FallbackPolicy`изначально настроен для разрешения запросов без разрешения.

Следующий пример совпадает с `DefaultPolicy` предыдущим примером, но использует `FallbackPolicy` всегда требовать `[AllowAnonymous]` проверки подлинности на всех конечных точках, за исключением случаев, когда указано:

```csharp
public void ConfigureServices(IServiceCollection services)
{
    ...

    services.AddAuthorization(options =>
    {
        options.FallbackPolicy = new AuthorizationPolicyBuilder()
          .RequireAuthenticatedUser()
          .Build();
    });
}

public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : Controller
{
    ...
}
```

Авторизация по промежуточным программам работает без каких-либо конкретных знаний о авторизации. Например, [проверки работоспособности](xref:host-and-deploy/health-checks) не имеют конкретных знаний о авторизации, но проверки работоспособности могут иметь настраиваемую политику авторизации, применяемую посредниками.

Кроме того, каждая конечная точка может настроить свои требования к авторизации. В следующем примере `UseAuthorization` обрабатывает `DefaultPolicy`авторизацию `/healthz` с помощью `admin` точки проверки работоспособности, которая требует от пользователя:

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

Защита осуществляется для некоторых сценариев. Endpoints Middleware выбрасывает исключение, если авторизация или политика CORS пропущены из-за отсутствия промежуточного посуды. Ведется поддержка анализатора для предоставления дополнительной обратной связи о неправильной настройке.

#### <a name="custom-authorization-handlers"></a>Обработчики пользовательских авторизаций

Если приложение использует [пользовательские обработчики авторизации,](xref:security/authorization/policies#authorization-handlers)конечная точка, отправивая в обработчики другой тип ресурсов, чем MVC. Обработчики, которые ожидают, что <xref:Microsoft.AspNetCore.Mvc.Filters.AuthorizationFilterContext> контекстный ресурс обработчика авторизации будет типа (тип ресурса, [предоставляемый фильтрами MVC),](xref:security/authorization/policies#accessing-mvc-request-context-in-handlers)необходимо будет обновить для обработки ресурсов типа <xref:Microsoft.AspNetCore.Routing.RouteEndpoint> (тип ресурса, отданный обработчикам авторизации по конечной точке).

MVC по-прежнему использует `AuthorizationFilterContext` ресурсы, поэтому, если приложение использует фильтры авторизации MVC наряду с авторизацией конечной точки, может потребоваться обработка обоих типов ресурсов.

### SignalR

Картирование SignalR узлов теперь происходит `UseEndpoints`внутри.

Карта каждого `MapHub`концентратора с . Как и в предыдущих версиях, каждый концентратор явно указан.

В следующем примере добавляется поддержка концентратора: `ChatHub` SignalR

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

Существует новая опция для контроля пределов размера сообщения от клиентов. Например, для `Startup.ConfigureServices`:

```csharp
services.AddSignalR(hubOptions =>
{
    hubOptions.MaximumReceiveMessageSize = 32768;
});
```

В ASP.NET Core 2.2 можно `TransportMaxBufferSize` установить и эффективно контролировать максимальный размер сообщения. В ASP.NET Core 3.0, этот вариант теперь контролирует только максимальный размер до обратного давления наблюдается.

### <a name="mvc-controllers"></a>Контроллеры MVC

Картирование контроллеров теперь `UseEndpoints`происходит внутри .

Добавить, `MapControllers` если приложение использует атрибут ывырения. Поскольку маршрутирование включает поддержку многих инфраструктур в ASP.NET Core 3.0 или позже, добавление контроллеров, направивных атрибутами, является отказом.

Вместо

* `MapRoute` с `MapControllerRoute`
* `MapAreaRoute` с `MapAreaControllerRoute`

Поскольку в настоящее время в направлении входная часть включает в себя поддержку не только MVC, терминология изменилась, чтобы эти методы четко указали, что они делают. Обычные маршруты, такие `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` как применяются в порядке, который они добавляются. Сначала поместите более конкретные маршруты (например, маршруты для области).

Рассмотрим следующий пример:

* `MapControllers`добавляет поддержку контроллеров, направив атрибуты.
* `MapAreaControllerRoute`добавляет обычный маршрут для контроллеров в области.
* `MapControllerRoute`добавляет обычный маршрут для контроллеров.

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin",
            "admin",
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

#### <a name="async-suffix-removal-from-controller-action-names"></a>Удаление суффикса Async из имен действий контроллера

В ASP.NET Core 3.0 ASP.NET Core `Async` MVC удаляет суффикс из имен действий контроллера. Это влияет на генерацию как реуктора, так и генерацию ссылок. Пример:

```csharp
public class ProductsController : Controller
{
    public async Task<IActionResult> ListAsync()
    {
        var model = await _dbContext.Products.ToListAsync();
        return View(model);
    }
}
```

До ASP.NET Ядра 3.0:

* Предыдущее действие можно получить на маршруте *Products/ListAsync.*
* Генерация ссылок `Async` требует указания суффикса. Пример:

    ```cshtml
    <a asp-controller="Products" asp-action="ListAsync">List</a>
    ```

В ASP.NET Core 3.0:

* Предыдущее действие можно получить на маршруте *«Продукты/Список».*
* Генерация ссылок не требует `Async` указания суффикса. Пример:

    ```cshtml
    <a asp-controller="Products" asp-action="List">List</a>
    ```

Это изменение не влияет на [`[ActionName]`](/dotnet/api/microsoft.aspnetcore.mvc.actionnameattribute) имена, указанные с помощью атрибута. Поведение по умолчанию может быть `Startup.ConfigureServices`отключено со следующим кодом в:

```csharp
services.AddMvc(options =>
    options.SuppressAsyncSuffixInActionNames = false);
```

#### <a name="changes-to-link-generation"></a>Изменения в генерации ссылок

Как поясняется в документации по [отличию от предыдущих версий реуктора,](xref:fundamentals/routing#differences-from-earlier-versions-of-routing)есть некоторые различия в генерации ссылок (использование `Url.Link` и аналогичные AIS, например). Сюда входит следующее.

* По умолчанию при использовании конечной точки параметры маршрута не обязательно сохраняются. Это поведение можно контролировать `IOutboundParameterTransformer` с помощью интерфейса.
* Создание URI для недействительного маршрута (контроллер/действие или страница, которая не существует) приведет к созданию пустой строки под маршрутизацией конечной точки вместо создания недействительного URI.
* Окружающие значения (параметры маршрута из текущего контекста) не используются автоматически в генерации ссылок с конечным маршрутизированием. Ранее при создании ссылки на другое действие (или страницу) из *текущих* значений окружающего маршрута будут выведены неопределенные значения маршрута. При использовании маршрутики конечных точек все параметры маршрута должны быть четко указаны во время генерации ссылок.

### <a name="razor-pages"></a>Razor Pages

Отображение Razor `UseEndpoints`Страницы теперь происходит внутри .

Добавить, `MapRazorPages` если приложение использует Razor Pages. Поскольку конечная точка Routing включает поддержку многих инфраструктур, добавление страниц Razor теперь является отказом.

В следующем `Startup.Configure` методе `MapRazorPages` добавляется поддержка для страниц razor:

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a>Используйте MVC без конечных точек реуктора

Использование MVC `UseMvc` `UseMvcWithDefaultRoute` через или в ASP.NET Core 3.0 `Startup.ConfigureServices`требует явного выбора внутри. Это необходимо, потому что MVC должен знать, может ли он полагаться на авторизацию и CORS Middleware во время инициализации. Предоставляется анализатор, который предупреждает, если приложение пытается использовать неподдерживаемую конфигурацию.

Если приложение требует `IRouter` устаревшей `EnableEndpointRouting` поддержки, отменяйте использование любого из следующих подходов в: `Startup.ConfigureServices`

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="health-checks"></a>Проверки работоспособности

Проверки работоспособности могут быть использованы в качестве *маршрутизатора-посуды* с конечным маршрутизанием.

Добавьте `MapHealthChecks` в пользование проверки работоспособности с помощью конечных точек. Метод `MapHealthChecks` принимает аргументы, `UseHealthChecks`аналогичные . Преимуществом использования `MapHealthChecks` `UseHealthChecks` более является возможность применения авторизации и более точного контроля над соответствующей политикой.

В следующем примере `MapHealthChecks` требуется конечная точка `/healthz`проверки работоспособности по адресу:

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a>HostBuilder заменяет WebHostBuilder

В шаблонах core 3.0 ASP.NET используется [общий хост.](xref:fundamentals/host/generic-host) Предыдущие версии использовали [web Host](xref:fundamentals/host/web-host). Следующий код показывает ASP.NET сгенерированный `Program` шаблон Core 3.0:

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

Следующий код показывает ASP.NET шаблон-генерируемый `Program` класс Core 2.2:

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder>остается в 3.0 и `webBuilder` является типом увиденного в предыдущем образце кода. <xref:Microsoft.AspNetCore.Hosting.WebHostBuilder>будет уничена в будущем выпуске `HostBuilder`и заменена .

Наиболее значительное изменение от `WebHostBuilder` `HostBuilder` происходит в [инъекциях зависимости (DI).](xref:fundamentals/dependency-injection) При `HostBuilder`использовании, вы можете `Startup`только вводить следующие в 'S конструктор:

* <xref:Microsoft.Extensions.Configuration.IConfiguration>
* <xref:Microsoft.Extensions.Hosting.IHostEnvironment>
* <xref:Microsoft.AspNetCore.Hosting.IWebHostEnvironment>

Ограничения `HostBuilder` DI:

* Включите контейнер DI, который будет построен только один раз.
* Избегает возникающих проблем при жизни объекта, таких как разрешение нескольких экземпляров синглтонов.

Для получения дополнительной [информации см. Избегая впрыска службы запуска в ASP.NET Core 3](https://andrewlock.net/avoiding-startup-service-injection-in-asp-net-core-3/).

## <a name="addauthorization-moved-to-a-different-assembly"></a>Добавление Авторизация перемещено в другую сборку

В ASP.NET Core 2.2 и более низкие `AddAuthorization` методы в *Microsoft.AspNetCore.Authorization.dll*:

* Были переименованы `AddAuthorizationCore`.
* Были перемещены в *Microsoft.AspNetCore.Authorization.Policy.dll*.

Приложения, использующие *как Microsoft.AspNetCore.Authorization.dll,* так и *Microsoft.AspNetCore.Authorization.Policy.dll* не влияют.

Приложения, которые не используют *Microsoft.AspNetCore.Authorization.Policy.dll* должны сделать один из следующих:

* Добавить ссылку на *Microsoft.AspNetCore.Authorization.Policy.dll*. Этот подход работает для большинства приложений и является всем, что требуется.
* Переключиться на использование`AddAuthorizationCore`

Для получения дополнительной информации [см. Breaking change in `AddAuthorization(o =>`) перегрузка живет в другой сборке #386](https://github.com/aspnet/Announcements/issues/386).

## <a name="identity-ui"></a>Удостоверение личности

Обновления uI для ASP.NET Core 3.0:

* Добавьте ссылку на пакет [microsoft.AspNetCore.Identity.UI](https://nuget.org/packages/Microsoft.AspNetCore.Identity.UI).
* Приложения, которые не используют Страницы `MapRazorPages`бритвы, должны вызываться под . В этом документе смотрите [страницы Razor.](#razor-pages)
* Bootstrap 4 — это платформа uI по умолчанию. Установите `IdentityUIFrameworkVersion` свойство проекта для изменения значения по умолчанию. Для получения дополнительной информации смотрите [это объявление GitHub](https://github.com/aspnet/Announcements/issues/380).

## SignalR

Клиент SignalR JavaScript изменился `@aspnet/signalr` `@microsoft/signalr`с . Чтобы отреагировать на это изменение, измените ссылки в файлах, *package.json* `require` заявлениях и заявлениях ECMAScript. `import`

### <a name="systemtextjson-is-the-default-protocol"></a>System.Text.Json является протоколом по умолчанию

`System.Text.Json`теперь протокол концентратора по умолчанию, используемый как клиентом, так и сервером.

В `Startup.ConfigureServices`, `AddJsonProtocol` вызов для установки опций serializer.

**Сервера:**

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.PayloadSerializerOptions.WriteIndented = false;
        })
```

**Клиента:**

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.PayloadSerializerOptions.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a>Переключиться на Ньютонсофт.Джсон

Если вы используете [функции Newtonsoft.Json, которые не поддерживаются в System.Text.Json,](/dotnet/standard/serialization/system-text-json-migrate-from-newtonsoft-how-to)вы можете вернуться к `Newtonsoft.Json`. Смотрите [Использование Newtonsoft.Json в ASP.NET SignalR Core 3.0 проекта](#use-newtonsoftjson-in-an-aspnet-core-30-signalr-project) ранее в этой статье.

## <a name="redis-distributed-caches"></a>Redis распределил тайники

Пакет [Microsoft.Extensions.Caching.Redis](https://www.nuget.org/packages/Microsoft.Extensions.Caching.Redis) недоступен для ASP.NET Core 3.0 или для более поздних приложений. Замените ссылку на пакет [на сайте Microsoft.Extensions.Caching.StackExchangeRedis](https://www.nuget.org/packages/Microsoft.Extensions.Caching.StackExchangeRedis). Для получения дополнительной информации см. <xref:performance/caching/distributed>.

## <a name="opt-in-to-runtime-compilation"></a>Выберите компиляцию времени выполнения

До ASP.NET Core 3.0 компиляция представлений во времени выполнения была неявной особенностью платформы. Компиляция Runtime дополняет компиляцию представлений времен сборки. Это позволяет инфраструктуре компилировать представления и страницы Razor *(файлы .cshtml),* когда файлы изменены, без необходимости перестраивать все приложение. Эта функция поддерживает сценарий быстрого изменения iDE и обновления браузера для просмотра изменений.

В ASP.NET Core 3.0 компиляция времени выполнения является сценарием отказа. Компиляция времени сборки является единственным механизмом компиляции представления, включенным по умолчанию. Время выполнения зависит от Visual Studio или [дотнет-часы](xref:tutorials/dotnet-watch) в Visual Studio Code для восстановления проекта, когда он обнаруживает изменения в *файлах .cshtml.* В Visual Studio, изменения в *.cs*, *.cshtml*, или *.razor* файлы в проекте, который запускается (<kbd>Ctrl-F5</kbd>), но не отлажены (<kbd>F5</kbd>), вызвать перекомпиляцию проекта.

Для включения компиляции времени выполнения в проекте ASP.NET Core 3.0:

1. Установите пакет [Microsoft.AspNetCore.Mvc.Razor.RuntimeCompilation](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.Razor.RuntimeCompilation) NuGet.
1. Обновление `Startup.ConfigureServices` для `AddRazorRuntimeCompilation`вызова:

    Для ASP.NET Core MVC используйте следующий код:

    ```csharp
    services.AddControllersWithViews()
        .AddRazorRuntimeCompilation(...);
    ```
 
    Для ASP.NET Core Razor Pages используйте следующий код:
 
    ```csharp
    services.AddRazorPages()
        .AddRazorRuntimeCompilation(...);
    ```
 
В выборке https://github.com/aspnet/samples/tree/master/samples/aspnetcore/mvc/runtimecompilation показан пример включения компиляции времени выполнения условно в средах разработки.

Для получения дополнительной информации о <xref:mvc/views/view-compilation>компиляции файлов Razor см.

## <a name="migrate-libraries-via-multi-targeting"></a>Мигрировать библиотеки через мультитаргетинг

Библиотекам часто требуется поддерживать несколько версий ASP.NET Core. Большинство библиотек, которые были составлены в отношении предыдущих версий ASP.NET Core, должны продолжать работать без проблем. Следующие условия требуют, чтобы приложение было перекрестным компилировано:

* Библиотека опирается на функцию, которая имеет двоичное [изменение лома.](#breaking-api-changes)
* Библиотека хочет воспользоваться новыми функциями в ASP.NET Core 3.0. 

Пример:

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>netcoreapp3.0;netstandard2.0</TargetFrameworks>
  </PropertyGroup>

  <ItemGroup Condition="'$(TargetFramework)' == 'netcoreapp3.0'">
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup Condition="'$(TargetFramework)' == 'netstandard2.0'">
    <PackageReference Include="Microsoft.AspNetCore" Version="2.1.0" />
  </ItemGroup>
</Project>
```

Используйте `#ifdefs` для включения ASP.NET основных AAP 3.0:

```csharp
var webRootFileProvider =
#if NETCOREAPP3_0
    GetRequiredService<IWebHostEnvironment>().WebRootFileProvider;
#elif NETSTANDARD2_0
    GetRequiredService<IHostingEnvironment>().WebRootFileProvider;
#else
#error unknown target framework
#endif
```

Для получения дополнительной информации об использовании ASP.NET <xref:fundamentals/target-aspnetcore>основных AIS в библиотеке класса см.

## <a name="miscellaneous-changes"></a>Прочие изменения

Система проверки в .NET Core 3.0 и более поздних версий рассматривает не допускающие значение NULL параметры или свойства привязки так, как если бы они имели атрибут `[Required]`. Для получения дополнительной [[Required] attribute](xref:mvc/models/validation?view=aspnetcore-3.1#required-attribute)информации см.

### <a name="publish"></a>Публикация

Удалите папки *ячейки* и *оби* в каталоге проекта.

## <a name="testserver"></a>TestServer

Для приложений, которые <xref:Microsoft.AspNetCore.TestHost.TestServer> используют непосредственно с общим [хостом,](xref:fundamentals/host/web-host)создайте `TestServer` <xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> <xref:Microsoft.Extensions.Hosting.GenericHostWebHostBuilderExtensions.ConfigureWebHost%2A>в:

```csharp
[Fact]
public async Task GenericCreateAndStartHost_GetTestServer()
{
    using var host = await new HostBuilder()
        .ConfigureWebHost(webBuilder =>
        {
            webBuilder
                .UseTestServer()
                .Configure(app => { });
        })
    .StartAsync();

    var response = await host.GetTestServer().CreateClient().GetAsync("/");

    Assert.Equal(HttpStatusCode.NotFound, response.StatusCode);
}
```

<a name="break"></a>

## <a name="breaking-api-changes"></a>Нарушение изменений API

Просмотр изменений:

* [Полный список изменений в ASP.NET выпуске Core 3.0](https://github.com/aspnet/Announcements/issues?page=1&q=is%3Aissue+is%3Aopen+label%3A%22Breaking+change%22+label%3A3.0.0)
* [Нарушение изменений API в антифоргерии, CORS, диагностике, MVC и Routing.](https://github.com/aspnet/Announcements/issues/387) Этот список содержит критические изменения для параметров совместимости.
* Для резюме 2,2-к-3.0 нарушение изменений в .NET Core, ASP.NET Core и Entity Framework Core [см. Нарушение изменений для миграции из версии 2.2 до 3.0](/dotnet/core/compatibility/2.2-3.0).

## <a name="net-core-30-on-azure-app-service"></a>.NET Core 3.0 на сервисе приложений Azure

Для достижения прогресса в развертывании системы приложений .NET в Azure— см. [.NET Core on App Service](https://aspnetcoreon.azurewebsites.net/) До тех пор, пока .NET Core 3.0 не будет доступен в службе приложений Azure, следуйте инструкциям в [развертывании ASP.NET выпуске предварительного просмотра в Azure App Service.](xref:host-and-deploy/azure-apps/index#deploy-aspnet-core-preview-release-to-azure-app-service)
