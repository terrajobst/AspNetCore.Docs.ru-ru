---
title: Миграция с ASP.NET Core предварительной версии 2.2 до 3.0
author: tdykstra
description: Узнайте, как перенести проекте ASP.NET Core 2.2 в ASP.NET Core 3.0.
ms.author: tdykstra
ms.custom: mvc
ms.date: 06/17/2019
uid: migration/22-to-30
ms.openlocfilehash: 85f640d968b7b613f5cc8634e5d1ef1102c06e68
ms.sourcegitcommit: 357a7120632b20465801c093e4e5bd4a315496a8
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 07/08/2019
ms.locfileid: "67649244"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="a399a-103">Миграция с ASP.NET Core 2.2 до 3.0</span><span class="sxs-lookup"><span data-stu-id="a399a-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="a399a-104">По [Scott Addie](https://github.com/scottaddie) и [Рик Андерсон](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="a399a-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="a399a-105">В этой статье объясняется, как выполнить обновление до ASP.NET Core 3.0 существующий проект ASP.NET Core 2.2.</span><span class="sxs-lookup"><span data-stu-id="a399a-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="a399a-106">Предварительные требования</span><span class="sxs-lookup"><span data-stu-id="a399a-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="a399a-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="a399a-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="a399a-108">Visual Studio Code.</span><span class="sxs-lookup"><span data-stu-id="a399a-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="a399a-109">Visual Studio для Mac</span><span class="sxs-lookup"><span data-stu-id="a399a-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="a399a-110">Обновляет файл проекта</span><span class="sxs-lookup"><span data-stu-id="a399a-110">Update the project file</span></span>

* <span data-ttu-id="a399a-111">Задайте [моникер целевой платформы (TFM)](/dotnet/standard/frameworks#referring-to-frameworks) для `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="a399a-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks#referring-to-frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="a399a-112">Удалите все `<PackageReference>` для [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) или [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) метапакет.</span><span class="sxs-lookup"><span data-stu-id="a399a-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="a399a-113">Обновление `Version` атрибут на оставшихся `<PackageReference>` элементы для `Microsoft.AspNetCore.*` пакетов в текущей предварительной версии (например, `3.0.0-preview5-19227-01`).</span><span class="sxs-lookup"><span data-stu-id="a399a-113">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="a399a-114">Если отсутствует версия 3.0 пакета, пакет может стали нерекомендуемыми в 3.0.</span><span class="sxs-lookup"><span data-stu-id="a399a-114">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="a399a-115">Многие из этих пакетов являются частью `Microsoft.AspNetCore.App` и не должен ссылаться по отдельности.</span><span class="sxs-lookup"><span data-stu-id="a399a-115">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="a399a-116">Для предварительного списка пакетов, больше не создаются в 3.0, см. в разделе [перестать формировать пакеты для сборок общей платформы 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="a399a-116">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="a399a-117">*Общей платформы* — это набор сборок ( *.dll* файлы), которые установлены на компьютере и ссылается `Microsoft.AspNetCore.App`.</span><span class="sxs-lookup"><span data-stu-id="a399a-117">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="a399a-118">Дополнительную информацию см. в этой публикации об [общей платформе](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span><span class="sxs-lookup"><span data-stu-id="a399a-118">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="a399a-119">Сборки для нескольких важных компонентов были удалены из `Microsoft.AspNetCore.App` в 3.0.</span><span class="sxs-lookup"><span data-stu-id="a399a-119">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="a399a-120">Добавить `<PackageReference>` элементов, если вы используете API-интерфейсы из пакеты, перечисленные в [сборки, удаляемой из Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="a399a-120">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="a399a-121">Примеры удаленных компонентов.</span><span class="sxs-lookup"><span data-stu-id="a399a-121">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="a399a-122">Список сборок, доставкой `Microsoft.AspNetCore.App` еще нет и приведет к изменению до 3.0 RTM.</span><span class="sxs-lookup"><span data-stu-id="a399a-122">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="a399a-123">Рассмотрим следующий код.</span><span class="sxs-lookup"><span data-stu-id="a399a-123">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="a399a-124">`ReadAsAsync` Метод с именем в приведенном выше коде включается в `Microsoft.AspNet.WebApi.Client`.</span><span class="sxs-lookup"><span data-stu-id="a399a-124">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="a399a-125">Установка [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) пакет NuGet для решения проблемы компиляции в 3.0.</span><span class="sxs-lookup"><span data-stu-id="a399a-125">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="a399a-126">Добавить [Json.NET поддержки](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="a399a-126">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="a399a-127">По умолчанию проекты [модель размещения в процессе](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) в ASP.NET Core 3.0 или более поздней версии.</span><span class="sxs-lookup"><span data-stu-id="a399a-127">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="a399a-128">Вы можете при необходимости удалить `<AspNetCoreHostingModel>` свойство в файле проекта, если его значение равно `InProcess`.</span><span class="sxs-lookup"><span data-stu-id="a399a-128">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="a399a-129">Поддержка Json.NET</span><span class="sxs-lookup"><span data-stu-id="a399a-129">Json.NET support</span></span>

<span data-ttu-id="a399a-130">Как часть работы по [повышения общей платформы ASP.NET Core](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) был удален из общей платформы ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="a399a-130">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="a399a-131">Чтобы использовать Json.NET в проекте ASP.NET Core 3.0:</span><span class="sxs-lookup"><span data-stu-id="a399a-131">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="a399a-132">Добавьте ссылку на пакет [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="a399a-132">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="a399a-133">Обновление `Startup.ConfigureServices` для вызова `AddNewtonsoftJson`.</span><span class="sxs-lookup"><span data-stu-id="a399a-133">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="a399a-134">`AddNewtonsoftJson` совместим с помощью новых методов регистрации службы MVC:</span><span class="sxs-lookup"><span data-stu-id="a399a-134">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="a399a-135">Можно задать параметры Json.NET в вызове `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="a399a-135">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="a399a-136">Регистрация службы MVC</span><span class="sxs-lookup"><span data-stu-id="a399a-136">MVC service registration</span></span>

<span data-ttu-id="a399a-137">ASP.NET Core 3.0 добавляет новые возможности для регистрации сценариев MVC внутри `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="a399a-137">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="a399a-138">Три новых методов расширения верхнего уровня, связанные с MVC на `IServiceCollection` доступны.</span><span class="sxs-lookup"><span data-stu-id="a399a-138">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="a399a-139">Эти новые методы, а не использовать шаблоны `UseMvc`.</span><span class="sxs-lookup"><span data-stu-id="a399a-139">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="a399a-140">Тем не менее `AddMvc` поведение, что и в предыдущих выпусках.</span><span class="sxs-lookup"><span data-stu-id="a399a-140">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="a399a-141">В следующем примере добавляется поддержка для контроллеров и функции, связанные с API, но не представлений или страниц.</span><span class="sxs-lookup"><span data-stu-id="a399a-141">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="a399a-142">Шаблон API использует этот код:</span><span class="sxs-lookup"><span data-stu-id="a399a-142">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="a399a-143">В следующем примере добавляется поддержка контроллеров, функции, связанные с API и представления, но не для страниц.</span><span class="sxs-lookup"><span data-stu-id="a399a-143">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="a399a-144">Шаблон веб-приложение (MVC) использует этот код:</span><span class="sxs-lookup"><span data-stu-id="a399a-144">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="a399a-145">В следующем примере добавляется поддержка Razor Pages и минимальной контроллера.</span><span class="sxs-lookup"><span data-stu-id="a399a-145">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="a399a-146">Шаблон веб-приложение использует этот код:</span><span class="sxs-lookup"><span data-stu-id="a399a-146">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="a399a-147">Новые методы также могут быть объединены.</span><span class="sxs-lookup"><span data-stu-id="a399a-147">The new methods can also be combined.</span></span> <span data-ttu-id="a399a-148">Следующий пример аналогичен вызову `AddMvc` в ASP.NET Core 2.2:</span><span class="sxs-lookup"><span data-stu-id="a399a-148">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="a399a-149">Обновить маршрутизации код запуска</span><span class="sxs-lookup"><span data-stu-id="a399a-149">Update routing startup code</span></span>

<span data-ttu-id="a399a-150">Если приложение вызывает `UseMvc` или `UseSignalR`, перенести приложение, чтобы [конечной точки маршрутизации](xref:fundamentals/routing) , если это возможно.</span><span class="sxs-lookup"><span data-stu-id="a399a-150">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="a399a-151">Для улучшения маршрутизации конечной точки совместимости с предыдущими версиями MVC, мы восстановлено ряд изменений в формировании URL-адресов, представленные в ASP.NET Core 2.2.</span><span class="sxs-lookup"><span data-stu-id="a399a-151">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="a399a-152">Возникли проблемы при использовании маршрутизации конечной точки в 2.2, следует ожидать усовершенствования в ASP.NET Core 3.0 со следующими исключениями:</span><span class="sxs-lookup"><span data-stu-id="a399a-152">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="a399a-153">Если приложение реализует `IRouter` или наследует от `Route`, вы можете избежать переноса в данный момент.</span><span class="sxs-lookup"><span data-stu-id="a399a-153">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="a399a-154">Отзыв в нижней [планируется перенести IRouter основе реализаций на конечную точку маршрута](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="a399a-154">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="a399a-155">Если приложение напрямую обращается к `RouteData.Routers` внутри MVC, можно избежать переноса в данный момент.</span><span class="sxs-lookup"><span data-stu-id="a399a-155">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="a399a-156">Отзыв в нижней [руководство по миграции для использования RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="a399a-156">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="a399a-157">Конечная точка маршрутизации поддерживает того же синтаксиса шаблона маршрута и шаблон маршрута, создание функции, что `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="a399a-157">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="a399a-158">Конечная точка поддерживает маршрутизации `IRouteContraint`.</span><span class="sxs-lookup"><span data-stu-id="a399a-158">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="a399a-159">Конечная точка маршрутизации поддерживает `[Route]`, `[HttpGet]`и других атрибутов маршрутизации MVC.</span><span class="sxs-lookup"><span data-stu-id="a399a-159">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="a399a-160">Для большинства приложений, только `Startup` требует внесения изменений.</span><span class="sxs-lookup"><span data-stu-id="a399a-160">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="a399a-161">Перенос Startup.Configure</span><span class="sxs-lookup"><span data-stu-id="a399a-161">Migrate Startup.Configure</span></span>

<span data-ttu-id="a399a-162">Общий Совет:</span><span class="sxs-lookup"><span data-stu-id="a399a-162">General advice:</span></span>

* <span data-ttu-id="a399a-163">Добавление `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="a399a-163">Add `UseRouting`.</span></span> 
* <span data-ttu-id="a399a-164">Если приложение вызывает `UseStaticFiles`, поместите `UseStaticFiles` **перед** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="a399a-164">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="a399a-165">Если приложение использует функции проверки подлинности и авторизации, таких как `AuthorizePage` или `[Authorize]`, вызвать `UseAuthentication` и `UseAuthorization` **после** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="a399a-165">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="a399a-166">Если приложение использует [CORS](xref:security/cors) компонентам, таким как `[EnableCors]`, поместите `UseCors` Далее.</span><span class="sxs-lookup"><span data-stu-id="a399a-166">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="a399a-167">Замените `UseMvc` или `UseSignalR` с `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a399a-167">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="a399a-168">Ниже приведен пример `Startup.Configure` в типичном приложении ASP.NET Core 2.2:</span><span class="sxs-lookup"><span data-stu-id="a399a-168">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="a399a-169">После обновления предыдущей `Startup.Configure` кода:</span><span class="sxs-lookup"><span data-stu-id="a399a-169">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="a399a-170">Руководство по безопасности по промежуточного слоя</span><span class="sxs-lookup"><span data-stu-id="a399a-170">Security middleware guidance</span></span>

<span data-ttu-id="a399a-171">Поддержка авторизации и CORS унифицирована вокруг [по промежуточного слоя](xref:fundamentals/middleware/index) подход.</span><span class="sxs-lookup"><span data-stu-id="a399a-171">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="a399a-172">Это позволяет использовать те же по промежуточного слоя и функциональные возможности в этих сценариях.</span><span class="sxs-lookup"><span data-stu-id="a399a-172">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="a399a-173">По промежуточного слоя для обновленных авторизации, реализованные в этом выпуске, и по промежуточного слоя CORS улучшены, чтобы он мог распознать атрибуты, используемые контроллерами MVC.</span><span class="sxs-lookup"><span data-stu-id="a399a-173">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="a399a-174">CORS</span><span class="sxs-lookup"><span data-stu-id="a399a-174">CORS</span></span>

<span data-ttu-id="a399a-175">Ранее может быть сложно настроить CORS.</span><span class="sxs-lookup"><span data-stu-id="a399a-175">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="a399a-176">По промежуточного слоя был предоставлен для использования в некоторые варианты использования, но фильтры MVC были предназначен для использования **без** по промежуточного слоя в других вариантах использования.</span><span class="sxs-lookup"><span data-stu-id="a399a-176">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="a399a-177">С помощью ASP.NET Core 3.0 — рекомендуется использовать все приложения, требующие CORS по промежуточного слоя CORS в тандеме с маршрутизацией конечной точки.</span><span class="sxs-lookup"><span data-stu-id="a399a-177">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="a399a-178">`UseCors` можно указать политику по умолчанию, и `[EnableCors]` и `[DisableCors]` атрибутов может использоваться для переопределения политики по умолчанию там, где необходимо.</span><span class="sxs-lookup"><span data-stu-id="a399a-178">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="a399a-179">В следующем примере:</span><span class="sxs-lookup"><span data-stu-id="a399a-179">In the following example:</span></span>

* <span data-ttu-id="a399a-180">CORS включен для всех конечных точек с `default` с именем политики.</span><span class="sxs-lookup"><span data-stu-id="a399a-180">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="a399a-181">`MyController` Класс отключает CORS с `[DisableCors]` атрибута.</span><span class="sxs-lookup"><span data-stu-id="a399a-181">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="a399a-182">Авторизация</span><span class="sxs-lookup"><span data-stu-id="a399a-182">Authorization</span></span>

<span data-ttu-id="a399a-183">В более ранних версиях ASP.NET Core, для поддержки проверки подлинности был предоставлен через `[Authorize]` атрибута.</span><span class="sxs-lookup"><span data-stu-id="a399a-183">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="a399a-184">По промежуточного слоя авторизации был недоступен.</span><span class="sxs-lookup"><span data-stu-id="a399a-184">Authorization middleware wasn't available.</span></span> <span data-ttu-id="a399a-185">В ASP.NET Core 3.0 — по промежуточного слоя для авторизации не требуется.</span><span class="sxs-lookup"><span data-stu-id="a399a-185">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="a399a-186">Рекомендуется размещать по промежуточного слоя ASP.NET Core авторизации (`UseAuthorization`) сразу же после `UseAuthentication`.</span><span class="sxs-lookup"><span data-stu-id="a399a-186">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="a399a-187">По промежуточного слоя авторизации можно также настроить политику по умолчанию, который можно переопределить.</span><span class="sxs-lookup"><span data-stu-id="a399a-187">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="a399a-188">В ASP.NET Core 3.0 или более поздней версии `UseAuthorization` вызывается в `Startup.Configure`и следующие `HomeController` требует со знаком в пользователя:</span><span class="sxs-lookup"><span data-stu-id="a399a-188">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="a399a-189">Если приложение использует `AuthorizeFilter` глобального фильтра в MVC, мы рекомендуем рефакторинг код, чтобы указать политику, чтобы `UseAuthorization` по промежуточного слоя.</span><span class="sxs-lookup"><span data-stu-id="a399a-189">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="a399a-190">В следующем примере настраиваемой политики, применяемые ко всем запрашивает при `UseAuthorization` вызывается и `HomeController` разрешает доступ без пользователя, при входе в приложение:</span><span class="sxs-lookup"><span data-stu-id="a399a-190">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="a399a-191">Авторизация с использованием по промежуточного слоя работает без необходимости знания конкретного авторизации framework.</span><span class="sxs-lookup"><span data-stu-id="a399a-191">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="a399a-192">Например [проверки работоспособности](xref:host-and-deploy/health-checks) не требуются сведения авторизации, но проверки работоспособности можно настроить политику авторизации можно настроить, применить по промежуточного слоя.</span><span class="sxs-lookup"><span data-stu-id="a399a-192">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="a399a-193">В следующем примере `UseAuthorization` обрабатывает авторизации без политики по умолчанию, но `/healthz` конечную точку проверки работоспособности требует, чтобы пользователь был в `admin` роли:</span><span class="sxs-lookup"><span data-stu-id="a399a-193">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="a399a-194">Защита реализована для некоторых сценариев.</span><span class="sxs-lookup"><span data-stu-id="a399a-194">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="a399a-195">`UseEndpoint` по промежуточного слоя вызывает исключение, если авторизации или политики CORS пропускается из-за отсутствующих по промежуточного слоя.</span><span class="sxs-lookup"><span data-stu-id="a399a-195">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="a399a-196">Поддержка анализатора предоставить дополнительный отзыв о неправильной настройки выполняется.</span><span class="sxs-lookup"><span data-stu-id="a399a-196">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="a399a-197">Перенос SignalR</span><span class="sxs-lookup"><span data-stu-id="a399a-197">Migrate SignalR</span></span>

<span data-ttu-id="a399a-198">Сопоставления концентраторов SignalR выполняемое внутри `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a399a-198">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="a399a-199">Сопоставьте каждый центр с `MapHub`.</span><span class="sxs-lookup"><span data-stu-id="a399a-199">Map each hub with `MapHub`.</span></span> <span data-ttu-id="a399a-200">Как и в предыдущих версиях в каждом центре явно перечислены.</span><span class="sxs-lookup"><span data-stu-id="a399a-200">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="a399a-201">В следующем примере поддержка `ChatHub` добавляется концентратора SignalR:</span><span class="sxs-lookup"><span data-stu-id="a399a-201">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="a399a-202">Миграция контроллеров MVC</span><span class="sxs-lookup"><span data-stu-id="a399a-202">Migrate MVC controllers</span></span>

<span data-ttu-id="a399a-203">Сопоставление контроллеров выполняемое внутри `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a399a-203">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="a399a-204">Добавление `MapControllers` Если приложение использует маршрутизацию с помощью атрибутов.</span><span class="sxs-lookup"><span data-stu-id="a399a-204">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="a399a-205">Поскольку Маршрутизация включает поддержку многих платформ, в ASP.NET Core 3.0 или более поздней версии, добавление атрибутов маршрутизации контроллеров — участвовать.</span><span class="sxs-lookup"><span data-stu-id="a399a-205">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="a399a-206">Замените следующий код:</span><span class="sxs-lookup"><span data-stu-id="a399a-206">Replace the following:</span></span>

* <span data-ttu-id="a399a-207">`MapRoute` с помощью `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="a399a-207">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="a399a-208">`MapAreaRoute` с помощью `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="a399a-208">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="a399a-209">Поскольку Маршрутизация теперь включает поддержку больше, чем просто MVC, терминологии был изменен на эти методы, которые явно указано, что они делают.</span><span class="sxs-lookup"><span data-stu-id="a399a-209">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="a399a-210">Обычные маршруты, такие как `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` применяются в порядке, что они добавляются.</span><span class="sxs-lookup"><span data-stu-id="a399a-210">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="a399a-211">Сначала установите более определенных маршрутов (например, маршруты для области).</span><span class="sxs-lookup"><span data-stu-id="a399a-211">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="a399a-212">В следующем примере:</span><span class="sxs-lookup"><span data-stu-id="a399a-212">In the following example:</span></span>

* <span data-ttu-id="a399a-213">`MapControllers` добавлена поддержка маршрутизации атрибутов контроллеров.</span><span class="sxs-lookup"><span data-stu-id="a399a-213">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="a399a-214">`MapAreaControllerRoute` Добавляет маршрут на основе соглашения для контроллеров в области.</span><span class="sxs-lookup"><span data-stu-id="a399a-214">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="a399a-215">`MapControllerRoute` Добавляет маршрут на основе соглашения для контроллеров.</span><span class="sxs-lookup"><span data-stu-id="a399a-215">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="a399a-216">Перенести страницы Razor</span><span class="sxs-lookup"><span data-stu-id="a399a-216">Migrate Razor Pages</span></span>

<span data-ttu-id="a399a-217">Сопоставление Razor Pages теперь происходит в `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a399a-217">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="a399a-218">Добавление `MapRazorPages` Если приложение использует Razor Pages.</span><span class="sxs-lookup"><span data-stu-id="a399a-218">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="a399a-219">Так как конечная точка маршрутизации включает поддержку многих платформ, добавление страниц Razor — теперь участвовать.</span><span class="sxs-lookup"><span data-stu-id="a399a-219">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="a399a-220">В следующем примере `MapRazorPages` добавляет поддержку для Razor Pages:</span><span class="sxs-lookup"><span data-stu-id="a399a-220">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="a399a-221">Использовать MVC без маршрутизации конечной точки</span><span class="sxs-lookup"><span data-stu-id="a399a-221">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="a399a-222">С помощью MVC с помощью `UseMvc` или `UseMvcWithDefaultRoute` в ASP.NET Core 3.0 необходимо явно согласиться внутри `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="a399a-222">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="a399a-223">Это необходимо, так как MVC необходимо знать, является ли он может полагаться на авторизации и по промежуточного слоя CORS во время инициализации.</span><span class="sxs-lookup"><span data-stu-id="a399a-223">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="a399a-224">Анализатор предоставляется, выдает предупреждение, если приложение пытается использовать Неподдерживаемая конфигурация.</span><span class="sxs-lookup"><span data-stu-id="a399a-224">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="a399a-225">Если приложению требуется прежних версий `IRouter` поддержки, отключите `EnableEndpointRouting` с помощью любого из следующих приближается к в `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="a399a-225">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="a399a-226">Миграция проверки работоспособности</span><span class="sxs-lookup"><span data-stu-id="a399a-226">Migrate health checks</span></span>

<span data-ttu-id="a399a-227">Проверки работоспособности можно использовать в качестве *маршрутизатора наиболее* с маршрутизацией конечной точки.</span><span class="sxs-lookup"><span data-stu-id="a399a-227">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="a399a-228">Добавление `MapHealthChecks` для использования работоспособности проверяет с маршрутизацией конечной точки.</span><span class="sxs-lookup"><span data-stu-id="a399a-228">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="a399a-229">`MapHealthChecks` Метод принимает аргументы, аналогичную `UseHealthChecks`.</span><span class="sxs-lookup"><span data-stu-id="a399a-229">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="a399a-230">Преимущество использования `MapHealthChecks` через `UseHealthChecks` является возможность применять проверку подлинности и для более детального контроля над соответствующей политики.</span><span class="sxs-lookup"><span data-stu-id="a399a-230">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="a399a-231">В следующем примере `MapHealthChecks` вызывается для конечной точки проверки работоспособности в `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="a399a-231">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="a399a-232">Заменяет HostBuilder WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="a399a-232">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="a399a-233">Шаблоны ASP.NET Core 3.0 используют [универсальный узел](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="a399a-233">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="a399a-234">Использовать предыдущие версии [веб-узел](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="a399a-234">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="a399a-235">В следующем коде показано создание шаблона ASP.NET Core 3.0 `Program` класса:</span><span class="sxs-lookup"><span data-stu-id="a399a-235">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="a399a-236">В следующем коде показано Core в ASP.NET 2.2, в созданный шаблон `Program` класса:</span><span class="sxs-lookup"><span data-stu-id="a399a-236">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="a399a-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> остается в 3.0, и тип `webBuilder` видели в предыдущем примере кода.</span><span class="sxs-lookup"><span data-stu-id="a399a-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="a399a-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> Рекомендуется использовать в будущем выпуске и заменяется `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="a399a-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="a399a-239">Наиболее значительное изменение из `WebHostBuilder` для `HostBuilder` в [внедрения зависимостей (DI)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="a399a-239">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="a399a-240">При использовании `HostBuilder`, вы можете только внедрить <xref:Microsoft.Extensions.Configuration.IConfiguration> и <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> в `Startup`в конструктор.</span><span class="sxs-lookup"><span data-stu-id="a399a-240">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="a399a-241">`HostBuilder` DI ограничения:</span><span class="sxs-lookup"><span data-stu-id="a399a-241">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="a399a-242">Включите контейнер с внедрением Зависимостей построения только один раз.</span><span class="sxs-lookup"><span data-stu-id="a399a-242">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="a399a-243">Позволяет избежать итоговый проблемы времени существования объекта, например устранение нескольких экземпляров одноэлементных экземпляров.</span><span class="sxs-lookup"><span data-stu-id="a399a-243">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="a399a-244">Обновление кода SignalR</span><span class="sxs-lookup"><span data-stu-id="a399a-244">Update SignalR code</span></span>

<span data-ttu-id="a399a-245">`System.Text.Json` — Теперь по умолчанию центр протокол, используемый клиентом и сервером.</span><span class="sxs-lookup"><span data-stu-id="a399a-245">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="a399a-246">В `Startup.ConfigureServices`, вызовите `AddJsonProtocol` для задания параметров сериализатора.</span><span class="sxs-lookup"><span data-stu-id="a399a-246">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="a399a-247">**Сервер:**</span><span class="sxs-lookup"><span data-stu-id="a399a-247">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="a399a-248">**Клиент:**</span><span class="sxs-lookup"><span data-stu-id="a399a-248">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="a399a-249">Переключиться в режим Newtonsoft.Json</span><span class="sxs-lookup"><span data-stu-id="a399a-249">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="a399a-250">Если вы используете возможности `Newtonsoft.Json` , не поддерживаются в `System.Text.Json`, вы можете переключиться обратно `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="a399a-250">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="a399a-251">Установка [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) пакет NuGet.</span><span class="sxs-lookup"><span data-stu-id="a399a-251">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="a399a-252">На стороне клиента, можно объединить в цепочку `AddNewtonsoftJsonProtocol` вызов метода `HubConnectionBuilder` экземпляр:</span><span class="sxs-lookup"><span data-stu-id="a399a-252">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="a399a-253">На сервере, можно объединить в цепочку `AddNewtonsoftJsonProtocol` вызов метода `AddSignalR` вызов метода `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="a399a-253">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="a399a-254">Согласиться на компиляции во время выполнения</span><span class="sxs-lookup"><span data-stu-id="a399a-254">Opt in to runtime compilation</span></span>

<span data-ttu-id="a399a-255">В версии 3.0 сценарий согласием не компиляции во время выполнения.</span><span class="sxs-lookup"><span data-stu-id="a399a-255">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="a399a-256">Чтобы включить компиляцию среды выполнения, см. в разделе <xref:mvc/views/view-compilation#runtime-compilation>.</span><span class="sxs-lookup"><span data-stu-id="a399a-256">To enable runtime compilation, see <xref:mvc/views/view-compilation#runtime-compilation>.</span></span>
