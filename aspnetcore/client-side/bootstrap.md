---
title: Создание привлекательных и адаптивных веб-сайтов с помощью начальной загрузки и ASP.NET Core
author: ardalis
description: Сведения об использовании начальной загрузки для разработки быстрых веб-приложений с помощью ASP.NET Core.
ms.author: riande
ms.date: 10/14/2016
uid: client-side/bootstrap
ms.openlocfilehash: 1ccf33b299739f5aa963a53feb70b44b290443ca
ms.sourcegitcommit: d53e0cc71542b92de867bcce51575b054886f529
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 08/16/2018
ms.locfileid: "41836434"
---
# <a name="build-beautiful-responsive-sites-with-bootstrap-and-aspnet-core"></a>Создание привлекательных и адаптивных веб-сайтов с помощью начальной загрузки и ASP.NET Core

<a name="bootstrap-index"></a>

Автор: [Стив Смит](https://ardalis.com/) (Steve Smith)

Начальной загрузки в данный момент является наиболее популярных веб-платформа для разработки быстро реагирующих веб-приложений. Он предлагает ряд функций и преимуществ, которые могут повысить удобство работы пользователей с веб-сайт, будь вы новичок в переднего плана проектирования и разработки или экспертом. Bootstrap развертывается как набор файлов CSS и JavaScript и предназначен для масштабирования веб-сайта или приложения эффективно от телефонов для планшетных ПК для настольных компьютеров.

## <a name="get-started"></a>Начало работы

Чтобы приступить к работе с помощью Bootstrap несколькими способами. Если вы начинаете новый веб-приложения в Visual Studio, можно выбрать начальный шаблон по умолчанию для ASP.NET Core, в котором будут поступать предустановленных вариантов начальной загрузки:

![Начальная загрузка в представлении решения начального шаблона](bootstrap/_static/bootstrap-in-starter-template.png)

Добавление начальной загрузки для ASP.NET Core проекта является просто добавляется в *bower.json* как зависимость:

[!code-json[](../common/samples/WebApplication1/bower.json?highlight=5)]

Это рекомендуемый способ добавить начальной загрузки в проекте ASP.NET Core.

Также можно установить с помощью одного из несколько диспетчеров пакетов, например Bower, npm или NuGet начальной загрузки. В каждом случае процесс очень похожа:

### <a name="bower"></a>Bower

```console
bower install bootstrap
```

### <a name="npm"></a>npm

```console
npm install bootstrap
```

### <a name="nuget"></a>NuGet

```console
Install-Package bootstrap
```

> [!NOTE]
> Рекомендуется для установки зависимостей на стороне клиента, как Bootstrap в ASP.NET Core с помощью Bower (с помощью *bower.json*, как показано выше). Чтобы продемонстрировать, насколько легко начальной загрузки можно добавить другие виды веб-приложений, включая более ранних версиях ASP.NET показаны использование npm и NuGet.

Если при попытке сослаться на локальные версии программы начальной загрузки, необходимо ссылаться на них в любые страницы, которые будут его использовать. В рабочей среде следует ссылаться на начальной загрузки, с помощью CDN. В шаблоне сайта по умолчанию ASP.NET Core *_Layout.cshtml* файл таким образом следующим образом:

[!code-html[](../common/samples/WebApplication1/Views/Shared/_Layout.cshtml?highlight=9,13,51,59)]

> [!NOTE]
> Если вы собираетесь использовать какую-либо подключаемые модули jQuery Bootstrap, также необходимо будет ссылаться на jQuery.

## <a name="basic-templates-and-features"></a>Базовых шаблонов и компонентов

Самый простой шаблон Bootstrap выглядит очень похожим *_Layout.cshtml* файл, показанный выше, а просто включает базовое меню навигации и место для отрисовки оставшейся части страницы.

### <a name="basic-navigation"></a>Основные средства перемещения

Шаблон по умолчанию использует набор `<div>` элементы, которые отображаются в верхней панели навигации и основной части страницы. Если вы используете HTML5, вы можете заменить первый `<div>` тег с `<nav>` тег, чтобы получить тот же эффект, но с более точной семантики. В этом первом `<div>` вы видите, есть и другие. Во-первых, `<div>` с классом «container», а затем в рамках этого, две дополнительные `<div>` элементов: «navbar-header» и «navbar-collapse». Div заголовке панели имеется кнопка, которая будет отображаться, когда экран под определенные минимальную ширину, показывающая три горизонтальные линии (так называемое «"значок" гамбургер»). Значок отображается с помощью чистый HTML и CSS; изображение не является обязательным. Это код, который отображает значок, с каждым из <span> теги, один из столбцов белый визуализации:

```html
<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
</button>
```

Он также включает имя приложения, которое отображается в левом верхнем углу. Основное меню навигации, отображаемому вспомогательным методом `<ul>` сервисном второй элемент div и содержит ссылки на дома, около и обратитесь в службу. Ниже панели навигации основной части каждой страницы отображается в другом `<div>`, помеченная с классами «container» и «текст content». В простой значение по умолчанию \_файл макета, показанный здесь содержимое страницы к просмотру указанное представление, связанное со страницы, а также простую `<footer>` добавляется в конец `<div>` элемент. Вы увидите, как встроенный о странице отображается с помощью этого шаблона:

![О странице](bootstrap/_static/about-page-wide.png)

Свернута панель навигации, с помощью кнопки «гамбургер» в правом верхнем углу, открываемом окна опускается ниже определенную ширину:

![страница с меню "гамбургер"](bootstrap/_static/about-page-hamburger.png)

Щелкнув значок раскрывает пункты меню в ящике по вертикали, слайды вниз из верхней части страницы:

![страница с развернутое меню "гамбургер"](bootstrap/_static/about-page-hamburger-open.png)

### <a name="typography-and-links"></a>Элементы оформления и ссылки

Начальная загрузка устанавливает базовый оформления веб-узла, цвета и ссылки, форматирование в CSS-файл. Этот файл CSS содержит стили по умолчанию для таблиц, кнопки, элементы формы, изображения и многое другое ([Подробнее](http://getbootstrap.com/css/)). Один особенно полезная функция — это система макета сетки, описанные далее.

### <a name="grids"></a>Сетки

Одним из наиболее популярных возможностей платформы начальной загрузки является его система макета сетки. Современных веб-приложений следует избегать использования `<table>` тег для макета, вместо этого ограничение использования этого элемента для фактического табличных данных. Вместо этого, столбцов и строк можно компоновать с помощью серии `<div>` элементов и соответствующие классы CSS. Существует несколько преимуществ такого подхода, включая возможность настройки макета сетки для отображения по вертикали на узких экранах, например на телефонах.

[Система макета сетки платформы начальной загрузки](http://getbootstrap.com/css/#grid) основан на 12 столбцов. Это число было выбрано, так как его можно разделить поровну 1, 2, 3 или 4 столбца, а ширина столбцов может составлять до в 1/12-го по вертикали ширины экрана. Чтобы приступить к использованию система макета сетки, должно начинаться с контейнером `<div>` и затем добавьте строку `<div>`, как показано ниже:

```html
<div class="container">
    <div class="row">
        ...
    </div>
</div>
```

Добавьте дополнительные `<div>` элементы для каждого столбца и укажите количество столбцов, `<div>` должен занимать как часть класса CSS, начиная с «col - md-» (из 12). Например если вы хотите просто содержать два столбца одинакового размера, используется класс объекта «col-md-6» для каждого из них. В этом случае «md» — сокращение «средний» и ссылается на размера отображения стандартного размера настольного компьютера. Четыре различные параметры, которые можно выбрать, и каждой будет использоваться для выше ширины Если не переопределен (то есть если требуется макет, чтобы исправить независимо от ширины экрана, можно просто указать классы xs).

Префикс класс CSS | Уровень устройства | По ширине
:---: | :---: | :---:
col-xs - | Телефоны | < 768px
col-sm - | Планшетных ПК | > = 768px
col-md - | Рабочие столы | > = 992px
col-lg - | Экраны большого размера рабочего стола | > = 1200px

При указании два столбца обоих с «col-md-6» получившийся макет будет два столбца с разрешением рабочего стола, но эти столбцы будут расположены вертикально при подготовке к просмотру на небольших устройствах (или уже окно браузера на настольном компьютере), позволяя пользователям легко просматривать содержимое без необходимости выполнять прокрутку по горизонтали.

Начальной загрузки по умолчанию всегда создается макет одного столбца, поэтому необходимо указать столбцы, если необходимо, чтобы более одного столбца. Единственный случай, необходимо явно указать, что `<div>` длительности всех 12 столбцов было бы переопределить поведение большего уровня устройства. При указании нескольких классов уровня устройства, может потребоваться сбросить отображения столбцов в определенные моменты. Добавление div clearfix, отображается только в пределах определенных окно просмотра можно добиться этого, как показано ниже:

![Сетка просмотра узким и широким](bootstrap/_static/narrow-and-wide-viewport-grid.png)

В приведенном выше примере 1 и 2 совместно используют строку в формате «md», пока второй и третий совместного использования строк в макете «xs». Без clearfix `<div>`, второй и третий отображаются неправильно в представлении «xs» (Обратите внимание, что отображаются только один, 4 и 5):

![Сетка без использования clearfix](bootstrap/_static/grid-without-clearfix.png)

В этом примере только одну строку `<div>` использовалась, и начальной загрузки по-прежнему был главным образом правильные действия в отношении макета, а также расположения столбцов. Как правило, следует указать строку `<div>` для каждой строки по горизонтали требует настройки макета и безусловно можно вложить начальной загрузки таблицы друг в друга. При этом каждая вложенная таблица будет занимать 100% от ширины элемента, в котором она размещается, которой затем могут быть разделены с помощью классов столбцов.

### <a name="jumbotron"></a>Jumbotron

Если вы использовали шаблоны ASP.NET Core MVC по умолчанию в Visual Studio 2012 или 2013, можно заметить Jumbotron в действии. Он ссылается на большой раздел полной ширины страницы, который может использоваться для отображения большого фонового изображения, вызов действия, поворота или аналогичные элементы. Чтобы добавить jumbotron на страницу, просто добавьте `<div>` и присвойте ему класс объекта «jumbotron», а затем поместить контейнер `<div>` внутри и добавить свое содержимое. Можно легко настроить стандарте о страница, используемая для основные заголовки, которые в нем отображаются jumbotron:

![Пример jumbotron](bootstrap/_static/jumbotron.png)

### <a name="buttons"></a>Кнопки

На рисунке ниже показаны классы кнопку по умолчанию и их цвета.

![Тематические кнопок](bootstrap/_static/theme-buttons.png)

### <a name="badges"></a>Значки

Значки ссылаться на небольших, обычно числовые выноски рядом с элементом навигации. Они могут указать число сообщений или уведомлений о ожидающих или наличие обновлений. Указание такие значки является простым добавлением `<span>` , содержащую текст, с помощью класса «значок»:

![Тематические значки](bootstrap/_static/theme-badges.png)

### <a name="alerts"></a>Предупреждения

Может потребоваться отображать какого-либо рода уведомлений, предупреждение или сообщение об ошибке для пользователей вашего приложения. Вот где полезны стандартных классов предупреждений. Существует четыре различной степени серьезности со схемами связанный цвет:

![Тематические оповещения](bootstrap/_static/theme-alerts.png)

### <a name="navbars-and-menus"></a>Панели навигации и меню

Наш макет уже включает в себя стандартные переходов, но теме начальной загрузки поддерживает параметры дополнительными стилями. Мы также легко можно выбрать для отображения в области навигации по вертикали, а не по горизонтали Если, предпочтительный вариант, также, как добавление навигации вложенные элементы в всплывающее меню. Меню простой навигации, например вкладки ленты, строятся на основе `<ul>` элементов. Их можно создать очень просто, просто предоставляя им классы CSS «nav» и «nav вкладок»:

![Тематические со](bootstrap/_static/theme-tabstrips.png)

Панели навигации построена аналогичным образом, но немного сложнее. Они начинаются с `<nav>` или `<div>` с классом «navbar», в течение которого div контейнер содержит остальные элементы. Нашей странице включает navbar в ее заголовке уже, показанному ниже просто раскрывается на это, добавляя поддержку раскрывающегося меню:

![Тематические панели навигации](bootstrap/_static/theme-navbars.png)

### <a name="additional-elements"></a>Дополнительные элементы

Тема по умолчанию может также использоваться для представления таблиц HTML в стиле, отформатированное, включая поддержку чередующиеся представления. Существуют метки с использованием стилей, которые аналогичны шагам из кнопок. Можно создать пользовательские раскрывающиеся меню, которые поддерживают стандартный HTML, не ограничиваясь дополнительными стилями `<select>` элемента вместе с панели навигации, как в нашем начального сайта по умолчанию уже используется. Если вам нужна индикатор хода выполнения, существует несколько стилей, а также список групп и панели, включающие заголовок и содержимое. Дополнительные варианты в здесь стандартной темы начальной загрузки:

[http://getbootstrap.com/examples/theme/](http://getbootstrap.com/examples/theme/)

## <a name="more-themes"></a>Дополнительные темы

Вы можете расширить стандартный теме начальной загрузки путем переопределения некоторых или всех его CSS Настройка цвета и стили в соответствии с потребностями приложения. Если вы хотите начать с готовыми темы, существует несколько галереи темы, доступные через Интернет, специализирующихся в начальной загрузки темы, такие как WrapBootstrap.com (который имеет широкий набор коммерческих темы) и Bootswatch.com (который предлагает бесплатные темы). Некоторые из платных шаблонов, доступных предоставляют немало функций поверх основных начальной загрузки тему, например, широкую поддержку администрирования меню и панелей мониторинга с широкими возможностями диаграмм и датчиков. Примером популярных платных шаблонов является Inspinia, как показано на следующем снимке экрана:

![Пример темы inspinia](bootstrap/_static/theme-inspinia.png)

Если вы хотите изменить в теме начальной загрузки, поместите *bootstrap.css* файла для темы в **wwwroot/css** папку и измените все ссылки в *_Layout.cshtml* Чтобы указать. Изменение ссылок для всех сред:

```html
<environment names="Development">
    <link rel="stylesheet" href="~/css/bootstrap.css" />
```

```html
<environment names="Staging,Production">
    <link rel="stylesheet" href="~/css/bootstrap.min.css" />
```

Если вы хотите создавать собственные панели мониторинга, можно запустить из примера бесплатно доступны здесь: [ http://getbootstrap.com/examples/dashboard/ ](http://getbootstrap.com/examples/dashboard/).

## <a name="components"></a>Компоненты

Помимо этих элементов уже обсуждали, начальной загрузки включает в себя поддержку различных [встроенные компоненты пользовательского интерфейса](http://getbootstrap.com/components/).

### <a name="glyphicons"></a>Glyphicons сайте

Начальная загрузка включает в себя наборы значков из glyphicons сайте ([http://glyphicons.com](http://glyphicons.com)), с помощью более чем 200 значков, свободно доступен для использования в пределах начальной загрузки с поддержкой веб-приложения. Вот небольшой пример:

![Glyphicons сайте](bootstrap/_static/theme-glyphicons.png)

### <a name="input-groups"></a>Входной группы

Входной группы позволяют объединение дополнительный текст или кнопки с помощью элемента input, предоставляя ему его более понятным:

![входной группы](bootstrap/_static/input-groups.png)

### <a name="breadcrumbs"></a>Адресная строка

Деревья являются общий компонент пользовательского интерфейса, используемый для предоставления пользователю их истории или глубины в иерархии навигации по узлу. Легко добавлять их, применяя к любому классу «Навигатор» `<ol>` элемент списка. Включает встроенную поддержку разбиения на страницы с помощью класса «разбиение на страницы» на `<ul>` сервисном `<nav>`. Добавление быстрые внедренный показы слайдов и видео с помощью `<iframe>`, `<embed>`, `<video>`, или `<object>` элементы, которые автоматически будет стиля Bootstrap. Укажите определенный соотношение сторон, с помощью определенных классов, таких как «внедрение быстрые 16by9».

## <a name="javascript-support"></a>Поддержка JavaScript

Программе начальной загрузки библиотеки JavaScript поддерживает API включены компоненты, что позволяет управлять ими программным способом в приложении. Кроме того *bootstrap.js* включает более десяти подключаемых модулей пользовательского jQuery, предоставляя дополнительные функции, например переходы, модальные диалоговые окна, прокрутите обнаружения (обновление стилей, в зависимости от того, где пользователь прокрутил в документе), Свернуть поведение, карусели и меню, добавив в окно, поэтому они не прокрутить на экране. Недостаточно места для охвата всех надстроек JavaScript, встроенный в Bootstrap — Дополнительные сведения см. на [ http://getbootstrap.com/javascript/ ](http://getbootstrap.com/javascript/).

## <a name="summary"></a>Сводка

Начальная загрузка предоставляет веб-платформа, для которого можно быстро и эффективно разместить и разнообразные веб-сайтов и приложений в стиле. Его основные элементы оформления и стили предоставляют приятной внешнего вида и поведения, можно легко управлять с помощью поддержки пользовательские темы, который может быть созданный вручную или приобрести коммерческий доступ к. Он поддерживает целый ряд веб-компоненты, которые раньше потребовался дорогостоящие сторонние элементы управления, чтобы выполнить задачу, поддерживая современные и откройте веб-стандартов.
