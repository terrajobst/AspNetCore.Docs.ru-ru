---
title: Создание привлекательных, быстро реагирующих сайтов с начальной загрузки и ASP.NET Core
author: ardalis
description: Сведения об использовании начальной загрузки для разработки отвечать на запросы веб-приложений с ASP.NET Core.
manager: wpickett
ms.author: riande
ms.date: 10/14/2016
ms.prod: asp.net-core
ms.technology: aspnet
ms.topic: article
uid: client-side/bootstrap
ms.openlocfilehash: a11ed13c709830795ebfd0e658d3f2fd2fd5a458
ms.sourcegitcommit: c79fd3592f444d58e17518914f8873d0a11219c0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/18/2018
ms.locfileid: "31483715"
---
# <a name="build-beautiful-responsive-sites-with-bootstrap-and-aspnet-core"></a>Создание привлекательных, быстро реагирующих сайтов с начальной загрузки и ASP.NET Core

<a name="bootstrap-index"></a>

Автор: [Стив Смит](https://ardalis.com/) (Steve Smith)

Bootstrap сейчас наиболее популярных веб-платформа для разработки отвечать на запросы веб-приложений. Он предлагает ряд функций и преимуществ, которые могут упростить работу пользователей с веб-сайте ли вы новичок в переднего плана проектирования и разработки или экспертом. Bootstrap развертывается как набор файлов CSS и JavaScript и призван помочь масштаба веб-сайта или приложения эффективно от телефонов для планшетных ПК для настольных компьютеров.

## <a name="get-started"></a>Начало работы

Чтобы приступить к работе с начальной загрузки несколькими способами. Если вы начинаете новое веб-приложение в Visual Studio, можно выбрать шаблон начального уровня по умолчанию для ASP.NET Core, в котором вариантов начальной загрузки будет предустановлены.

![Начальная загрузка в представление решения шаблон начального уровня](bootstrap/_static/bootstrap-in-starter-template.png)

Добавление начальной загрузки в ASP.NET Core проекта — это просто добавляя его в *bower.json* как зависимость:

[!code-json[](../common/samples/WebApplication1/bower.json?highlight=5)]

Это рекомендуемый способ добавить в проект ASP.NET Core начальной загрузки.

Также можно установить с помощью одного из нескольких диспетчеров пакетов, например Bower, npm или NuGet начальной загрузки. В каждом случае процесс в основном одинаков:

### <a name="bower"></a>Bower

```console
bower install bootstrap
```

### <a name="npm"></a>npm

```console
npm install bootstrap
```

### <a name="nuget"></a>NuGet

```console
Install-Package bootstrap
```

> [!NOTE]
> Рекомендуется устанавливать клиентские зависимостей как начальной загрузки в ASP.NET Core можно с помощью Bower (с помощью *bower.json*, как показано выше). Чтобы продемонстрировать, как легко можно добавить начальной загрузки для других типов веб-приложений, включая более ранние версии ASP.NET показаны использование npm/NuGet.

Если ссылаетесь на свои собственные локальные версии начальной загрузки, необходимо ссылаться на них в все страницы, которые будут использовать его. В рабочей среде следует ссылаться начальной загрузки с помощью CDN. В шаблоне сайта ASP.NET по умолчанию *_Layout.cshtml* файл таким образом следующим образом:

[!code-html[](../common/samples/WebApplication1/Views/Shared/_Layout.cshtml?highlight=9,13,51,59)]

> [!NOTE]
> Если вы собираетесь использовать любой из подключаемых модулей начальной загрузки jQuery, также необходимо будет ссылаться на jQuery.

## <a name="basic-templates-and-features"></a>Основные шаблоны и компоненты

Самый простой начальной загрузки шаблона внешне похожи *_Layout.cshtml* файл приведенный выше, просто содержит основные меню навигации, а также место для подготовки к просмотру остальной части страницы.

### <a name="basic-navigation"></a>Основные средства перемещения

Шаблон по умолчанию используется набор `<div>` элементы, которые отображаются верхняя панель переходов и основной части страницы. Если вы используете HTML5, можно заменить первый `<div>` тег с `<nav>` тег, чтобы получить тот же эффект, но с более точно определить семантику. В первом `<div>` можно узнать, существует несколько других. Во-первых, `<div>` с классом «контейнер», а затем в нем две дополнительные `<div>` элементы: «панель навигации заголовок» и «панель навигации свернуть». Панель навигации заголовок div имеется кнопка, которая будет отображаться, когда экран ниже определенных минимальную ширину, показывающая три горизонтальные линии (так называемые значок «гамбургер»). Значок отображается с помощью чистый HTML и CSS; изображение не является обязательным. Это код, который отображается значок, с каждым из <span> теги, Подготовка к просмотру элементов белый диаграммы:

```html
<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
</button>
```

Она также включает имя приложения, которое отображается в верхнем левом углу. Основное меню навигации визуализируется `<ul>` элемент внутри второй div и содержит ссылки на главном около и обратитесь в службу. Ниже области навигации основной части каждой страницы отображается в другом `<div>`, помеченная классы «контейнер» и «содержимое текста». В файле _Layout простой по умолчанию, показанный здесь, содержимое страницы подготавливаются к просмотру с определенной представление, связанное с страницы и простой `<footer>` добавляется в конец `<div>` элемента. Вы увидите, как встроенные о странице отображается с помощью этого шаблона:

![О странице](bootstrap/_static/about-page-wide.png)

Свернутый панель навигации, с помощью кнопки «гамбургер» в верхнем правом углу, появляется, когда окно становится меньше определенную ширину:

![страница с меню гамбургер](bootstrap/_static/about-page-hamburger.png)

Если щелкнуть значок раскроется пункты меню в ящике вертикальной, соскальзывает из верхней части страницы:

![страница с меню гамбургер развернут.](bootstrap/_static/about-page-hamburger-open.png)

### <a name="typography-and-links"></a>Типографская разметка и ссылки

Bootstrap устанавливает основные оформления веб-узла, цвета и ссылки, форматирование в CSS-файл. CSS-файл включает стили по умолчанию для таблиц, кнопки, элементы формы, изображения и многое другое ([дополнительные](http://getbootstrap.com/css/)). Один особенно полезной функцией является система макета сетки, описанные далее.

### <a name="grids"></a>Таблицы

Одним из наиболее востребованных возможностей начальной загрузки является его система макета сетки. Современных веб-приложений следует избегать использования `<table>` тег макет, вместо ограничения использовать этот элемент к фактическое табличных данных. Вместо этого столбцов и строк могут размещаться с помощью ряда `<div>` элементов и соответствующие классы CSS. Существует несколько преимуществ такого подхода в том числе возможность настройки макета сетки для отображения по вертикали на узких экранах, такие как на телефонах.

[Система макета сетки начальной загрузки](http://getbootstrap.com/css/#grid) основана на двенадцати столбцов. Это число было выбрано, так как он может быть разделен равномерно 1, 2, 3 или 4 столбцами, а ширина столбцов может варьироваться для в рамках 1/12-го вертикальной ширины экрана. Чтобы начать использовать систему макета сетки, должно начинаться с контейнером `<div>` и затем добавить строку `<div>`, как показано ниже:

```html
<div class="container">
    <div class="row">
        ...
    </div>
</div>
```

Добавьте дополнительные `<div>` элементы для каждого столбца и укажите количество столбцов, `<div>` должен занимать в рамках класса CSS, начиная с «col - md-» (из 12). Для экземпляра Если вы хотите просто содержать два столбца одинакового размера, используется класс «col-md-6» для каждого из них. В этом случае «md» — сокращение для «средний» и ссылается на размеры стандартного размера настольного компьютера. Имеются четыре различные параметры, выбранные из и каждой будет использоваться для ширины выше Если не переопределен (то есть если требуется макет будет исправлено независимо от ширины экрана, можно просто указать xs классы).

Префикс класс CSS | Уровня устройства | По ширине
:---: | :---: | :---:
col-xs - | Телефоны | < 768px
col-sm - | Планшетные ПК | > = 768px
col-md - | Настольные компьютеры | > = 992px
col-lg - | Экраны большого размера рабочего стола | > = 1200px

При указании два столбца, как «col-md-6» получившийся макет будет два столбца с разрешением рабочего стола, но эти столбцы будут расположены вертикально, при отображении на небольших устройствах (или уже окно браузера на рабочем столе), позволяя пользователям легко просматривать содержимое без необходимости выполнять прокрутку по горизонтали.

Начальной загрузки по умолчанию всегда создается макет одного столбца, поэтому необходимо указать столбцы, если требуется более одного столбца. Единственный случай, необходимо явно указать, что `<div>` длительности всех 12 столбцов, можно переопределить поведение большего уровня устройства. При указании нескольких классов уровня устройства, может потребоваться выполнить сброс отображения столбцов в некоторых точках. Добавление div clearfix, которая отображается только в определенные области просмотра можно добиться этого, как показано ниже:

![Сетка обычных, так и ширину окна просмотра](bootstrap/_static/narrow-and-wide-viewport-grid.png)

В приведенном выше примере один и два совместно использовать строку в формате «md», пока вторая и третья совместно используют строку в формате «xs». Без clearfix `<div>`, 2 и 3 отображаются неправильно в представлении «xs» (Обратите внимание, что отображаются только один, 4 и 5):

![Сетка, без использования clearfix](bootstrap/_static/grid-without-clearfix.png)

В этом примере только одну строку `<div>` был использован, и практически начальной загрузки по-прежнему правильно части макета и расположения столбцов. Как правило, следует указать строку `<div>` для каждой горизонтальной строки требует настройки макета и конечно можно вложить начальной загрузки таблицы друг в друга. При этом каждая вложенная таблица будет занимать 100% от ширины элемента, в котором она размещена, который затем может быть разделена с помощью классов столбцов.

### <a name="jumbotron"></a>Jumbotron

При использовании шаблонов ASP.NET MVC по умолчанию в Visual Studio 2012 или 2013, возможно, вы сталкивались Jumbotron в действии. Он ссылается на большой раздел с полной шириной страницы, который может использоваться для отображения большого фонового изображения, вызов действия, поворота или одинаковые элементы. Чтобы добавить на страницу jumbotron, просто добавьте `<div>` и присвойте ему класса «jumbotron», а затем поместить контейнер `<div>` внутри и добавление содержимого. Мы можете легко настроить стандартный о страница, используемая для основные заголовки, которые в нем отображаются jumbotron:

![Пример jumbotron](bootstrap/_static/jumbotron.png)

### <a name="buttons"></a>Кнопки

На рисунке ниже показаны классы кнопку по умолчанию и их цвета.

![Тематические кнопок](bootstrap/_static/theme-buttons.png)

### <a name="badges"></a>Эмблемы

Значки ссылаться на маленькие, обычно числовые выноски рядом с элементом навигации. Они могут указывать на количество сообщений или уведомлений о, ожидающих или наличие обновлений. Указание таких эмблемы является простым добавлением `<span>` содержащую текст, с классом «badge»:

![Тематические эмблемы](bootstrap/_static/theme-badges.png)

### <a name="alerts"></a>Предупреждения

Может потребоваться отображать каким-либо уведомления, предупреждение или сообщение об ошибке для пользователей приложения. Это происходит, где полезны стандартные классы оповещений. Существует четыре различные уровни важности с связанного цветовые схемы.

![Тематические оповещения](bootstrap/_static/theme-alerts.png)

### <a name="navbars-and-menus"></a>Меню и панели навигации

Наш макет уже содержит стандартные переходов, но начальной загрузки темы поддерживает параметры дополнительных стилей. Мы также легко можно выбрать для отображения панели переходов по вертикали, а не по горизонтали Если, основной, навигации, а также добавление вложенные элементы в во всплывающем меню. Меню простой навигации, например вкладки ленты, построены на основе `<ul>` элементов. Эти компоненты можно создать очень просто, просто предоставляя им классов CSS «nav» и «nav вкладки»:

![Тематические tabstrips](bootstrap/_static/theme-tabstrips.png)

Панели навигации построены аналогичным образом, но являются более сложными. Они начинаются с `<nav>` или `<div>` с классом «navbar», в течение которого div контейнер содержит остальные элементы. Страницу, где есть панель навигации в ее заголовке уже — следующей просто расширяется на это, добавляя поддержку раскрывающееся меню:

![Тематические панели навигации](bootstrap/_static/theme-navbars.png)

### <a name="additional-elements"></a>Дополнительные элементы

Тема по умолчанию может также использоваться для представления таблиц HTML в стиле, отформатированное, включая поддержку чередующиеся представления. Существуют метки со стилями, которые аналогичны кнопок. Можно создать пользовательские раскрывающиеся меню, которые поддерживают дополнительные стили, не ограничиваясь стандартный HTML `<select>` элемент вместе с панели навигации, подобную нашей начального сайта по умолчанию уже используется. Если вам требуется индикатор хода выполнения, существует несколько стилей, а также список групп и панелей, которые включают заголовок и содержимое. Просмотр дополнительных параметров в пределах здесь стандартную тему начальной загрузки:

[http://getbootstrap.com/examples/theme/](http://getbootstrap.com/examples/theme/)

## <a name="more-themes"></a>Дополнительные темы

Можно расширить стандартную тему начальной загрузки путем переопределения некоторых или всех его CSS Настройка цветов и стилей, в соответствии с потребностями приложения. Если вы хотите запустить из готовых темы, существует несколько галереи темы, доступные через Интернет, специализирующихся на начальной загрузки темы, например WrapBootstrap.com (который содержит разнообразные темы коммерческих) и Bootswatch.com (которая предлагает бесплатные темы). Некоторые платные доступные шаблоны предоставляют механизмы функциональности над упрощенный стиль начальной загрузки, например широкую поддержку администрирования меню и панели мониторинга с форматированным диаграмм и датчиков. Пример шаблона популярные платная — Inspinia, в настоящее время для продажи для $18, включая шаблон ASP.NET MVC5 помимо AngularJS и статических HTML-версий. Ниже приведен пример снимка экрана.

![Пример inspinia темы](bootstrap/_static/theme-inspinia.png)

Если вы хотите изменить начальной загрузки темы, поместите *bootstrap.css* файла для темы в **wwwroot/css** папки и измените все ссылки в *_Layout.cshtml* Чтобы указать. Изменение ссылок для всех сред:

```html
<environment names="Development">
    <link rel="stylesheet" href="~/css/bootstrap.css" />
```

```html
<environment names="Staging,Production">
    <link rel="stylesheet" href="~/css/bootstrap.min.css" />
```

Если вы хотите создавать свою собственную панель мониторинга, можно запустить из свободного примера доступен здесь: [ http://getbootstrap.com/examples/dashboard/ ](http://getbootstrap.com/examples/dashboard/).

## <a name="components"></a>Компоненты

Помимо этих элементов уже говорили, начальной загрузки включает поддержку разнообразных [встроенные компоненты пользовательского интерфейса](http://getbootstrap.com/components/).

### <a name="glyphicons"></a>Glyphicons

Начальной загрузки включает в себя наборы значков из Glyphicons ([http://glyphicons.com](http://glyphicons.com)), с более чем 200 значки, доступной для использования в пределах начальной загрузки веб-приложения. Вот небольшой пример.

![Glyphicons](bootstrap/_static/theme-glyphicons.png)

### <a name="input-groups"></a>входной группы

Входной группы позволяют объединение дополнительный текст или кнопки с элемент input, предоставляя пользователю интуитивно навыки:

![входной группы](bootstrap/_static/input-groups.png)

### <a name="breadcrumbs"></a>Адресная строка

Адресная строка — это общий компонент пользовательского интерфейса используется для предоставления пользователю их истории или глубина в иерархии навигации по сайту. Добавить их легко путем применения класса «навигатора» на любой `<ol>` элемента списка. Включает встроенную поддержку разбиения на страницы с помощью класса «разбиение на страницы» на `<ul>` элемент в пределах `<nav>`. Добавление отвечать на запросы внедренных слайд-шоу и видео с помощью `<iframe>`, `<embed>`, `<video>`, или `<object>` элементы, которые автоматически будут стиля начальной загрузки. Укажите определенный соотношение сторон с помощью определенных классов, таких как «внедрить-отвечать на запросы 16by9».

## <a name="javascript-support"></a>Поддержка JavaScript

Библиотека JavaScript для начальной загрузки в поддерживает API включенные компоненты, что позволяет управлять ими программным способом в приложении. Кроме того *bootstrap.js* включает более десяти подключаемых модулей пользовательских jQuery, предоставляя дополнительные функции, например переходы, модальные диалоговые окна, прокручивать обнаружение (обновлением стилей, в зависимости от того, когда пользователь прокручивает в документе), Свернуть поведение, лент и привязке меню для окна, поэтому они не прокручиваются за пределы экрана. Не достаточно места, чтобы охватить все надстройки JavaScript, встроенные в начальной загрузки, чтобы узнать больше, посетите [ http://getbootstrap.com/javascript/ ](http://getbootstrap.com/javascript/).

## <a name="summary"></a>Сводка

Bootstrap предоставляет веб-платформа, которая может использоваться для быстрого и эффективного макета и стиля разнообразных веб-сайтов и приложений. Ее основные элементы оформления и стили обеспечивают удобную внешнего вида, легко могут быть подвергнуты поддержку пользовательские темы, что можно вручную создавать или приобретенных коммерчески. Он поддерживает узла веб-компонентов, которые в прошлом потребовался дорогостоящие сторонние элементы управления для выполнения, одновременно поддерживая современных и откройте веб-стандарты.
