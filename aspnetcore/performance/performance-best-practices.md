---
title: ASP.NET основные рекомендации по производительности
author: mjrousos
description: Советы по повышению производительности в ASP.NET приложений Core и предотвращению общих проблем с производительностью.
monikerRange: '>= aspnetcore-2.1'
ms.author: riande
ms.date: 04/06/2020
no-loc:
- SignalR
uid: performance/performance-best-practices
ms.openlocfilehash: 068a35fbe410dad24030fe68c0dfd062b402212c
ms.sourcegitcommit: f0aeeab6ab6e09db713bb9b7862c45f4d447771b
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/08/2020
ms.locfileid: "80977188"
---
# <a name="aspnet-core-performance-best-practices"></a><span data-ttu-id="fddab-103">ASP.NET основные рекомендации по производительности</span><span class="sxs-lookup"><span data-stu-id="fddab-103">ASP.NET Core Performance Best Practices</span></span>

<span data-ttu-id="fddab-104">Автор: [Майк Роусос (Mike Rousos)](https://github.com/mjrousos)</span><span class="sxs-lookup"><span data-stu-id="fddab-104">By [Mike Rousos](https://github.com/mjrousos)</span></span>

<span data-ttu-id="fddab-105">В этой статье содержатся рекомендации по наилучшей производительности с ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="fddab-105">This article provides guidelines for performance best practices with ASP.NET Core.</span></span>

## <a name="cache-aggressively"></a><span data-ttu-id="fddab-106">Кэш агрессивно</span><span class="sxs-lookup"><span data-stu-id="fddab-106">Cache aggressively</span></span>

<span data-ttu-id="fddab-107">Кэшинг обсуждается в нескольких частях этого документа.</span><span class="sxs-lookup"><span data-stu-id="fddab-107">Caching is discussed in several parts of this document.</span></span> <span data-ttu-id="fddab-108">Для получения дополнительной информации см. <xref:performance/caching/response>.</span><span class="sxs-lookup"><span data-stu-id="fddab-108">For more information, see <xref:performance/caching/response>.</span></span>

## <a name="understand-hot-code-paths"></a><span data-ttu-id="fddab-109">Понимание путей горячего кода</span><span class="sxs-lookup"><span data-stu-id="fddab-109">Understand hot code paths</span></span>

<span data-ttu-id="fddab-110">В этом документе *путь горячего кода* определяется как путь кода, который часто вызывается и где происходит большая часть времени выполнения.</span><span class="sxs-lookup"><span data-stu-id="fddab-110">In this document, a *hot code path* is defined as a code path that is frequently called and where much of the execution time occurs.</span></span> <span data-ttu-id="fddab-111">Горячие пути кода обычно ограничивают масштаб и производительность приложения и обсуждаются в нескольких частях этого документа.</span><span class="sxs-lookup"><span data-stu-id="fddab-111">Hot code paths typically limit app scale-out and performance and are discussed in several parts of this document.</span></span>

## <a name="avoid-blocking-calls"></a><span data-ttu-id="fddab-112">Избегайте блокирования вызовов</span><span class="sxs-lookup"><span data-stu-id="fddab-112">Avoid blocking calls</span></span>

<span data-ttu-id="fddab-113">ASP.NET основные приложения должны быть разработаны для обработки многих запросов одновременно.</span><span class="sxs-lookup"><span data-stu-id="fddab-113">ASP.NET Core apps should be designed to process many requests simultaneously.</span></span> <span data-ttu-id="fddab-114">Асинхронные AIS позволяют небольшому пулу потоков обрабатывать тысячи одновременных запросов, не дожидаясь блокировки вызовов.</span><span class="sxs-lookup"><span data-stu-id="fddab-114">Asynchronous APIs allow a small pool of threads to handle thousands of concurrent requests by not waiting on blocking calls.</span></span> <span data-ttu-id="fddab-115">Вместо того, чтобы ждать выполнения длительной синхронной задачи, поток может работать по другому запросу.</span><span class="sxs-lookup"><span data-stu-id="fddab-115">Rather than waiting on a long-running synchronous task to complete, the thread can work on another request.</span></span>

<span data-ttu-id="fddab-116">Общей проблемой производительности в приложениях ASP.NET Core является блокирование вызовов, которые могут быть асинхронными.</span><span class="sxs-lookup"><span data-stu-id="fddab-116">A common performance problem in ASP.NET Core apps is blocking calls that could be asynchronous.</span></span> <span data-ttu-id="fddab-117">Многие синхронные блокировки вызовов приводят к [голоду потока пула](https://blogs.msdn.microsoft.com/vancem/2018/10/16/diagnosing-net-core-threadpool-starvation-with-perfview-why-my-service-is-not-saturating-all-cores-or-seems-to-stall/) и деградированному времени отклика.</span><span class="sxs-lookup"><span data-stu-id="fddab-117">Many synchronous blocking calls lead to [Thread Pool starvation](https://blogs.msdn.microsoft.com/vancem/2018/10/16/diagnosing-net-core-threadpool-starvation-with-perfview-why-my-service-is-not-saturating-all-cores-or-seems-to-stall/) and degraded response times.</span></span>

<span data-ttu-id="fddab-118">**Не:**</span><span class="sxs-lookup"><span data-stu-id="fddab-118">**Do not**:</span></span>

* <span data-ttu-id="fddab-119">Блокируйте асинхронное исполнение, позвонив [в Task.Wait](/dotnet/api/system.threading.tasks.task.wait) или [Task.Result](/dotnet/api/system.threading.tasks.task-1.result).</span><span class="sxs-lookup"><span data-stu-id="fddab-119">Block asynchronous execution by calling [Task.Wait](/dotnet/api/system.threading.tasks.task.wait) or [Task.Result](/dotnet/api/system.threading.tasks.task-1.result).</span></span>
* <span data-ttu-id="fddab-120">Приобрести блокировки в общих путей кода.</span><span class="sxs-lookup"><span data-stu-id="fddab-120">Acquire locks in common code paths.</span></span> <span data-ttu-id="fddab-121">ASP.NET приложения Core являются наиболее performant, когда архитектор для запуска кода параллельно.</span><span class="sxs-lookup"><span data-stu-id="fddab-121">ASP.NET Core apps are most performant when architected to run code in parallel.</span></span>
* <span data-ttu-id="fddab-122">Позвоните [В Task.Run](/dotnet/api/system.threading.tasks.task.run) и сразу же дождитесь его.</span><span class="sxs-lookup"><span data-stu-id="fddab-122">Call [Task.Run](/dotnet/api/system.threading.tasks.task.run) and immediately await it.</span></span> <span data-ttu-id="fddab-123">ASP.NET Core уже запускает код приложения на обычных потоках потока потока, поэтому вызов Task.Run приводит только к дополнительному ненужному планированию потока пула.</span><span class="sxs-lookup"><span data-stu-id="fddab-123">ASP.NET Core already runs app code on normal Thread Pool threads, so calling Task.Run only results in extra unnecessary Thread Pool scheduling.</span></span> <span data-ttu-id="fddab-124">Даже если запланированный код блокирует поток, Task.Run не препятствует этому.</span><span class="sxs-lookup"><span data-stu-id="fddab-124">Even if the scheduled code would block a thread, Task.Run does not prevent that.</span></span>

<span data-ttu-id="fddab-125">**У:**</span><span class="sxs-lookup"><span data-stu-id="fddab-125">**Do**:</span></span>

* <span data-ttu-id="fddab-126">Сделайте [горячие пути кода](#understand-hot-code-paths) асинхронными.</span><span class="sxs-lookup"><span data-stu-id="fddab-126">Make [hot code paths](#understand-hot-code-paths) asynchronous.</span></span>
* <span data-ttu-id="fddab-127">Доступ к данным вызова, ввод/во-пасси и длительные операции API асинхронно при наличии асинхронного API.</span><span class="sxs-lookup"><span data-stu-id="fddab-127">Call data access, I/O, and long-running operations APIs asynchronously if an asynchronous API is available.</span></span> <span data-ttu-id="fddab-128">Не **not** используйте [Task.Run](/dotnet/api/system.threading.tasks.task.run) для синхронизации API асинхронным.</span><span class="sxs-lookup"><span data-stu-id="fddab-128">Do **not** use [Task.Run](/dotnet/api/system.threading.tasks.task.run) to make a synchronus API asynchronous.</span></span>
* <span data-ttu-id="fddab-129">Сделать действия контроллера/страницы Razor асинхронными.</span><span class="sxs-lookup"><span data-stu-id="fddab-129">Make controller/Razor Page actions asynchronous.</span></span> <span data-ttu-id="fddab-130">Весь стек вызовов асинхронный, чтобы извлечь выгоду из шаблонов [async/await.](/dotnet/csharp/programming-guide/concepts/async/)</span><span class="sxs-lookup"><span data-stu-id="fddab-130">The entire call stack is asynchronous in order to benefit from [async/await](/dotnet/csharp/programming-guide/concepts/async/) patterns.</span></span>

<span data-ttu-id="fddab-131">Профайлер, например [PerfView,](https://github.com/Microsoft/perfview)может использоваться для поиска потоков, часто добавляемых в [пул потоков.](/windows/desktop/procthread/thread-pools)</span><span class="sxs-lookup"><span data-stu-id="fddab-131">A profiler, such as [PerfView](https://github.com/Microsoft/perfview), can be used to find threads frequently added to the [Thread Pool](/windows/desktop/procthread/thread-pools).</span></span> <span data-ttu-id="fddab-132">Событие `Microsoft-Windows-DotNETRuntime/ThreadPoolWorkerThread/Start` указывает на поток, добавленный в пул потока.</span><span class="sxs-lookup"><span data-stu-id="fddab-132">The `Microsoft-Windows-DotNETRuntime/ThreadPoolWorkerThread/Start` event indicates a thread added to the thread pool.</span></span> <!--  For more information, see [async guidance docs](TBD-Link_To_Davifowl_Doc)  -->

## <a name="minimize-large-object-allocations"></a><span data-ttu-id="fddab-133">Минимизация выделения крупных объектов</span><span class="sxs-lookup"><span data-stu-id="fddab-133">Minimize large object allocations</span></span>

<span data-ttu-id="fddab-134">[Сборщик мусора .NET Core](/dotnet/standard/garbage-collection/) управляет распределением и выпуском памяти автоматически в ASP.NET приложений Core.</span><span class="sxs-lookup"><span data-stu-id="fddab-134">The [.NET Core garbage collector](/dotnet/standard/garbage-collection/) manages allocation and release of memory automatically in ASP.NET Core apps.</span></span> <span data-ttu-id="fddab-135">Автоматический сбор мусора обычно означает, что разработчикам не нужно беспокоиться о том, как и когда память освобождается.</span><span class="sxs-lookup"><span data-stu-id="fddab-135">Automatic garbage collection generally means that developers don't need to worry about how or when memory is freed.</span></span> <span data-ttu-id="fddab-136">Однако очистка неотсываемых объектов занимает время процессора, поэтому разработчики должны свести к минимуму выделение объектов в [горячих путях кода.](#understand-hot-code-paths)</span><span class="sxs-lookup"><span data-stu-id="fddab-136">However, cleaning up unreferenced objects takes CPU time, so developers should minimize allocating objects in [hot code paths](#understand-hot-code-paths).</span></span> <span data-ttu-id="fddab-137">Сбор мусора особенно дорого стоит на крупных объектах (> 85 Байт).</span><span class="sxs-lookup"><span data-stu-id="fddab-137">Garbage collection is especially expensive on large objects (> 85 K bytes).</span></span> <span data-ttu-id="fddab-138">Крупные объекты хранятся на [большой объектной кучи](/dotnet/standard/garbage-collection/large-object-heap) и требуют полного (поколения 2) сбора мусора для очистки.</span><span class="sxs-lookup"><span data-stu-id="fddab-138">Large objects are stored on the [large object heap](/dotnet/standard/garbage-collection/large-object-heap) and require a full (generation 2) garbage collection to clean up.</span></span> <span data-ttu-id="fddab-139">В отличие от коллекций поколения 0 и поколения 1, коллекция поколения 2 требует временной приостановки исполнения приложения.</span><span class="sxs-lookup"><span data-stu-id="fddab-139">Unlike generation 0 and generation 1 collections, a generation 2 collection requires a temporary suspension of app execution.</span></span> <span data-ttu-id="fddab-140">Частое распределение и де-распределение крупных объектов может привести к несогласованности производительности.</span><span class="sxs-lookup"><span data-stu-id="fddab-140">Frequent allocation and de-allocation of large objects can cause inconsistent performance.</span></span>

<span data-ttu-id="fddab-141">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="fddab-141">Recommendations:</span></span>

* <span data-ttu-id="fddab-142">**Рассмотрите** возможность кэширования крупных объектов, которые часто используются.</span><span class="sxs-lookup"><span data-stu-id="fddab-142">**Do** consider caching large objects that are frequently used.</span></span> <span data-ttu-id="fddab-143">Кэширование больших объектов предотвращает дорогостоящие выделения.</span><span class="sxs-lookup"><span data-stu-id="fddab-143">Caching large objects prevents expensive allocations.</span></span>
* <span data-ttu-id="fddab-144">**Сделайте** буферы пула с помощью [>\<ArrayPool T](/dotnet/api/system.buffers.arraypool-1) для хранения больших массивов.</span><span class="sxs-lookup"><span data-stu-id="fddab-144">**Do** pool buffers by using an [ArrayPool\<T>](/dotnet/api/system.buffers.arraypool-1) to store large arrays.</span></span>
* <span data-ttu-id="fddab-145">**Не** выделяйте много, недолго крупных объектов на [пути горячего кода.](#understand-hot-code-paths)</span><span class="sxs-lookup"><span data-stu-id="fddab-145">**Do not** allocate many, short-lived large objects on [hot code paths](#understand-hot-code-paths).</span></span>

<span data-ttu-id="fddab-146">Проблемы с памятью, такие как предыдущая, можно диагностировать, просмотрев статистику сбора мусора (GC) в [PerfView](https://github.com/Microsoft/perfview) и изучив:</span><span class="sxs-lookup"><span data-stu-id="fddab-146">Memory issues, such as the preceding, can be diagnosed by reviewing garbage collection (GC) stats in [PerfView](https://github.com/Microsoft/perfview) and examining:</span></span>

* <span data-ttu-id="fddab-147">Время сбора мусора.</span><span class="sxs-lookup"><span data-stu-id="fddab-147">Garbage collection pause time.</span></span>
* <span data-ttu-id="fddab-148">Какой процент времени процессора тратится на сбор мусора.</span><span class="sxs-lookup"><span data-stu-id="fddab-148">What percentage of the processor time is spent in garbage collection.</span></span>
* <span data-ttu-id="fddab-149">Сколько коллекций мусора составляют поколения 0, 1 и 2.</span><span class="sxs-lookup"><span data-stu-id="fddab-149">How many garbage collections are generation 0, 1, and 2.</span></span>

<span data-ttu-id="fddab-150">Для получения дополнительной информации [см.](/dotnet/standard/garbage-collection/performance)</span><span class="sxs-lookup"><span data-stu-id="fddab-150">For more information, see [Garbage Collection and Performance](/dotnet/standard/garbage-collection/performance).</span></span>

## <a name="optimize-data-access-and-io"></a><span data-ttu-id="fddab-151">Оптимизация доступа к данным и ввоза/o</span><span class="sxs-lookup"><span data-stu-id="fddab-151">Optimize data access and I/O</span></span>

<span data-ttu-id="fddab-152">Взаимодействия с хранилищем данных и другими удаленными службами часто являются самыми медленными частями приложения ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="fddab-152">Interactions with a data store and other remote services are often the slowest parts of an ASP.NET Core app.</span></span> <span data-ttu-id="fddab-153">Эффективное чтение и написание данных имеет решающее значение для хорошей производительности.</span><span class="sxs-lookup"><span data-stu-id="fddab-153">Reading and writing data efficiently is critical for good performance.</span></span>

<span data-ttu-id="fddab-154">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="fddab-154">Recommendations:</span></span>

* <span data-ttu-id="fddab-155">**Вызов** все данные доступа AIS асинхронно.</span><span class="sxs-lookup"><span data-stu-id="fddab-155">**Do** call all data access APIs asynchronously.</span></span>
* <span data-ttu-id="fddab-156">**Не извлекайте** больше данных, чем это необходимо.</span><span class="sxs-lookup"><span data-stu-id="fddab-156">**Do not** retrieve more data than is necessary.</span></span> <span data-ttu-id="fddab-157">Напишите запросы, чтобы вернуть только данные, необходимые для текущего запроса HTTP.</span><span class="sxs-lookup"><span data-stu-id="fddab-157">Write queries to return just the data that's necessary for the current HTTP request.</span></span>
* <span data-ttu-id="fddab-158">**Рассмотрите** возможность кэширования часто доступных данных, извлеченных из базы данных или удаленной службы, если немного устаревшие данные являются приемлемыми.</span><span class="sxs-lookup"><span data-stu-id="fddab-158">**Do** consider caching frequently accessed data retrieved from a database or remote service if slightly out-of-date data is acceptable.</span></span> <span data-ttu-id="fddab-159">В зависимости от сценария используйте [MemoryCache](xref:performance/caching/memory) или [DistributedCache.](xref:performance/caching/distributed)</span><span class="sxs-lookup"><span data-stu-id="fddab-159">Depending on the scenario, use a [MemoryCache](xref:performance/caching/memory) or a [DistributedCache](xref:performance/caching/distributed).</span></span> <span data-ttu-id="fddab-160">Для получения дополнительной информации см. <xref:performance/caching/response>.</span><span class="sxs-lookup"><span data-stu-id="fddab-160">For more information, see <xref:performance/caching/response>.</span></span>
* <span data-ttu-id="fddab-161">**Сделайте** минимизацию сетевых поездок.</span><span class="sxs-lookup"><span data-stu-id="fddab-161">**Do** minimize network round trips.</span></span> <span data-ttu-id="fddab-162">Цель состоит в том, чтобы получить необходимые данные в одном вызове, а не несколько вызовов.</span><span class="sxs-lookup"><span data-stu-id="fddab-162">The goal is to retrieve the required data in a single call rather than several calls.</span></span>
* <span data-ttu-id="fddab-163">**Используйте** [запросы без отслеживания](/ef/core/querying/tracking#no-tracking-queries) в Core Entity Framework При доступе к данным только для целей чтения.</span><span class="sxs-lookup"><span data-stu-id="fddab-163">**Do** use [no-tracking queries](/ef/core/querying/tracking#no-tracking-queries) in Entity Framework Core when accessing data for read-only purposes.</span></span> <span data-ttu-id="fddab-164">EF Core может более эффективно возвращать результаты запросов без отслеживания.</span><span class="sxs-lookup"><span data-stu-id="fddab-164">EF Core can return the results of no-tracking queries more efficiently.</span></span>
* <span data-ttu-id="fddab-165">**Фильтруйте** и агрегируете `.Select`запросы LIN '(например, с `.Sum` `.Where`операторами или операторами), чтобы фильтрация выполнялась базой данных.</span><span class="sxs-lookup"><span data-stu-id="fddab-165">**Do** filter and aggregate LINQ queries (with `.Where`, `.Select`, or `.Sum` statements, for example) so that the filtering is performed by the database.</span></span>
* <span data-ttu-id="fddab-166">**Учтите,** что EF Core разрешает некоторые операторы запросов на клиенте, что может привести к неэффективному исполнению запроса.</span><span class="sxs-lookup"><span data-stu-id="fddab-166">**Do** consider that EF Core resolves some query operators on the client, which may lead to inefficient query execution.</span></span> <span data-ttu-id="fddab-167">Для получения дополнительной [Client evaluation performance issues](/ef/core/querying/client-eval#client-evaluation-performance-issues)информации см.</span><span class="sxs-lookup"><span data-stu-id="fddab-167">For more information, see [Client evaluation performance issues](/ef/core/querying/client-eval#client-evaluation-performance-issues).</span></span>
* <span data-ttu-id="fddab-168">**Не** используйте проекционные запросы на собрания, что может привести к выполнения запросов «N и 1» S'L.</span><span class="sxs-lookup"><span data-stu-id="fddab-168">**Do not** use projection queries on collections, which can result in executing "N + 1" SQL queries.</span></span> <span data-ttu-id="fddab-169">Для получения дополнительной информации [см.](/ef/core/what-is-new/ef-core-2.1#optimization-of-correlated-subqueries)</span><span class="sxs-lookup"><span data-stu-id="fddab-169">For more information, see [Optimization of correlated subqueries](/ef/core/what-is-new/ef-core-2.1#optimization-of-correlated-subqueries).</span></span>

<span data-ttu-id="fddab-170">[См. подходы EF High Performance,](/ef/core/what-is-new/ef-core-2.0#explicitly-compiled-queries) которые могут повысить производительность в высококлассных приложениях:</span><span class="sxs-lookup"><span data-stu-id="fddab-170">See [EF High Performance](/ef/core/what-is-new/ef-core-2.0#explicitly-compiled-queries) for approaches that may improve performance in high-scale apps:</span></span>

* [<span data-ttu-id="fddab-171">Создание пулов DbContext</span><span class="sxs-lookup"><span data-stu-id="fddab-171">DbContext pooling</span></span>](/ef/core/what-is-new/ef-core-2.0#dbcontext-pooling)
* [<span data-ttu-id="fddab-172">Явным образом скомпилированные запросы</span><span class="sxs-lookup"><span data-stu-id="fddab-172">Explicitly compiled queries</span></span>](/ef/core/what-is-new/ef-core-2.0#explicitly-compiled-queries)

<span data-ttu-id="fddab-173">Мы рекомендуем измерить влияние предыдущих высокопроизводительных подходов перед фиксацией базы кода.</span><span class="sxs-lookup"><span data-stu-id="fddab-173">We recommend measuring the impact of the preceding high-performance approaches before committing the code base.</span></span> <span data-ttu-id="fddab-174">Дополнительная сложность компилированных запросов может не оправдать повышение производительности.</span><span class="sxs-lookup"><span data-stu-id="fddab-174">The additional complexity of compiled queries may not justify the performance improvement.</span></span>

<span data-ttu-id="fddab-175">Проблемы с запросами можно обнаружить, проанализировав время, затраченное на доступ к данным с помощью [Application Insights](/azure/application-insights/app-insights-overview) или с помощью инструментов профилирования.</span><span class="sxs-lookup"><span data-stu-id="fddab-175">Query issues can be detected by reviewing the time spent accessing data with [Application Insights](/azure/application-insights/app-insights-overview) or with profiling tools.</span></span> <span data-ttu-id="fddab-176">Большинство баз данных также предоставляют статистические данные по часто выполняемым запросам.</span><span class="sxs-lookup"><span data-stu-id="fddab-176">Most databases also make statistics available concerning frequently executed queries.</span></span>

## <a name="pool-http-connections-with-httpclientfactory"></a><span data-ttu-id="fddab-177">Соединение пула HTTP с httpClientfactory</span><span class="sxs-lookup"><span data-stu-id="fddab-177">Pool HTTP connections with HttpClientFactory</span></span>

<span data-ttu-id="fddab-178">Хотя [HttpClient](/dotnet/api/system.net.http.httpclient) реализует `IDisposable` интерфейс, он предназначен для повторного использования.</span><span class="sxs-lookup"><span data-stu-id="fddab-178">Although [HttpClient](/dotnet/api/system.net.http.httpclient) implements the `IDisposable` interface, it's designed for reuse.</span></span> <span data-ttu-id="fddab-179">Закрытые `HttpClient` экземпляры оставляют розетки открытыми в состоянии `TIME_WAIT` в течение короткого периода времени.</span><span class="sxs-lookup"><span data-stu-id="fddab-179">Closed `HttpClient` instances leave sockets open in the `TIME_WAIT` state for a short period of time.</span></span> <span data-ttu-id="fddab-180">Если часто используется путь кода, `HttpClient` который создает и утилизирует объекты, приложение может исчерпать доступные розетки.</span><span class="sxs-lookup"><span data-stu-id="fddab-180">If a code path that creates and disposes of `HttpClient` objects is frequently used, the app may exhaust available sockets.</span></span> <span data-ttu-id="fddab-181">[HttpClientFactory](/dotnet/standard/microservices-architecture/implement-resilient-applications/use-httpclientfactory-to-implement-resilient-http-requests) был представлен в ASP.NET Core 2.1 в качестве решения этой проблемы.</span><span class="sxs-lookup"><span data-stu-id="fddab-181">[HttpClientFactory](/dotnet/standard/microservices-architecture/implement-resilient-applications/use-httpclientfactory-to-implement-resilient-http-requests) was introduced in ASP.NET Core 2.1 as a solution to this problem.</span></span> <span data-ttu-id="fddab-182">Он обрабатывает объединение соединений HTTP для оптимизации производительности и надежности.</span><span class="sxs-lookup"><span data-stu-id="fddab-182">It handles pooling HTTP connections to optimize performance and reliability.</span></span>

<span data-ttu-id="fddab-183">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="fddab-183">Recommendations:</span></span>

* <span data-ttu-id="fddab-184">**Не создавайте** и `HttpClient` не распоряжайтесь экземплярами напрямую.</span><span class="sxs-lookup"><span data-stu-id="fddab-184">**Do not** create and dispose of `HttpClient` instances directly.</span></span>
* <span data-ttu-id="fddab-185">**Используйте** [httpClientFactory](/dotnet/standard/microservices-architecture/implement-resilient-applications/use-httpclientfactory-to-implement-resilient-http-requests) для `HttpClient` извлечения экземпляров.</span><span class="sxs-lookup"><span data-stu-id="fddab-185">**Do** use [HttpClientFactory](/dotnet/standard/microservices-architecture/implement-resilient-applications/use-httpclientfactory-to-implement-resilient-http-requests) to retrieve `HttpClient` instances.</span></span> <span data-ttu-id="fddab-186">Для получения дополнительной информации [см.](/dotnet/standard/microservices-architecture/implement-resilient-applications/use-httpclientfactory-to-implement-resilient-http-requests)</span><span class="sxs-lookup"><span data-stu-id="fddab-186">For more information, see [Use HttpClientFactory to implement resilient HTTP requests](/dotnet/standard/microservices-architecture/implement-resilient-applications/use-httpclientfactory-to-implement-resilient-http-requests).</span></span>

## <a name="keep-common-code-paths-fast"></a><span data-ttu-id="fddab-187">Держите общие пути кода быстро</span><span class="sxs-lookup"><span data-stu-id="fddab-187">Keep common code paths fast</span></span>

<span data-ttu-id="fddab-188">Вы хотите, чтобы все ваши коды были быстрыми.</span><span class="sxs-lookup"><span data-stu-id="fddab-188">You want all of your code to be fast.</span></span> <span data-ttu-id="fddab-189">Часто называемые пути кода являются наиболее важными для оптимизации.</span><span class="sxs-lookup"><span data-stu-id="fddab-189">Frequently-called code paths are the most critical to optimize.</span></span> <span data-ttu-id="fddab-190">Сюда входит следующее.</span><span class="sxs-lookup"><span data-stu-id="fddab-190">These include:</span></span>

* <span data-ttu-id="fddab-191">Компоненты Middleware в конвейере обработки запросов приложения, особенно промежуточное программное обеспечение, запущенное на ранних стадиях конвейера.</span><span class="sxs-lookup"><span data-stu-id="fddab-191">Middleware components in the app's request processing pipeline, especially middleware run early in the pipeline.</span></span> <span data-ttu-id="fddab-192">Эти компоненты оказывают большое влияние на производительность.</span><span class="sxs-lookup"><span data-stu-id="fddab-192">These components have a large impact on performance.</span></span>
* <span data-ttu-id="fddab-193">Код, выполняемый для каждого запроса или несколько раз за запрос.</span><span class="sxs-lookup"><span data-stu-id="fddab-193">Code that's executed for every request or multiple times per request.</span></span> <span data-ttu-id="fddab-194">Например, пользовательские журналы, обработчики авторизации или инициализация временных служб.</span><span class="sxs-lookup"><span data-stu-id="fddab-194">For example, custom logging, authorization handlers, or initialization of transient services.</span></span>

<span data-ttu-id="fddab-195">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="fddab-195">Recommendations:</span></span>

* <span data-ttu-id="fddab-196">**Не** используйте пользовательские компоненты промежуточного посуды с длительными задачами.</span><span class="sxs-lookup"><span data-stu-id="fddab-196">**Do not** use custom middleware components with long-running tasks.</span></span>
* <span data-ttu-id="fddab-197">**Используйте** инструменты профилирования производительности, такие как [Visual Studio Diagnostic Tools](/visualstudio/profiling/profiling-feature-tour) или [PerfView),](https://github.com/Microsoft/perfview)для определения [путей горячего кода.](#understand-hot-code-paths)</span><span class="sxs-lookup"><span data-stu-id="fddab-197">**Do** use performance profiling tools, such as [Visual Studio Diagnostic Tools](/visualstudio/profiling/profiling-feature-tour) or [PerfView](https://github.com/Microsoft/perfview)), to identify [hot code paths](#understand-hot-code-paths).</span></span>

## <a name="complete-long-running-tasks-outside-of-http-requests"></a><span data-ttu-id="fddab-198">Полные длительные задачи за пределами запросов HTTP</span><span class="sxs-lookup"><span data-stu-id="fddab-198">Complete long-running Tasks outside of HTTP requests</span></span>

<span data-ttu-id="fddab-199">Большинство запросов в ASP.NET приложение Core может обрабатываться контроллером или моделью страницы, вызывающей необходимые службы и возвращающей ответ HTTP.</span><span class="sxs-lookup"><span data-stu-id="fddab-199">Most requests to an ASP.NET Core app can be handled by a controller or page model calling necessary services and returning an HTTP response.</span></span> <span data-ttu-id="fddab-200">Для некоторых запросов, которые связаны с длительными задачами, лучше сделать весь процесс ответа запроса асинхронным.</span><span class="sxs-lookup"><span data-stu-id="fddab-200">For some requests that involve long-running tasks, it's better to make the entire request-response process asynchronous.</span></span>

<span data-ttu-id="fddab-201">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="fddab-201">Recommendations:</span></span>

* <span data-ttu-id="fddab-202">**Не** ждите длительных задач, которые будут выполнены в рамках обычной обработки запросов HTTP.</span><span class="sxs-lookup"><span data-stu-id="fddab-202">**Do not** wait for long-running tasks to complete as part of ordinary HTTP request processing.</span></span>
* <span data-ttu-id="fddab-203">**Рассмотрите** возможность обработки долгосрочных запросов с [помощью фоновых служб](xref:fundamentals/host/hosted-services) или вне процесса с [функцией Azure.](/azure/azure-functions/)</span><span class="sxs-lookup"><span data-stu-id="fddab-203">**Do** consider handling long-running requests with [background services](xref:fundamentals/host/hosted-services) or out of process with an [Azure Function](/azure/azure-functions/).</span></span> <span data-ttu-id="fddab-204">Завершение работы вне процесса особенно полезно для процессора-интенсивных задач.</span><span class="sxs-lookup"><span data-stu-id="fddab-204">Completing work out-of-process is especially beneficial for CPU-intensive tasks.</span></span>
* <span data-ttu-id="fddab-205">**Используйте** варианты общения в [SignalR](xref:signalr/introduction)режиме реального времени, такие как, чтобы общаться с клиентами асинхронно.</span><span class="sxs-lookup"><span data-stu-id="fddab-205">**Do** use real-time communication options, such as [SignalR](xref:signalr/introduction), to communicate with clients asynchronously.</span></span>

## <a name="minify-client-assets"></a><span data-ttu-id="fddab-206">Миниатюрные клиентские активы</span><span class="sxs-lookup"><span data-stu-id="fddab-206">Minify client assets</span></span>

<span data-ttu-id="fddab-207">ASP.NET приложения Core со сложными передними концами часто обслуживают множество файлов JavaScript, CSS или изображений.</span><span class="sxs-lookup"><span data-stu-id="fddab-207">ASP.NET Core apps with complex front-ends frequently serve many JavaScript, CSS, or image files.</span></span> <span data-ttu-id="fddab-208">Производительность первоначальных запросов нагрузки может быть улучшена за счет:</span><span class="sxs-lookup"><span data-stu-id="fddab-208">Performance of initial load requests can be improved by:</span></span>

* <span data-ttu-id="fddab-209">Комплектация, которая объединяет несколько файлов в один.</span><span class="sxs-lookup"><span data-stu-id="fddab-209">Bundling, which combines multiple files into one.</span></span>
* <span data-ttu-id="fddab-210">Минулямия, которая уменьшает размер файлов, удаляя пробел и комментарии.</span><span class="sxs-lookup"><span data-stu-id="fddab-210">Minifying, which reduces the size of files by removing whitespace and comments.</span></span>

<span data-ttu-id="fddab-211">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="fddab-211">Recommendations:</span></span>

* <span data-ttu-id="fddab-212">**Используйте** [встроенную поддержку](xref:client-side/bundling-and-minification) ASP.NET Core для объединения и минимации клиентских активов.</span><span class="sxs-lookup"><span data-stu-id="fddab-212">**Do** use ASP.NET Core's [built-in support](xref:client-side/bundling-and-minification) for bundling and minifying client assets.</span></span>
* <span data-ttu-id="fddab-213">**Рассмотрите** другие сторонние инструменты, такие как [Webpack,](https://webpack.js.org/)для комплексного управления активами клиентов.</span><span class="sxs-lookup"><span data-stu-id="fddab-213">**Do** consider other third-party tools, such as [Webpack](https://webpack.js.org/), for complex client asset management.</span></span>

## <a name="compress-responses"></a><span data-ttu-id="fddab-214">Компресс ответы</span><span class="sxs-lookup"><span data-stu-id="fddab-214">Compress responses</span></span>

 <span data-ttu-id="fddab-215">Уменьшение размера ответа обычно увеличивает отзывчивость приложения, часто резко.</span><span class="sxs-lookup"><span data-stu-id="fddab-215">Reducing the size of the response usually increases the responsiveness of an app, often dramatically.</span></span> <span data-ttu-id="fddab-216">Одним из способов уменьшения размеров полезной нагрузки является сжатие ответов приложения.</span><span class="sxs-lookup"><span data-stu-id="fddab-216">One way to reduce payload sizes is to compress an app's responses.</span></span> <span data-ttu-id="fddab-217">Для получения дополнительной [информации см.](xref:performance/response-compression)</span><span class="sxs-lookup"><span data-stu-id="fddab-217">For more information, see [Response compression](xref:performance/response-compression).</span></span>

## <a name="use-the-latest-aspnet-core-release"></a><span data-ttu-id="fddab-218">Используйте последний релиз ASP.NET Core</span><span class="sxs-lookup"><span data-stu-id="fddab-218">Use the latest ASP.NET Core release</span></span>

<span data-ttu-id="fddab-219">Каждый новый выпуск ASP.NET Core включает в себя повышение производительности.</span><span class="sxs-lookup"><span data-stu-id="fddab-219">Each new release of ASP.NET Core includes performance improvements.</span></span> <span data-ttu-id="fddab-220">Оптимизация в .NET Core и ASP.NET Core означает, что новые версии обычно превосходят старые версии.</span><span class="sxs-lookup"><span data-stu-id="fddab-220">Optimizations in .NET Core and ASP.NET Core mean that newer versions generally outperform older versions.</span></span> <span data-ttu-id="fddab-221">Например, .NET Core 2.1 добавил поддержку для компилированных регулярных выражений и получил выгоду от [Span\<T>. ](https://msdn.microsoft.com/magazine/mt814808.aspx)</span><span class="sxs-lookup"><span data-stu-id="fddab-221">For example, .NET Core 2.1 added support for compiled regular expressions and benefitted from [Span\<T>](https://msdn.microsoft.com/magazine/mt814808.aspx).</span></span> <span data-ttu-id="fddab-222">ASP.NET Core 2.2 добавил поддержку HTTP/2.</span><span class="sxs-lookup"><span data-stu-id="fddab-222">ASP.NET Core 2.2 added support for HTTP/2.</span></span> <span data-ttu-id="fddab-223">[ASP.NET Core 3.0 добавляет множество улучшений,](xref:aspnetcore-3.0) которые снижают использование памяти и улучшают пропускную работу.</span><span class="sxs-lookup"><span data-stu-id="fddab-223">[ASP.NET Core 3.0 adds many improvements](xref:aspnetcore-3.0) that reduce memory usage and improve throughput.</span></span> <span data-ttu-id="fddab-224">Если производительность является приоритетной задачей, рассмотрите возможность обновления до текущей версии ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="fddab-224">If performance is a priority, consider upgrading to the current version of ASP.NET Core.</span></span>

## <a name="minimize-exceptions"></a><span data-ttu-id="fddab-225">Минимизация исключений</span><span class="sxs-lookup"><span data-stu-id="fddab-225">Minimize exceptions</span></span>

<span data-ttu-id="fddab-226">Исключения должны быть редкими.</span><span class="sxs-lookup"><span data-stu-id="fddab-226">Exceptions should be rare.</span></span> <span data-ttu-id="fddab-227">Исключение метания и ловли происходит медленно по сравнению с другими шаблонами потока кода.</span><span class="sxs-lookup"><span data-stu-id="fddab-227">Throwing and catching exceptions is slow relative to other code flow patterns.</span></span> <span data-ttu-id="fddab-228">Из-за этого исключения не должны использоваться для управления обычным потоком программ.</span><span class="sxs-lookup"><span data-stu-id="fddab-228">Because of this, exceptions shouldn't be used to control normal program flow.</span></span>

<span data-ttu-id="fddab-229">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="fddab-229">Recommendations:</span></span>

* <span data-ttu-id="fddab-230">**Не** используйте метание или ловлю исключений в качестве средства нормального потока программы, особенно в [горячих путей кода.](#understand-hot-code-paths)</span><span class="sxs-lookup"><span data-stu-id="fddab-230">**Do not** use throwing or catching exceptions as a means of normal program flow, especially in [hot code paths](#understand-hot-code-paths).</span></span>
* <span data-ttu-id="fddab-231">**Включите** логику в приложение для обнаружения и обработки условий, которые могут привести к исключению.</span><span class="sxs-lookup"><span data-stu-id="fddab-231">**Do** include logic in the app to detect and handle conditions that would cause an exception.</span></span>
* <span data-ttu-id="fddab-232">**Бросьте** или поймать исключения для необычных или неожиданных условий.</span><span class="sxs-lookup"><span data-stu-id="fddab-232">**Do** throw or catch exceptions for unusual or unexpected conditions.</span></span>

<span data-ttu-id="fddab-233">Диагностические инструменты приложения, такие как Application Insights, могут помочь определить общие исключения в приложении, которые могут повлиять на производительность.</span><span class="sxs-lookup"><span data-stu-id="fddab-233">App diagnostic tools, such as Application Insights, can help to identify common exceptions in an app that may affect performance.</span></span>

## <a name="performance-and-reliability"></a><span data-ttu-id="fddab-234">Производительность и надежность</span><span class="sxs-lookup"><span data-stu-id="fddab-234">Performance and reliability</span></span>

<span data-ttu-id="fddab-235">Следующие разделы предоставляют советы по производительности и известные проблемы с надежностью и решения.</span><span class="sxs-lookup"><span data-stu-id="fddab-235">The following sections provide performance tips and known reliability problems and solutions.</span></span>

## <a name="avoid-synchronous-read-or-write-on-httprequesthttpresponse-body"></a><span data-ttu-id="fddab-236">Избегайте синхронного чтения или записи на httprequest/HttpResponse тела</span><span class="sxs-lookup"><span data-stu-id="fddab-236">Avoid synchronous read or write on HttpRequest/HttpResponse body</span></span>

<span data-ttu-id="fddab-237">Все ввод/вASP.NET Core является асинхронным.</span><span class="sxs-lookup"><span data-stu-id="fddab-237">All I/O in ASP.NET Core is asynchronous.</span></span> <span data-ttu-id="fddab-238">Серверы `Stream` реализуют интерфейс, который имеет как синхронные, так и асинхронные перегрузки.</span><span class="sxs-lookup"><span data-stu-id="fddab-238">Servers implement the `Stream` interface, which has both synchronous and asynchronous overloads.</span></span> <span data-ttu-id="fddab-239">Асинхронные из них следует предпочесть, чтобы избежать блокирования потоков пула потоков.</span><span class="sxs-lookup"><span data-stu-id="fddab-239">The asynchronous ones should be preferred to avoid blocking thread pool threads.</span></span> <span data-ttu-id="fddab-240">Блокирование потоков может привести к голоду пула потоков.</span><span class="sxs-lookup"><span data-stu-id="fddab-240">Blocking threads can lead to thread pool starvation.</span></span>

<span data-ttu-id="fddab-241">**Не делайте этого:** В следующем примере <xref:System.IO.StreamReader.ReadToEnd*>используется .</span><span class="sxs-lookup"><span data-stu-id="fddab-241">**Do not do this:** The following example uses the <xref:System.IO.StreamReader.ReadToEnd*>.</span></span> <span data-ttu-id="fddab-242">Он блокирует текущий поток, чтобы ждать результата.</span><span class="sxs-lookup"><span data-stu-id="fddab-242">It blocks the current thread to wait for the result.</span></span> <span data-ttu-id="fddab-243">Это пример [синхронизации над async](https://github.com/davidfowl/AspNetCoreDiagnosticScenarios/blob/master/AsyncGuidance.md#warning-sync-over-async
).</span><span class="sxs-lookup"><span data-stu-id="fddab-243">This is an example of [sync over async](https://github.com/davidfowl/AspNetCoreDiagnosticScenarios/blob/master/AsyncGuidance.md#warning-sync-over-async
).</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/MyFirstController.cs?name=snippet1)]

<span data-ttu-id="fddab-244">В предыдущем коде `Get` синхронно считывает весь тело запроса HTTP в память.</span><span class="sxs-lookup"><span data-stu-id="fddab-244">In the preceding code, `Get` synchronously reads the entire HTTP request body into memory.</span></span> <span data-ttu-id="fddab-245">Если клиент медленно загружается, приложение синхронизируется над async.</span><span class="sxs-lookup"><span data-stu-id="fddab-245">If the client is slowly uploading, the app is doing sync over async.</span></span> <span data-ttu-id="fddab-246">Приложение синхронизируется над async, потому что Kestrel **не** поддерживает синхронные чтения.</span><span class="sxs-lookup"><span data-stu-id="fddab-246">The app does sync over async because Kestrel does **NOT** support synchronous reads.</span></span>

<span data-ttu-id="fddab-247">**Сделайте это:** Следующий пример <xref:System.IO.StreamReader.ReadToEndAsync*> использует и не блокирует поток во время чтения.</span><span class="sxs-lookup"><span data-stu-id="fddab-247">**Do this:** The following example uses <xref:System.IO.StreamReader.ReadToEndAsync*> and does not block the thread while reading.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/MyFirstController.cs?name=snippet2)]

<span data-ttu-id="fddab-248">Предыдущий код асинхронно считывает весь тело запроса HTTP в память.</span><span class="sxs-lookup"><span data-stu-id="fddab-248">The preceding code asynchronously reads the entire HTTP request body into memory.</span></span>

> [!WARNING]
> <span data-ttu-id="fddab-249">Если запрос большой, чтение всего тела запроса HTTP в память может привести к изврцеловане (OOM) состоянию.</span><span class="sxs-lookup"><span data-stu-id="fddab-249">If the request is large, reading the entire HTTP request body into memory could lead to an out of memory (OOM) condition.</span></span> <span data-ttu-id="fddab-250">OOM может привести к отказу в обслуживании.</span><span class="sxs-lookup"><span data-stu-id="fddab-250">OOM can result in a Denial Of Service.</span></span>  <span data-ttu-id="fddab-251">Для получения дополнительной информации в этом документе [см.](#arlb)</span><span class="sxs-lookup"><span data-stu-id="fddab-251">For more information, see [Avoid reading large request bodies or response bodies into memory](#arlb) in this document.</span></span>

<span data-ttu-id="fddab-252">**Сделайте это:** Следующий пример полностью асинхронный с использованием тела не буферизированного запроса:</span><span class="sxs-lookup"><span data-stu-id="fddab-252">**Do this:** The following example is fully asynchronous using a non buffered request body:</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/MyFirstController.cs?name=snippet3)]

<span data-ttu-id="fddab-253">Предыдущий код асинхронно десериализирует тело запроса в объект C..</span><span class="sxs-lookup"><span data-stu-id="fddab-253">The preceding code asynchronously de-serializes the request body into a C# object.</span></span>

## <a name="prefer-readformasync-over-requestform"></a><span data-ttu-id="fddab-254">Предпочитаю readFormAsync по сравнению с Request.Form</span><span class="sxs-lookup"><span data-stu-id="fddab-254">Prefer ReadFormAsync over Request.Form</span></span>

<span data-ttu-id="fddab-255">Используйте `HttpContext.Request.ReadFormAsync` вместо `HttpContext.Request.Form`.</span><span class="sxs-lookup"><span data-stu-id="fddab-255">Use `HttpContext.Request.ReadFormAsync` instead of `HttpContext.Request.Form`.</span></span>
<span data-ttu-id="fddab-256">`HttpContext.Request.Form`безопасно ею можно читать только при следующих условиях:</span><span class="sxs-lookup"><span data-stu-id="fddab-256">`HttpContext.Request.Form` can be safely read only with the following conditions:</span></span>

* <span data-ttu-id="fddab-257">Форма была прочитана по `ReadFormAsync`призыву к</span><span class="sxs-lookup"><span data-stu-id="fddab-257">The form has been read by a call to `ReadFormAsync`, and</span></span>
* <span data-ttu-id="fddab-258">Значение кэшированной формы считывается с помощью`HttpContext.Request.Form`</span><span class="sxs-lookup"><span data-stu-id="fddab-258">The cached form value is being read using `HttpContext.Request.Form`</span></span>

<span data-ttu-id="fddab-259">**Не делайте этого:** В следующем `HttpContext.Request.Form`примере используется .</span><span class="sxs-lookup"><span data-stu-id="fddab-259">**Do not do this:** The following example uses `HttpContext.Request.Form`.</span></span>  <span data-ttu-id="fddab-260">`HttpContext.Request.Form`синхронизирует [синхронизацию над async](https://github.com/davidfowl/AspNetCoreDiagnosticScenarios/blob/master/AsyncGuidance.md#warning-sync-over-async
) и может привести к голоду пула потоков.</span><span class="sxs-lookup"><span data-stu-id="fddab-260">`HttpContext.Request.Form` uses [sync over async](https://github.com/davidfowl/AspNetCoreDiagnosticScenarios/blob/master/AsyncGuidance.md#warning-sync-over-async
) and can lead to thread pool starvation.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/MySecondController.cs?name=snippet1)]

<span data-ttu-id="fddab-261">**Сделайте это:** Следующий пример `HttpContext.Request.ReadFormAsync` использует для чтения формы тела асинхронно.</span><span class="sxs-lookup"><span data-stu-id="fddab-261">**Do this:** The following example uses `HttpContext.Request.ReadFormAsync` to read the form body asynchronously.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/MySecondController.cs?name=snippet2)]

<a name="arlb"></a>

## <a name="avoid-reading-large-request-bodies-or-response-bodies-into-memory"></a><span data-ttu-id="fddab-262">Избегайте чтения больших тел запросов или ответных органов в память</span><span class="sxs-lookup"><span data-stu-id="fddab-262">Avoid reading large request bodies or response bodies into memory</span></span>

<span data-ttu-id="fddab-263">В .NET каждое распределение объектов, превышающее 85 кБ, попадает в большую объектную кучу[(LOH).](https://blogs.msdn.microsoft.com/maoni/2006/04/19/large-object-heap/)</span><span class="sxs-lookup"><span data-stu-id="fddab-263">In .NET, every object allocation greater than 85 KB ends up in the large object heap ([LOH](https://blogs.msdn.microsoft.com/maoni/2006/04/19/large-object-heap/)).</span></span> <span data-ttu-id="fddab-264">Крупные объекты стоят дорого двумя способами:</span><span class="sxs-lookup"><span data-stu-id="fddab-264">Large objects are expensive in two ways:</span></span>

* <span data-ttu-id="fddab-265">Стоимость выделения высока, поскольку память для недавно выделенного крупного объекта должна быть очищена.</span><span class="sxs-lookup"><span data-stu-id="fddab-265">The allocation cost is high because the memory for a newly allocated large object has to be cleared.</span></span> <span data-ttu-id="fddab-266">CLR гарантирует, что память для всех вновь выделенных объектов очищается.</span><span class="sxs-lookup"><span data-stu-id="fddab-266">The CLR guarantees that memory for all newly allocated objects is cleared.</span></span>
* <span data-ttu-id="fddab-267">LOH собирается с остальной частью кучи.</span><span class="sxs-lookup"><span data-stu-id="fddab-267">LOH is collected with the rest of the heap.</span></span> <span data-ttu-id="fddab-268">LOH требует полного [сбора мусора](/dotnet/standard/garbage-collection/fundamentals) или [сбора Gen2.](/dotnet/standard/garbage-collection/fundamentals#generations)</span><span class="sxs-lookup"><span data-stu-id="fddab-268">LOH requires a full [garbage collection](/dotnet/standard/garbage-collection/fundamentals) or [Gen2 collection](/dotnet/standard/garbage-collection/fundamentals#generations).</span></span>

<span data-ttu-id="fddab-269">Этот [блог](https://adamsitnik.com/Array-Pool/#the-problem) описывает проблему кратко:</span><span class="sxs-lookup"><span data-stu-id="fddab-269">This [blog post](https://adamsitnik.com/Array-Pool/#the-problem) describes the problem succinctly:</span></span>

> <span data-ttu-id="fddab-270">При выделении крупного объекта он помечается как объект Gen 2.</span><span class="sxs-lookup"><span data-stu-id="fddab-270">When a large object is allocated, it's marked as Gen 2 object.</span></span> <span data-ttu-id="fddab-271">Не Gen 0, как для небольших объектов.</span><span class="sxs-lookup"><span data-stu-id="fddab-271">Not Gen 0 as for small objects.</span></span> <span data-ttu-id="fddab-272">Последствия в том, что если вы бежите из памяти в LOH, GC очищает всю управляемую кучу, а не только LOH.</span><span class="sxs-lookup"><span data-stu-id="fddab-272">The consequences are that if you run out of memory in LOH, GC cleans up the whole managed heap, not only LOH.</span></span> <span data-ttu-id="fddab-273">Таким образом, он очищает Gen 0, Gen 1 и Gen 2 в том числе LOH.</span><span class="sxs-lookup"><span data-stu-id="fddab-273">So it cleans up Gen 0, Gen 1 and Gen 2 including LOH.</span></span> <span data-ttu-id="fddab-274">Это называется полным сбором мусора и является наиболее трудоемким сбором мусора.</span><span class="sxs-lookup"><span data-stu-id="fddab-274">This is called full garbage collection and is the most time-consuming garbage collection.</span></span> <span data-ttu-id="fddab-275">Для многих приложений это может быть приемлемым.</span><span class="sxs-lookup"><span data-stu-id="fddab-275">For many applications, it can be acceptable.</span></span> <span data-ttu-id="fddab-276">Но определенно не для высокопроизводительных веб-серверов, где несколько больших буферов памяти необходимы для обработки среднего веб-запроса (читай из гнезда, декомпресс, расшифровка JSON & больше).</span><span class="sxs-lookup"><span data-stu-id="fddab-276">But definitely not for high-performance web servers, where few big memory buffers are needed to handle an average web request (read from a socket, decompress, decode JSON & more).</span></span>

<span data-ttu-id="fddab-277">Наивное хранение большого органа запроса `byte[]` `string`или ответа в один или:</span><span class="sxs-lookup"><span data-stu-id="fddab-277">Naively storing a large request or response body into a single `byte[]` or `string`:</span></span>

* <span data-ttu-id="fddab-278">Может привести к быстрому иссяке места в LOH.</span><span class="sxs-lookup"><span data-stu-id="fddab-278">May result in quickly running out of space in the LOH.</span></span>
* <span data-ttu-id="fddab-279">Может вызвать проблемы с производительностью для приложения из-за полного выполнения GCs.</span><span class="sxs-lookup"><span data-stu-id="fddab-279">May cause performance issues for the app because of full GCs running.</span></span>

## <a name="working-with-a-synchronous-data-processing-api"></a><span data-ttu-id="fddab-280">Работа с синхронной обработкой данных API</span><span class="sxs-lookup"><span data-stu-id="fddab-280">Working with a synchronous data processing API</span></span>

<span data-ttu-id="fddab-281">При использовании сериализатора/де-серизатора, который поддерживает только синхронные чтения и записи (например, [JSON.NET):](https://www.newtonsoft.com/json/help/html/Introduction.htm)</span><span class="sxs-lookup"><span data-stu-id="fddab-281">When using a serializer/de-serializer that only supports synchronous reads and writes (for example,  [JSON.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm)):</span></span>

* <span data-ttu-id="fddab-282">Buffer данные в память асинхронно перед передачей его в serializer / де-сериализатор.</span><span class="sxs-lookup"><span data-stu-id="fddab-282">Buffer the data into memory asynchronously before passing it into the serializer/de-serializer.</span></span>

> [!WARNING]
> <span data-ttu-id="fddab-283">Если запрос большой, это может привести к изврянке памяти (OOM) состояние.</span><span class="sxs-lookup"><span data-stu-id="fddab-283">If the request is large, it could lead to an out of memory (OOM) condition.</span></span> <span data-ttu-id="fddab-284">OOM может привести к отказу в обслуживании.</span><span class="sxs-lookup"><span data-stu-id="fddab-284">OOM can result in a Denial Of Service.</span></span>  <span data-ttu-id="fddab-285">Для получения дополнительной информации в этом документе [см.](#arlb)</span><span class="sxs-lookup"><span data-stu-id="fddab-285">For more information, see [Avoid reading large request bodies or response bodies into memory](#arlb) in this document.</span></span>

<span data-ttu-id="fddab-286">ASP.NET Core 3.0 использует <xref:System.Text.Json> по умолчанию для сериализации JSON.</span><span class="sxs-lookup"><span data-stu-id="fddab-286">ASP.NET Core 3.0 uses <xref:System.Text.Json> by default for JSON serialization.</span></span> <span data-ttu-id="fddab-287"><xref:System.Text.Json>:</span><span class="sxs-lookup"><span data-stu-id="fddab-287"><xref:System.Text.Json>:</span></span>

* <span data-ttu-id="fddab-288">асинхронно считывает и записывает JSON;</span><span class="sxs-lookup"><span data-stu-id="fddab-288">Reads and writes JSON asynchronously.</span></span>
* <span data-ttu-id="fddab-289">оптимизирован для текста UTF-8;</span><span class="sxs-lookup"><span data-stu-id="fddab-289">Is optimized for UTF-8 text.</span></span>
* <span data-ttu-id="fddab-290">предоставляет более высокую производительность, чем `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="fddab-290">Typically higher performance than `Newtonsoft.Json`.</span></span>

## <a name="do-not-store-ihttpcontextaccessorhttpcontext-in-a-field"></a><span data-ttu-id="fddab-291">Не храните IHttpContextaccessor.HttpContext в поле</span><span class="sxs-lookup"><span data-stu-id="fddab-291">Do not store IHttpContextAccessor.HttpContext in a field</span></span>

<span data-ttu-id="fddab-292">[IHttpContextAccessor.httpContext](xref:Microsoft.AspNetCore.Http.IHttpContextAccessor.HttpContext) возвращает `HttpContext` активный запрос при доступе из потока запроса.</span><span class="sxs-lookup"><span data-stu-id="fddab-292">The [IHttpContextAccessor.HttpContext](xref:Microsoft.AspNetCore.Http.IHttpContextAccessor.HttpContext) returns the `HttpContext` of the active request when accessed from the request thread.</span></span> <span data-ttu-id="fddab-293">Не `IHttpContextAccessor.HttpContext` **следует** хранить в поле или переменной.</span><span class="sxs-lookup"><span data-stu-id="fddab-293">The `IHttpContextAccessor.HttpContext` should **not** be stored in a field or variable.</span></span>

<span data-ttu-id="fddab-294">**Не делайте этого:** Следующий пример `HttpContext` хранит в поле, а затем пытается использовать его позже.</span><span class="sxs-lookup"><span data-stu-id="fddab-294">**Do not do this:** The following example stores the `HttpContext` in a field and then attempts to use it later.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/MyType.cs?name=snippet1)]

<span data-ttu-id="fddab-295">Предыдущий код часто фиксирует `HttpContext` нулевую или неправильную в конструкторе.</span><span class="sxs-lookup"><span data-stu-id="fddab-295">The preceding code frequently captures a null or incorrect `HttpContext` in the constructor.</span></span>

<span data-ttu-id="fddab-296">**Сделайте это:** Следующий пример:</span><span class="sxs-lookup"><span data-stu-id="fddab-296">**Do this:** The following example:</span></span>

* <span data-ttu-id="fddab-297">Хранит <xref:Microsoft.AspNetCore.Http.IHttpContextAccessor> в поле.</span><span class="sxs-lookup"><span data-stu-id="fddab-297">Stores the <xref:Microsoft.AspNetCore.Http.IHttpContextAccessor> in a field.</span></span>
* <span data-ttu-id="fddab-298">Использует `HttpContext` поле в нужное время `null`и проверяет.</span><span class="sxs-lookup"><span data-stu-id="fddab-298">Uses the `HttpContext` field at the correct time and checks for `null`.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/MyType.cs?name=snippet2)]

## <a name="do-not-access-httpcontext-from-multiple-threads"></a><span data-ttu-id="fddab-299">Не получите доступ к HttpContext из нескольких потоков</span><span class="sxs-lookup"><span data-stu-id="fddab-299">Do not access HttpContext from multiple threads</span></span>

<span data-ttu-id="fddab-300">`HttpContext`*НЕ* является безопасным потоком.</span><span class="sxs-lookup"><span data-stu-id="fddab-300">`HttpContext` is *NOT* thread-safe.</span></span> <span data-ttu-id="fddab-301">Параллельный `HttpContext` доступ из нескольких потоков может привести к неопределенным поведением, таким как зависания, сбои и повреждения данных.</span><span class="sxs-lookup"><span data-stu-id="fddab-301">Accessing `HttpContext` from multiple threads in parallel can result in undefined behavior such as hangs, crashes, and data corruption.</span></span>

<span data-ttu-id="fddab-302">**Не делайте этого:** Следующий пример делает три параллельных запроса и регистрирует входящий путь запроса до и после исходящего запроса HTTP.</span><span class="sxs-lookup"><span data-stu-id="fddab-302">**Do not do this:** The following example makes three parallel requests and logs the incoming request path before and after the outgoing HTTP request.</span></span> <span data-ttu-id="fddab-303">Путь запроса доступен из нескольких потоков, потенциально параллельно.</span><span class="sxs-lookup"><span data-stu-id="fddab-303">The request path is accessed from multiple threads, potentially in parallel.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/AsyncFirstController.cs?name=snippet1&highlight=25,28)]

<span data-ttu-id="fddab-304">**Сделайте это:** Следующий пример копирует все данные из входящего запроса, прежде чем сделать три параллельных запроса.</span><span class="sxs-lookup"><span data-stu-id="fddab-304">**Do this:** The following example copies all data from the incoming request before making the three parallel requests.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/AsyncFirstController.cs?name=snippet2&highlight=6,8,22,28)]

## <a name="do-not-use-the-httpcontext-after-the-request-is-complete"></a><span data-ttu-id="fddab-305">Не используйте HttpContext после завершения запроса</span><span class="sxs-lookup"><span data-stu-id="fddab-305">Do not use the HttpContext after the request is complete</span></span>

<span data-ttu-id="fddab-306">`HttpContext`действителен только при наличии активного запроса HTTP в конвейере ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="fddab-306">`HttpContext` is only valid as long as there is an active HTTP request in the ASP.NET Core pipeline.</span></span> <span data-ttu-id="fddab-307">Весь конвейер ASP.NET Core представляет собой асинхронную цепочку делегатов, которая выполняет каждый запрос.</span><span class="sxs-lookup"><span data-stu-id="fddab-307">The entire ASP.NET Core pipeline is an asynchronous chain of delegates that executes every request.</span></span> <span data-ttu-id="fddab-308">Когда `Task` возвращенное из этой `HttpContext` цепочки завершается, перерабатывается.</span><span class="sxs-lookup"><span data-stu-id="fddab-308">When the `Task` returned from this chain completes, the `HttpContext` is recycled.</span></span>

<span data-ttu-id="fddab-309">**Не делайте этого:** Следующий пример `async void` использует, который делает запрос `await` HTTP завершен, когда первый достигнут:</span><span class="sxs-lookup"><span data-stu-id="fddab-309">**Do not do this:** The following example uses `async void` which makes the HTTP request complete when the first `await` is reached:</span></span>

* <span data-ttu-id="fddab-310">Что **всегда** плохая практика в ASP.NET приложений Core.</span><span class="sxs-lookup"><span data-stu-id="fddab-310">Which is **ALWAYS** a bad practice in ASP.NET Core apps.</span></span>
* <span data-ttu-id="fddab-311">Доступ к `HttpResponse` запросу после завершения http.</span><span class="sxs-lookup"><span data-stu-id="fddab-311">Accesses the `HttpResponse` after the HTTP request is complete.</span></span>
* <span data-ttu-id="fddab-312">Сбои процесса.</span><span class="sxs-lookup"><span data-stu-id="fddab-312">Crashes the process.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/AsyncBadVoidController.cs?name=snippet1)]

<span data-ttu-id="fddab-313">**Сделайте это:** Следующий пример `Task` возвращается в фреймворк, поэтому запрос HTTP не завершается до завершения действия.</span><span class="sxs-lookup"><span data-stu-id="fddab-313">**Do this:** The following example returns a `Task` to the framework, so the HTTP request doesn't complete until the action completes.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/AsyncSecondController.cs?name=snippet1)]

## <a name="do-not-capture-the-httpcontext-in-background-threads"></a><span data-ttu-id="fddab-314">Не захватывайте httpContext в фоновых потоках</span><span class="sxs-lookup"><span data-stu-id="fddab-314">Do not capture the HttpContext in background threads</span></span>

<span data-ttu-id="fddab-315">**Не делайте этого:** Следующий пример показывает, что `HttpContext` замыкание захватывает из `Controller` свойства.</span><span class="sxs-lookup"><span data-stu-id="fddab-315">**Do not do this:** The following example shows a closure is capturing the `HttpContext` from the `Controller` property.</span></span> <span data-ttu-id="fddab-316">Это плохая практика, потому что рабочий элемент может:</span><span class="sxs-lookup"><span data-stu-id="fddab-316">This is a bad practice because the work item could:</span></span>

* <span data-ttu-id="fddab-317">Выполнить вне сферы запроса.</span><span class="sxs-lookup"><span data-stu-id="fddab-317">Run outside of the request scope.</span></span>
* <span data-ttu-id="fddab-318">Попытка прочитать `HttpContext`неправильно .</span><span class="sxs-lookup"><span data-stu-id="fddab-318">Attempt to read the wrong `HttpContext`.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/FireAndForgetFirstController.cs?name=snippet1)]

<span data-ttu-id="fddab-319">**Сделайте это:** Следующий пример:</span><span class="sxs-lookup"><span data-stu-id="fddab-319">**Do this:** The following example:</span></span>

* <span data-ttu-id="fddab-320">Копирует данные, необходимые в фоновой задаче во время запроса.</span><span class="sxs-lookup"><span data-stu-id="fddab-320">Copies the data required in the background task during the request.</span></span>
* <span data-ttu-id="fddab-321">Не ссылается на что-либо от контроллера.</span><span class="sxs-lookup"><span data-stu-id="fddab-321">Doesn't reference anything from the controller.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/FireAndForgetFirstController.cs?name=snippet2)]

<span data-ttu-id="fddab-322">Фоновые задачи должны выполняться как хосусуемые службы.</span><span class="sxs-lookup"><span data-stu-id="fddab-322">Background tasks should be implemented as hosted services.</span></span> <span data-ttu-id="fddab-323">Дополнительные сведения см. в статье [Background tasks with hosted services in ASP.NET Core](xref:fundamentals/host/hosted-services) (Фоновые задачи с размещенными службами в ASP.NET Core).</span><span class="sxs-lookup"><span data-stu-id="fddab-323">For more information, see [Background tasks with hosted services](xref:fundamentals/host/hosted-services).</span></span>

## <a name="do-not-capture-services-injected-into-the-controllers-on-background-threads"></a><span data-ttu-id="fddab-324">Не захватывайте службы, вводимые в контроллеры на фоновых потоках</span><span class="sxs-lookup"><span data-stu-id="fddab-324">Do not capture services injected into the controllers on background threads</span></span>

<span data-ttu-id="fddab-325">**Не делайте этого:** Следующий пример показывает, что `DbContext` замыкание захватывает параметр `Controller` действия.</span><span class="sxs-lookup"><span data-stu-id="fddab-325">**Do not do this:** The following example shows a closure is capturing the `DbContext` from the `Controller` action parameter.</span></span> <span data-ttu-id="fddab-326">Это плохая практика.</span><span class="sxs-lookup"><span data-stu-id="fddab-326">This is a bad practice.</span></span>  <span data-ttu-id="fddab-327">Элемент работы может выполниться за пределами сферы запроса.</span><span class="sxs-lookup"><span data-stu-id="fddab-327">The work item could run outside of the request scope.</span></span> <span data-ttu-id="fddab-328">Область `ContosoDbContext` к запросу, в результате `ObjectDisposedException`чего .</span><span class="sxs-lookup"><span data-stu-id="fddab-328">The `ContosoDbContext` is scoped to the request, resulting in an `ObjectDisposedException`.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/FireAndForgetSecondController.cs?name=snippet1)]

<span data-ttu-id="fddab-329">**Сделайте это:** Следующий пример:</span><span class="sxs-lookup"><span data-stu-id="fddab-329">**Do this:** The following example:</span></span>

* <span data-ttu-id="fddab-330">Вводит для <xref:Microsoft.Extensions.DependencyInjection.IServiceScopeFactory> создания области в элементе фоновой работы.</span><span class="sxs-lookup"><span data-stu-id="fddab-330">Injects an <xref:Microsoft.Extensions.DependencyInjection.IServiceScopeFactory> in order to create a scope in the background work item.</span></span> <span data-ttu-id="fddab-331">`IServiceScopeFactory`является синглтон.</span><span class="sxs-lookup"><span data-stu-id="fddab-331">`IServiceScopeFactory` is a singleton.</span></span>
* <span data-ttu-id="fddab-332">Создает новую область впрыска зависимости в фоновом потоке.</span><span class="sxs-lookup"><span data-stu-id="fddab-332">Creates a new dependency injection scope in the background thread.</span></span>
* <span data-ttu-id="fddab-333">Не ссылается на что-либо от контроллера.</span><span class="sxs-lookup"><span data-stu-id="fddab-333">Doesn't reference anything from the controller.</span></span>
* <span data-ttu-id="fddab-334">Не захватывает `ContosoDbContext` от входящего запроса.</span><span class="sxs-lookup"><span data-stu-id="fddab-334">Doesn't capture the `ContosoDbContext` from the incoming request.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/FireAndForgetSecondController.cs?name=snippet2)]

<span data-ttu-id="fddab-335">Следующий выделенный код:</span><span class="sxs-lookup"><span data-stu-id="fddab-335">The following highlighted code:</span></span>

* <span data-ttu-id="fddab-336">Создает область для продолжительности службы фоновой операции и разрешает службы из нее.</span><span class="sxs-lookup"><span data-stu-id="fddab-336">Creates a scope for the lifetime of the background operation and resolves services from it.</span></span>
* <span data-ttu-id="fddab-337">Использует `ContosoDbContext` из правильной области.</span><span class="sxs-lookup"><span data-stu-id="fddab-337">Uses `ContosoDbContext` from the correct scope.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Controllers/FireAndForgetSecondController.cs?name=snippet2&highlight=9-16)]

## <a name="do-not-modify-the-status-code-or-headers-after-the-response-body-has-started"></a><span data-ttu-id="fddab-338">Не изменяйте код состояния или заголовки после запуска тела ответа</span><span class="sxs-lookup"><span data-stu-id="fddab-338">Do not modify the status code or headers after the response body has started</span></span>

<span data-ttu-id="fddab-339">ASP.NET Core не буфер амплина ЦИММ.</span><span class="sxs-lookup"><span data-stu-id="fddab-339">ASP.NET Core does not buffer the HTTP response body.</span></span> <span data-ttu-id="fddab-340">Первый раз, когда ответ написан:</span><span class="sxs-lookup"><span data-stu-id="fddab-340">The first time the response is written:</span></span>

* <span data-ttu-id="fddab-341">Заголовки отправляются вместе с этим куском тела клиенту.</span><span class="sxs-lookup"><span data-stu-id="fddab-341">The headers are sent along with that chunk of the body to the client.</span></span>
* <span data-ttu-id="fddab-342">Изменить заголовки ответов уже невозможно.</span><span class="sxs-lookup"><span data-stu-id="fddab-342">It's no longer possible to change response headers.</span></span>

<span data-ttu-id="fddab-343">**Не делайте этого:** Следующий код пытается добавить заголовки ответов после того, как ответ уже начался:</span><span class="sxs-lookup"><span data-stu-id="fddab-343">**Do not do this:** The following code tries to add response headers after the response has already started:</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Startup22.cs?name=snippet1)]

<span data-ttu-id="fddab-344">В предыдущем коде будет сделано исключение, `context.Response.Headers["test"] = "test value";` если `next()` он написал ответ.</span><span class="sxs-lookup"><span data-stu-id="fddab-344">In the preceding code, `context.Response.Headers["test"] = "test value";` will throw an exception if `next()` has written to the response.</span></span>

<span data-ttu-id="fddab-345">**Сделайте это:** Следующий пример проверяет, запущен ли ответ HTTP перед изменением заголовков.</span><span class="sxs-lookup"><span data-stu-id="fddab-345">**Do this:** The following example checks if the HTTP response has started before modifying the headers.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Startup22.cs?name=snippet2)]

<span data-ttu-id="fddab-346">**Сделайте это:** Следующий пример `HttpResponse.OnStarting` используется для установки заголовков до того, как заголовки ответов будут смыты к клиенту.</span><span class="sxs-lookup"><span data-stu-id="fddab-346">**Do this:** The following example uses `HttpResponse.OnStarting` to set the headers before the response headers are flushed to the client.</span></span>

<span data-ttu-id="fddab-347">Проверка того, не запущен ли ответ, позволяет зарегистрировать обратный вызов, который будет вызываться непосредственно перед написанием заголовков ответов.</span><span class="sxs-lookup"><span data-stu-id="fddab-347">Checking if the response has not started allows registering a callback that will be invoked just before response headers are written.</span></span> <span data-ttu-id="fddab-348">Проверка, не начался ли ответ:</span><span class="sxs-lookup"><span data-stu-id="fddab-348">Checking if the response has not started:</span></span>

* <span data-ttu-id="fddab-349">Предоставляет возможность приложения или переопределения заголовков как раз вовремя.</span><span class="sxs-lookup"><span data-stu-id="fddab-349">Provides the ability to append or override headers just in time.</span></span>
* <span data-ttu-id="fddab-350">Не требует знания следующего промежуточного посуды в конвейере.</span><span class="sxs-lookup"><span data-stu-id="fddab-350">Doesn't require knowledge of the next middleware in the pipeline.</span></span>

[!code-csharp[](performance-best-practices/samples/3.0/Startup22.cs?name=snippet3)]

## <a name="do-not-call-next-if-you-have-already-started-writing-to-the-response-body"></a><span data-ttu-id="fddab-351">Не звоните в следующий(), если вы уже начали писать в орган реагирования</span><span class="sxs-lookup"><span data-stu-id="fddab-351">Do not call next() if you have already started writing to the response body</span></span>

<span data-ttu-id="fddab-352">Компоненты будут вызываться только в том случае, если они могут обрабатывать и манипулировать ответом.</span><span class="sxs-lookup"><span data-stu-id="fddab-352">Components only expect to be called if it's possible for them to handle and manipulate the response.</span></span>
